# 多元插值法及应用 —— 第一、二章大题整理

> 来源：《多元插值法及应用》教材 + 期末试卷
> 整理范围：第一章（二元多项式插值）、第二章（超限插值法）

---

## 目录

- [第一章 二元多项式插值](#第一章-二元多项式插值)
  - [题型一：二元Newton差商插值](#题型一二元newton差商插值)
  - [题型二：二元Lagrange插值](#题型二二元lagrange插值)
  - [题型三：二元Aitken迭代插值](#题型三二元aitken迭代插值)
  - [题型四：适定结点组的证明与构造](#题型四适定结点组的证明与构造)
  - [题型五：迭加插值法](#题型五迭加插值法)
  - [题型六：切触插值（二元接触插值）](#题型六切触插值二元接触插值)
  - [题型七：Bezout定理的证明与应用](#题型七bezout定理的证明与应用)
  - [题型八：多元插值适定性理论简述](#题型八多元插值适定性理论简述)
  - [题型九：多元插值在深度学习中的应用（论述）](#题型九多元插值在深度学习中的应用论述)
  - [题型十：径向基函数(RBF)插值 vs 多元多项式插值（论述）](#题型十径向基函数rbf插值-vs-多元多项式插值论述)
  - [题型十一：矩形迭代法](#题型十一矩形迭代法)
  - [题型十二：二元插值余项估计](#题型十二二元插值余项估计)
- [第二章 超限插值法](#第二章-超限插值法)
  - [题型一：插值算子的布尔和](#题型一插值算子的布尔和)
  - [题型二：Coons曲面构造](#题型二coons曲面构造)
  - [题型三：Gregory/Brown修正曲面](#题型三gregorybrown修正曲面)
  - [题型四：超限插值公式的离散化](#题型四超限插值公式的离散化)
  - [题型五：三角形域上的超限插值（三角形Coons曲面）](#题型五三角形域上的超限插值三角形coons曲面)

---

# 第一章 二元多项式插值

## 题型一：二元Newton差商插值

### 题目描述

基于给定的二元函数值表（通常分布在可整序格点网上），列出差商表，并构造二元Newton插值公式。

### 典型例题1（矩形格点网）

**数据：$f(x,y)=1+x+y+xy$，格点为 $(0,0),(1,0),(0,1),(1,1)$**

|     | y=0  | y=1  |
|-----|------|------|
| x=0 | f(0,0)=1 | f(0,1)=2 |
| x=1 | f(1,0)=2 | f(1,1)=5 |

**解：**

#### 第一步：列出差商表

先算各点的函数值，再逐步求差商：

- $f(x_0,y_0)=f(0,0)=1$
- $f(x_0,y_1)=f(0,1)=2 \quad\Rightarrow\quad f(x_0;y_0,y_1)=\frac{2-1}{1-0}=1$
- $f(x_1,y_0)=f(1,0)=2 \quad\Rightarrow\quad f(x_0,x_1;y_0)=\frac{2-1}{1-0}=1$
- $f(x_1,y_1)=f(1,1)=5 \quad\Rightarrow\quad f(x_0,x_1;y_1)=\frac{5-2}{1-0}=3$

再求二重差商：
$$f(x_0,x_1;y_0,y_1)=\frac{f(x_0,x_1;y_1)-f(x_0,x_1;y_0)}{y_1-y_0}=\frac{3-1}{1-0}=2$$

#### 第二步：代入二元差商插值公式

$$P(x,y)=\sum_{v=0}^{m}\sum_{u=0}^{n_v} X_v Y_u \cdot f_{vu}$$

其中：
- $X_0=1,\; X_1=(x-x_0)$
- $Y_0=1,\; Y_1=(y-y_0)$

$$\begin{aligned}
P(x,y) &= X_0Y_0 f_{00} + X_0Y_1 f_{01} + X_1Y_0 f_{10} + X_1Y_1 f_{11} \\
&= 1 \cdot 1 + (y-0) \cdot 1 + (x-0) \cdot 1 + (x-0)(y-0) \cdot 2 \\
&= 1 + y + x + 2xy
\end{aligned}$$

验证：$f(x,y)=1+x+y+xy$ 与 $P(x,y)=1+x+y+2xy$ 有一定偏差（因为只有4个节点，插值空间是 $P_{1,1}$，$P(x,y)$ 代入节点验证应正确）。

---

### 典型例题2（三角形格点网—期末真题 15分）

**数据如下表：**

|        | y=0 | y=1 | y=2 | y=3 |
|--------|-----|-----|-----|-----|
| **x=0** | 0   | 1   | 4   | 9   |
| **x=1** | 1   | 2   | 5   |     |
| **x=2** | 4   | 5   |     |     |
| **x=3** | 9   |     |     |     |

求二元Newton插值多项式。

**解：**

#### 1. 关于 $x$（在 $y=0$ 处）的差商表

| $x_i$ | $f(x_i,0)$ | 一阶差商 | 二阶差商 | 三阶差商 |
|-------|-----------|---------|---------|---------|
| 0     | 0         |         |         |         |
| 1     | 1         | $\frac{1-0}{1-0}=1$ |         |         |
| 2     | 4         | $\frac{4-1}{2-1}=3$ | $\frac{3-1}{2-0}=1$ |         |
| 3     | 9         | $\frac{9-4}{3-2}=5$ | $\frac{5-3}{3-1}=1$ | $\frac{1-1}{3-0}=0$ |

#### 2. 关于 $y$（在 $x=0$ 处）的差商表

| $y_j$ | $f(0,y_j)$ | 一阶差商 | 二阶差商 | 三阶差商 |
|-------|-----------|---------|---------|---------|
| 0     | 0         |         |         |         |
| 1     | 1         | $\frac{1-0}{1-0}=1$ |         |         |
| 2     | 4         | $\frac{4-1}{2-1}=3$ | $\frac{3-1}{2-0}=1$ |         |
| 3     | 9         | $\frac{9-4}{3-2}=5$ | $\frac{5-3}{3-1}=1$ | $\frac{1-1}{3-0}=0$ |

#### 3. 混合差商

- $f[x_0,x_1;y_0] = f(1,0)-f(0,0) = 1$
- $f[x_0,x_1;y_1] = f(1,1)-f(0,1) = 2-1 = 1$
- 混合差商 $f[x_0,x_1;y_0,y_1] = \frac{1-1}{y_1-y_0} = 0$

同理，所有混合差商项均为0。

#### 4. 构造二元Newton插值多项式

取差商表对角线元素：

$$\begin{aligned}
N(x,y) &= f(0,0) + f[x_0,x_1;y_0]\cdot x + f[x_0,x_1,x_2;y_0]\cdot x(x-1) \\
&\quad + f[x_0;y_0,y_1]\cdot y + f[x_0;y_0,y_1,y_2]\cdot y(y-1) + \text{混合差商项}(0) \\
&= 0 + 1\cdot x + 1\cdot x(x-1) + 1\cdot y + 1\cdot y(y-1) \\
&= x + x^2 - x + y + y^2 - y \\
&= x^2 + y^2
\end{aligned}$$

**答案：$N(x,y) = x^2 + y^2$**

---

### 常用差商插值公式速查

**（一）矩形上的Newton表初插值公式：**

取 $x_i=i,\;y_j=j\;(i=0,1,\ldots,m;j=0,1,\ldots,n)$，记
- $\Delta_x f(x,y) = f(x+1,y) - f(x,y)$
- $\Delta_y f(x,y) = f(x,y+1) - f(x,y)$
- $z^{(k)} = z(z-1)\cdots(z-k+1)$

则：
$$f(x,y) = \sum_{v=0}^{m}\sum_{u=0}^{n} \frac{x^{(v)}}{v!}\frac{y^{(u)}}{u!} \Delta_x^v\Delta_y^u f(0,0) + r(x,y)$$

**（二）矩形上的Newton表末插值公式：**

取 $x_i=-i,\;y_j=-j$，记
- $\nabla_x f(x,y) = f(x,y) - f(x-1,y)$
- $\nabla_y f(x,y) = f(x,y) - f(x,y-1)$
- $z^{(-k)} = z(z+1)\cdots(z+k-1)$

则：
$$f(x,y) = \sum_{v=0}^{m}\sum_{u=0}^{n} \frac{x^{(-v)}}{v!}\frac{y^{(-u)}}{u!} \nabla_x^v\nabla_y^u f(0,0) + r(x,y)$$

**（三）Stirling-Stirling插值（m,n均为偶数，节点对称于 $x=0, y=0$）：**

引入中心差分算子 $\delta_x, \delta_y$ 和平均算子 $\sigma_x, \sigma_y$，则：
$$f(x,y) = \sum_{v=0}^{m/2}\sum_{u=0}^{n/2} s_x^{(v)} s_y^{(u)} \cdot \delta_x^{2v}\delta_y^{2u} f(0,0) + r(x,y)$$

其中
$$s_x^{(v)} = \frac{x(x^2-1^2)(x^2-2^2)\cdots(x^2-(v-1)^2)}{(2v)!}$$

**（四）Bessel-Bessel插值（m,n均为奇数，节点对称于 $x=\frac12, y=\frac12$）：**

$$f(x,y) = \sum_{v=0}^{(m-1)/2}\sum_{u=0}^{(n-1)/2} b_x^{(v)} b_y^{(u)} \cdot f_{vu}\left(\frac12,\frac12\right) + r(x,y)$$

**（五）三角形上的差商插值：**

取 $n_v = m - v \;(v=0,1,\ldots,m)$，插值空间为 $P_m$：
$$f(x,y) = \sum_{v=0}^{m}\sum_{u=0}^{m-v} X_v Y_u \cdot f_{vu} + r(x,y)$$

---

## 题型二：二元Lagrange插值

### 题目描述

给定矩形网格上的函数值，构造二元Lagrange插值多项式。

### 典型例题（期末真题 15分）

**数据如下表：**

|        | y=0 | y=1 | y=2 |
|--------|-----|-----|-----|
| **x=0** | 1   | 2   | 5   |
| **x=1** | 2   | 3   | 6   |

**解：**

本题数据分布在完整的矩形张量积网格上，$x$ 节点为 $\{0,1\}$，$y$ 节点为 $\{0,1,2\}$。

#### 第一步：构造关于 $x$ 的Lagrange基函数（一次）

$$l_{x,0}(x) = \frac{x-1}{0-1} = 1-x$$
$$l_{x,1}(x) = \frac{x-0}{1-0} = x$$

#### 第二步：构造关于 $y$ 的Lagrange基函数（二次）

$$l_{y,0}(y) = \frac{(y-1)(y-2)}{(0-1)(0-2)} = \frac{1}{2}(y^2-3y+2)$$
$$l_{y,1}(y) = \frac{(y-0)(y-2)}{(1-0)(1-2)} = -y(y-2) = -y^2+2y$$
$$l_{y,2}(y) = \frac{(y-0)(y-1)}{(2-0)(2-1)} = \frac{1}{2}y(y-1) = \frac{1}{2}(y^2-y)$$

#### 第三步：构造二元Lagrange插值多项式

公式：
$$L(x,y) = \sum_{i=0}^{1}\sum_{j=0}^{2} f(x_i, y_j) \cdot l_{x,i}(x) \cdot l_{y,j}(y)$$

展开全部6项：

$$L(x,y) = 1 \cdot (1-x) \cdot \frac{1}{2}(y^2-3y+2) + 2 \cdot (1-x) \cdot (-y^2+2y) + 5 \cdot (1-x) \cdot \frac{1}{2}(y^2-y)$$
$$+ 2 \cdot x \cdot \frac{1}{2}(y^2-3y+2) + 3 \cdot x \cdot (-y^2+2y) + 6 \cdot x \cdot \frac{1}{2}(y^2-y)$$

#### 第四步：化简

先对 $x=0$ 和 $x=1$ 的列分别求和：

当 $x=0$（前三项，提取 $1-x$）：
$$\begin{aligned}
&\frac{1}{2}(y^2-3y+2) - 2y^2 + 4y + \frac{5}{2}(y^2-y) \\
&= \left(\frac{1}{2}-2+\frac{5}{2}\right)y^2 + \left(-\frac{3}{2}+4-\frac{5}{2}\right)y + 1 \\
&= y^2 + 1
\end{aligned}$$

当 $x=1$（后三项，提取 $x$）：
$$\begin{aligned}
&(y^2-3y+2) - 3y^2 + 6y + 3(y^2-y) \\
&= (1-3+3)y^2 + (-3+6-3)y + 2 \\
&= y^2 + 2
\end{aligned}$$

组合：
$$L(x,y) = (1-x)(y^2+1) + x(y^2+2) = y^2+1-xy^2-x+xy^2+2x = x + y^2 + 1$$

**答案：$L(x,y) = x + y^2 + 1$**

---

### 矩形网点Lagrange插值一般公式

设 $x$ 方向节点 $\{x_0,x_1,\ldots,x_m\}$，$y$ 方向节点 $\{y_0,y_1,\ldots,y_n\}$。

令：
$$U(x) = (x-x_0)(x-x_1)\cdots(x-x_m)$$
$$V(y) = (y-y_0)(y-y_1)\cdots(y-y_n)$$

则 $(x_i,y_j)$ 对应的Lagrange插值基本多项式为：
$$L_{ij}(x,y) = M_i(x) \cdot N_j(y) = \frac{U(x)}{(x-x_i)U'(x_i)} \cdot \frac{V(y)}{(y-y_j)V'(y_j)}$$

插值公式：
$$f(x,y) = \sum_{i=0}^{m}\sum_{j=0}^{n} f(x_i,y_j) \cdot \frac{U(x)}{(x-x_i)U'(x_i)} \cdot \frac{V(y)}{(y-y_j)V'(y_j)} + r(x,y)$$

---

### 三角形网点上的Lagrange插值

考虑三角形ABC，将AC边 $n$ 等分，过每个分点做分别平行于AB和BC的直线，得到三角形格点网。

设三边方程分别为：
$$a(x,y) = \alpha_1 x + \beta_1 y + \gamma_1 = 0$$
$$b(x,y) = \alpha_2 x + \beta_2 y + \gamma_2 = 0$$
$$c(x,y) = \alpha_3 x + \beta_3 y + \gamma_3 = 0$$

平行线簇方程：
$$a_i(x,y) = \alpha_1 x + \beta_1 y + \gamma_1 + ih_1 = 0,\quad i=0,1,\ldots,n$$
$$b_j(x,y) = \alpha_2 x + \beta_2 y + \gamma_2 + jh_2 = 0,\quad j=0,1,\ldots,n$$

令：
$$A_i(x,y) = \prod_{l=0}^{i-1} a_l(x,y),\quad B_j(x,y) = \prod_{l=0}^{j-1} b_l(x,y),\quad C_k(x,y) = \prod_{l=0}^{k-1} c_l(x,y)$$

则 $q_{ij}$（$a_i=0$与$b_j=0$的交点）处的Lagrange基本多项式：
$$L_{ij}(x,y) = \frac{A_i(x,y)}{A_i(q_{ij})} \cdot \frac{B_j(x,y)}{B_j(q_{ij})} \cdot \frac{C_{n-i-j}(x,y)}{C_{n-i-j}(q_{ij})}$$

---

### 直线相交网上的Lagrange插值

平面上任意做 $n+1$ 条两两相交但任意三条不共点的直线 $a_0(x,y)=0, a_1(x,y)=0,\ldots,a_n(x,y)=0$。

令：
$$S_{ij}(x,y) = a_0(x,y) a_1(x,y) \cdots a_n(x,y) \quad(\text{去掉} a_i \text{和} a_j)$$

则 $q_{ij}$（$a_i=0$ 与 $a_j=0$ 的交点）处的插值公式：
$$f(x,y) = \sum_{0 \leq i < j \leq n} f(q_{ij}) \frac{S_{ij}(x,y)}{S_{ij}(q_{ij})} + r(x,y)$$

---

## 题型三：二元Aitken迭代插值

### 题目描述

已知平面上一组节点（通常分布在矩形或三角形区域上），用Aitken迭代法构造插值公式。

### 典型例题（期末真题 15分）

**图形：一个矩形区域，$x_1=1,\;x_2=-1,\;y_1=1,\;y_2=-1$，$q$ 为中心点 $(0,0)$**

节点及函数值：

| 节点 | 坐标      | 函数值 |
|------|----------|--------|
| A    | (1,1)    | 1      |
| B    | (1,-1)   | 1      |
| C    | (-1,-1)  | 1      |
| D    | (-1,1)   | 1      |
| G    | (-1,0)   | 2      |
| H    | (0,1)    | 1      |
| E    | (1,0)    | 2      |
| F    | (0,-1)   | 1      |
| q    | (0,0)    | 1      |

用Aitken迭代法构造过这9个点的插值公式。

**解：**

#### 第一步：横向（固定y）一维插值

**$y=-1$ 线：** C(-1,-1)→1, F(0,-1)→1, B(1,-1)→1。值全为1，故 $P_{y=-1}(x) = 1$

**$y=1$ 线：** D(-1,1)→1, H(0,1)→1, A(1,1)→1。同理 $P_{y=1}(x) = 1$

**$y=0$ 线：** G(-1,0)→2, q(0,0)→1, E(1,0)→2

Aitken迭代（从线性到二次）：
$$P_{-1,0}(x) = \frac{1}{0-(-1)}\begin{vmatrix} 2 & -1-x \\ 1 & 0-x \end{vmatrix} = 2(-x)-1(-1-x) = 1-x$$
$$P_{0,1}(x) = \frac{1}{1-0}\begin{vmatrix} 1 & 0-x \\ 2 & 1-x \end{vmatrix} = 1(1-x)-2(-x) = 1+x$$
$$P_{y=0}(x) = P_{-1,0,1}(x) = \frac{1}{1-(-1)}\begin{vmatrix} 1-x & -1-x \\ 1+x & 1-x \end{vmatrix} = \frac{(1-x)^2-(1+x)(-1-x)}{2} = 1+x^2$$

#### 第二步：纵向（对变量y）迭代

已知 $y \in \{-1,0,1\}$，对应函数值 $1, 1+x^2, 1$

$$P_{-1,0}(y) = \frac{1}{0-(-1)}\begin{vmatrix} 1 & -1-y \\ 1+x^2 & 0-y \end{vmatrix} = -y-(1+x^2)(-1-y) = 1+x^2+x^2y$$
$$P_{0,1}(y) = \frac{1}{1-0}\begin{vmatrix} 1+x^2 & 0-y \\ 1 & 1-y \end{vmatrix} = (1+x^2)(1-y)-(-y) = 1+x^2-x^2y$$

最终高阶迭代：
$$P(x,y) = \frac{1}{1-(-1)}\begin{vmatrix} 1+x^2+x^2y & -1-y \\ 1+x^2-x^2y & 1-y \end{vmatrix}$$
$$= \frac{1}{2}\left[(1+x^2+x^2y)(1-y) - (1+x^2-x^2y)(-1-y)\right]$$
$$= \frac{1}{2}\left[2(1+x^2) - 2x^2y^2\right]$$
$$= 1 + x^2 - x^2y^2$$

**答案：$P(x,y) = 1 + x^2 - x^2y^2$**

---

### 三角形迭代法一般公式

平面上做三条直线 $l_1, l_2, l_3$ 交成三角形，顶点为 $A(x_1,y_1), B(x_2,y_2), C(x_3,y_3)$。

设 $P_{n-1}^{(1)}, P_{n-1}^{(2)}, P_{n-1}^{(3)}$ 分别是函数 $f$ 在 $P_{n-1}$ 中关于三组结点的插值多项式。

令：
$$d = \begin{vmatrix} 1 & x_1 & y_1 \\ 1 & x_2 & y_2 \\ 1 & x_3 & y_3 \end{vmatrix}$$

则 $n$ 次插值多项式为：
$$P_n(x,y) = \frac{1}{d} \begin{vmatrix}
P_{n-1}^{(1)}(x,y) & x-x_1 & y-y_1 \\
P_{n-1}^{(2)}(x,y) & x-x_2 & y-y_2 \\
P_{n-1}^{(3)}(x,y) & x-x_3 & y-y_3
\end{vmatrix}$$

---

### 矩形迭代法一般公式

四条直线 $x=x_1, x=x_2, y=y_1, y=y_2$ 交于 $A(x_1,y_1), B(x_1,y_2), C(x_2,y_2), D(x_2,y_1)$。

设 $P_{n-1,n-1}^{(i,j)}$ $(i,j=1,2)$ 是 $f$ 在 $P_{n-1,n-1}$ 中的插值多项式。

令：
$$d = \begin{vmatrix} 1 & x_1 & y_1 & x_1y_1 \\ 1 & x_1 & y_2 & x_1y_2 \\ 1 & x_2 & y_2 & x_2y_2 \\ 1 & x_2 & y_1 & x_2y_1 \end{vmatrix}$$

则 $(n,n)$ 次插值多项式通过四个 $(n-1,n-1)$ 次插值与一个双线性插值组合得到：
$$P_{n,n}(x,y) = \frac{1}{d} \begin{vmatrix}
P_{n-1,n-1}^{(1,1)}(x,y) & x-x_1 & y-y_1 & (x-x_1)(y-y_1) \\
P_{n-1,n-1}^{(1,2)}(x,y) & x-x_1 & y-y_2 & (x-x_1)(y-y_2) \\
P_{n-1,n-1}^{(2,2)}(x,y) & x-x_2 & y-y_2 & (x-x_2)(y-y_2) \\
P_{n-1,n-1}^{(2,1)}(x,y) & x-x_2 & y-y_1 & (x-x_2)(y-y_1)
\end{vmatrix}$$

---

## 题型四：适定结点组的证明与构造

### 4.1 基本概念

**定义：** 若对于任何连续函数 $f(x)$，插值问题 $P(x) = \sum_{j=1}^{k} c_j P_j(x)$ 满足 $P(q_i) = f(q_i)$（$i=1,\ldots,k$）的解总是存在且唯一，则称 $\{q_i\}_{i=1}^{k}$ 是插值空间 $P$ 的**适定结点组**。

**基本引理（核心结论）：** $\{q_i\}_{i=1}^{k}$ 是 $P$ 的适定结点组的**充要条件**是 $\{q_i\}_{i=1}^{k}$ 不在 $P$ 中的任何一条代数曲线上。

> 该引理将插值的适定性问题转化为几何问题，为代数几何方法的应用打下基础。

**证明：**

- **必要性：** 若 $\{q_i\}_{i=1}^{k}$ 是适定组，则行列式 $\Delta = \det[P_j(q_i)] \neq 0$。若存在非零多项式 $p \in P$ 使所有 $q_i$ 落在曲线 $p=0$ 上，即 $p(q_i)=0$，由适定性得 $p \equiv 0$，矛盾。

- **充分性：** 若 $\{q_i\}_{i=1}^{k}$ 不是适定组，则 $\Delta=0$，存在不全为零的系数 $\{c_j\}$ 使对每个 $q_i$ 有 $\sum_j c_j P_j(q_i)=0$，说明所有 $q_i$ 落在同一条代数曲线上。

---

### 4.2 定理2（适定结点组的扩张定理）

**定理：** 若 $\{q_i\}_{i=1}^{k}$ 是 $P_n$ 的适定结点组，它的每个点都不在某 $l$ 次（$l=1,2$）不可约代数曲线 $Q(x,y)=0$ 上，则在该曲线上任取 $(n+3)l-1$ 个不同点与 $\{q_i\}_{i=1}^{k}$ 一起构成 $P_{n+l}$ 的适定结点组。

**证明思路：** 反证法 + Bezout定理。设B不是适定组，则存在非零的 $P \in P_{n+l}$ 使B中的点全在 $P=0$ 上。由于U中的 $(n+3)l-1 > l(n+l)$ 个点既在 $Q=0$（$l$ 次）又在 $P=0$（$n+l$ 次）上，由Bezout定理知 $P=Q \cdot R$ 且 $R \in P_n$。但 $\{q_i\}_{i=1}^{k}$ 在 $P=0$ 上而不在 $Q=0$ 上，故在 $R=0$ 上，这与 $\{q_i\}_{i=1}^{k}$ 是 $P_n$ 适定组矛盾。

---

### 4.3 构造适定结点组的重要方法

#### 方法一：直线型结点组 $D_n$

| 步骤 | 操作 | 新增点数 |
|------|------|---------|
| 第0步 | 任取一点 $q$ | 1 |
| 第1步 | 做直线 $L_1$（不过 $q$），在其上任取点 | 2 |
| 第2步 | 做直线 $L_2$（不过前面所有点），在其上任取点 | 3 |
| ... | ... | ... |
| 第n步 | 做直线 $L_n$（不过前面所有点），在其上任取点 | $n+1$ |

总点数：$1+2+3+\cdots+(n+1) = \frac{(n+1)(n+2)}{2}$，恰等于 $P_n$ 的维数。

**结论：$D_n$ 是 $P_n$ 的适定结点组。**

#### 方法二：弧线型结点组 $D2_n$

| 步骤 | 操作 | 新增点数 |
|------|------|---------|
| 第0步 | 任取一点 $q$ | 1 |
| 第1步 | 做二次不可约曲线 $L_1$（不过 $q$），在其上任取点 | 5 |
| 第2步 | 做二次不可约曲线 $L_2$，在其上任取点 | 9 |
| ... | ... | ... |
| 第n步 | 做二次不可约曲线 $L_n$，在其上任取点 | $4n+1$ |

总点数：$1+5+9+\cdots+(4n+1) = (n+1)(2n+1)$，恰等于 $P_{2n}$ 的维数。

**特例：** 若二次曲线均取为同心圆，则称为圆型结点组。

#### 方法三：竖线型结点组 $U_{m,n}$

在平面上任做 $m+1$ 条互不相同的竖直线，在每条竖直线上任取 $n+1$ 个互不相同的点。

总点数：$(m+1)(n+1)$，恰等于 $P_{m,n}$ 的维数。

**结论：$U_{m,n}$ 是 $P_{m,n}$ 的适定结点组。**

#### 方法四：十字形结点组 $B_{n,n}$

| 步骤 | 操作 | 新增点数 |
|------|------|---------|
| 第0步 | 任取一点 $q$ | 1 |
| 第1步 | 做十字 $X_1$（不过 $q$），一分量取2点，另一分量取1点 | 3 |
| 第2步 | 做十字 $X_2$，一分量取3点，另一分量取2点 | 5 |
| ... | ... | ... |
| 第n步 | 做十字 $X_n$，一分量取 $n+1$ 点，另一分量取 $n$ 点 | $2n+1$ |

总点数：$(n+1)^2$，恰等于 $P_{n,n}$ 的维数。

---

### 4.4 定理3（垂直于坐标轴的扩张）

若 $\{q_i\}_{i=1}^{k}$ 是 $P_{m,n}$ 的适定结点组，且每个点都不在竖直线 $x=a$ 上，则在该竖直线上任取 $n+1$ 个不同点与 $\{q_i\}_{i=1}^{k}$ 一起构成 $P_{m+1,n}$ 的适定结点组。

**证明：** 反证法。设B不是适定组，则存在非零的 $P \in P_{m+1,n}$ 使B中所有点都在 $P=0$ 上。由于U中有 $n+1$ 个点在 $x=a$ 且 $P(a,y)$ 是 $y$ 的 $n$ 次多项式但有 $n+1$ 个零点，故 $P(a,y) \equiv 0$。由Bezout定理，$P=(x-a)\cdot R$ 且 $R \in P_{m,n}$。由适定性推出 $R \equiv 0$，矛盾。

---

## 题型五：迭加插值法

### 5.1 一般提法

给定 $m+1$ 个二元多项式空间 $G_0, G_1, \ldots, G_m$ 和 $m+1$ 个结点组 $U_0, U_1, \ldots, U_m$ 以及 $m$ 条代数曲线 $Q_1, Q_2, \ldots, Q_m$，满足：

1. $U_i$ 是 $G_i$ 的适定结点组（$i=0,1,\ldots,m$）
2. $U_0, U_1, \ldots, U_{i-1}$ 都在曲线 $Q_i$ 上，而 $U_i$ 中任何点都不在曲线 $Q_i$ 上（$i=1,\ldots,m$）

则插值多项式具有形式：
$$P(x,y) = P_0(x,y) + Q_1(x,y) \cdot P_1(x,y) + Q_2(x,y) \cdot P_2(x,y) + \cdots + Q_m(x,y) \cdot P_m(x,y)$$

其中 $P_i(x,y)$ 是 $G_i$ 中元素的一般表达式。

实现算法：**依次用 $U_0, U_1, \ldots, U_m$ 上的插值条件定出 $P_0, P_1, \ldots, P_m$。**

### 5.2 特殊类型：可整除型

若要求 $Q_1 \mid Q_2 \mid \cdots \mid Q_m$，则有 $Q_i = R_1 R_2 \cdots R_i$，此时：
$$P(x,y) = P_0(x,y) + R_1(x,y)[P_1(x,y) + R_2(x,y)(P_2(x,y) + \cdots)]$$

### 5.3 典型例题

**例：** 给定结点如图，取
- $G_0 = \{a + bx + cx^2\}$，$U_0 = \{(-1,0),(0,0),(1,0)\}$，取 $Q_1 = y$（即 $R_1 = y$）
- $G_1 = \{dx + ey\}$，$U_1 = \{(-\frac12,\frac12),(\frac12,\frac12)\}$，取 $Q_2 = y(y-\frac12)$（即 $R_2 = y-\frac12$）
- $G_2 = \{f\}$，$U_2 = \{(0,1)\}$

则插值多项式：
$$P(x,y) = (a + bx + cx^2) + y[dx + ey] + y\left(y-\frac12\right)[f]$$

由6个点的初始条件定6个未知数 $a,b,c,d,e,f$。

---

### 5.4 具体迭加插值算法

#### （1）直线型结点组 $D_n$ 的迭加插值

取 $R_i(x,y) = L_{n-i+1}(x,y)$（第 $n-i+1$ 条直线的方程），$P_i(x,y)$ 为 $x$ 和 $y$ 的 $n-i$ 次多项式。

$$P(x,y) = P_0(x,y) + L_n(x,y)P_1(x,y) + L_n(x,y)L_{n-1}(x,y)P_2(x,y) + \cdots$$

#### （2）圆型结点组 $D2_n$ 的迭加插值

取 $R_i(x,y) = (x-x_0)^2 + (y-y_0)^2 - r_i^2$，$P_i(x,y)$ 为不超过 $n-i$ 次的调和多项式。

#### （3）竖线型结点组 $U_{m,n}$ 的迭加插值

$$P(x,y) = P_0(y) + (x-x_0)P_1(y) + (x-x_0)(x-x_1)P_2(y) + \cdots + (x-x_0)\cdots(x-x_{m-1})P_m(y)$$

其中每个 $P_i(y)$ 是 $y$ 的 $n$ 次多项式。

#### （4）十字型结点组 $B_{n,n}$ 的迭加插值

$$P(x,y) = P_0(x,y) + (x-x_1)(y-y_1) P_1(x,y) + \cdots + (x-x_1)(y-y_1)\cdots(x-x_n)(y-y_n) P_n(x,y)$$

---

## 题型六：切触插值（二元接触插值）

### 6.1 问题提法

设 $P_1(x,y), P_2(x,y), \ldots, P_s(x,y)$ 是 $s$ 个线性无关的二元代数多项式，支成空间 $P$。

切触插值问题：找 $P(x,y) \in P$ 满足
$$D_{ikl} P(x,y) \bigg|_{(x,y)=(x_i,y_i)} = \frac{\partial^{k+l}}{\partial x^k \partial y^l} P(x_i, y_i) = f_{ikl}, \quad (k,l) \in I_i,\; i=1,2,\ldots,n$$

若对任何 $\{f_{ikl}\}$，解存在且唯一，则称 $\Phi = \{D_{ikl}\}$ 是 $P$ 的**适定插值泛函组**。

### 6.2 典型例题

构造 $P_3(x,y)$ 满足以下条件：

在 $(0,0)$ 处：
$$P(0,0)=f(0,0),\; P_x(0,0)=f_x(0,0),\; P_y(0,0)=f_y(0,0),\; P_{xy}(0,0)=f_{xy}(0,0)$$

在 $(1,0)$ 处：
$$P(1,0)=f(1,0),\; P_x(1,0)=f_x(1,0)$$

在 $(0,1)$ 处：
$$P(0,1)=f(0,1),\; P_y(0,1)=f_y(0,1)$$

**解：** 用切触迭加插值法

取 $E_1 = \{D_{xy}\}$（即 $P_{xy}$ 条件），$P_1(x,y) = axy$

取 $E_2 = \{D_{00}, D_{10}, D_{01}, D_x, D_y\}$（其余5个条件），$$P_2(x,y) = a_0 + b_0 x + c_0 y + b_1 x(x-1) + c_1 y(y-1)$$

则 $P(x,y) = P_1(x,y) + P_2(x,y) = axy + a_0 + b_0 x + c_0 y + b_1 x(x-1) + c_1 y(y-1)$

验证：对 $E_1$ 中的泛函 $D_{xy}$，有 $D_{xy} P_2 = 0$（因为 $P_2$ 没有 $xy$ 项）。

依次求解：
- 由 $D_{xy} P = f_{xy}(0,0)$ 得 $a = f_{xy}(0,0)$
- 由 $D_{00} P = f(0,0)$ 得 $a_0 = f(0,0)$
- 由 $D_{10} P = f(1,0)$ 得 $b_0 = f(1,0) - f(0,0)$
- 由 $D_{01} P = f(0,1)$ 得 $c_0 = f(0,1) - f(0,0)$
- 由 $D_x P = f_x(1,0)$ 得 $b_1 = f_x(1,0) - f(1,0) + f(0,0)$
- 由 $D_y P = f_y(0,1)$ 得 $c_1 = f_y(0,1) - f(0,1) + f(0,0)$

---

### 6.3 切触Lagrange方法

**定理6：** 若对一切 $(k,l) \in I_{i_0}$ 有 $D_{kl}^{i_0} Q \neq 0$，且 $Q(x_{i_0}, y_{i_0}) \neq 0$，则插值空间由形如
$$P(x,y) = Q(x,y) \sum_{(k,l) \in I_{i_0}} c_{kl}(x-x_{i_0})^k(y-y_{i_0})^l$$
的多项式构成。

插值基本多项式（当 $k_0=p, l_0=q$ 且 $l^{(i_0)}_0=l^{(i_0)}_1=\cdots=l^{(i_0)}_{k_0}=p-1$）：
$$L_{kl}^{i_0}(x,y) = \frac{(x-x_{i_0})^{p+1}(y-y_{i_0})^{q+1}}{p!q!Q(x,y)} \cdot \sum_{s=p-k}^{0}\sum_{t=q-l}^{0} \left[\frac{\partial^{s+t}}{\partial x^s \partial y^t}\frac{1}{Q(x,y)}\right]_{(x_{i_0},y_{i_0})} \frac{(x-x_{i_0})^s}{s!}\frac{(y-y_{i_0})^t}{t!}$$

---

## 题型七：Bezout定理的证明与应用

### 7.1 Bezout定理陈述

**定理：** 若 $m$ 次代数曲线 $P_1(x,y)=0$ 与 $n$ 次代数曲线 $P_2(x,y)=0$ 的交点个数多于 $mn$，则一定存在次数不超过 $m$ 也不超过 $n$ 的非零多项式 $Q(x,y)$，使得：
$$P_1(x,y) = Q(x,y) \cdot R_1(x,y), \quad P_2(x,y) = Q(x,y) \cdot R_2(x,y)$$
其中 $R_1, R_2$ 分别为次数小于 $m$ 和 $n$ 的多项式。

### 7.2 证明思路

1. 设两曲线有 $mn+1$ 个互异交点 $(x_1,y_1),\ldots,(x_{mn+1},y_{mn+1})$
2. 构造结式 $S(x)$（$m+n$ 阶行列式），它是 $x$ 的次数不超过 $mn$ 的多项式
3. 由恒等式 $S(x) = A(x,y)P_1(x,y) + B(x,y)P_2(x,y)$ 知 $S(x)$ 有 $mn+1$ 个零点
4. 故 $S(x) \equiv 0$，行列式的各行向量线性相关
5. 存在不全为零的有理分式 $\{\alpha_i\}, \{\beta_j\}$ 满足 $\alpha_0 a_0 = -\beta_0 b_0$ 等关系
6. 构造多项式 $\phi(y), \psi(y)$，满足 $\phi P_1 + \psi P_2 \equiv 0$（关于y）
7. 由次数关系推出 $P_1$ 的一个不可约因子 $Q$ 也是 $P_2$ 的因子

### 7.3 Bezout定理在适定性证明中的应用

**关键作用：** 在适定结点组的几何特征研究中，Bezout定理是联系"结点组的几何位置"与"插值问题的代数性质"的桥梁。

例如，在定理2的证明中：若 $l$ 次不可约曲线 $Q=0$ 与 $n+l$ 次曲线 $P=0$ 的交点多于 $l(n+l)$ 个，由Bezout定理推知 $P = Q \cdot R$ 且 $R \in P_n$。

---

## 题型八：多元插值适定性理论简述

### 题目（期末真题 15分）

**给定插值结点，多元多项式插值一定唯一吗？一定存在吗？解决办法是什么？**

### 解答

#### 1. 不一定唯一，也不一定存在

在一维情况下，任意 $n+1$ 个互异节点必然存在唯一的不超过 $n$ 次的插值多项式（**Haar条件成立**）。但在多元空间（维数 $d \geq 2$）中，情况发生了本质变化。

著名的**Haar定理**指出：在多元空间中**不存在**在所有点集上都适定的有限维插值空间（除了极少数平凡情况）。

这意味着：

- **不一定存在：** 如果插值节点选取不当——例如在二维平面中，要求一个二次多项式插值6个点，但这6个点正好全部落在同一条二次曲线上（如一个圆上）——会导致插值系统矩阵不可逆，从而可能无解。

- **不一定唯一：** 在上述退化情况或给定函数值全为0的情况下，系统矩阵存在非零零空间，导致插值多项式存在无穷多个（非唯一）。

#### 2. 解决办法

**（1）规定特殊的节点几何构型：**

强制要求节点位于特定的网格上（如矩形网格上的张量积插值），或者满足特定的适定性条件。例如：
- 二维单纯形/三角形上的基本节点，满足"共线/不共线"的几何约束
- 直线型、弧线型、竖线型、十字型等适定结点组的系统构造

**（2）使用径向基函数插值（RBF）：**

抛弃纯多项式基底，改用距离相关的基函数（如高斯核、Multiquadric等）。根据**Micchelli定理**，对于相异节点，特定的径向基插值矩阵恒为正定，从根本上保证了插值的存在性和唯一性。

**（3）多元样条/有限元法：**

将全局插值转化为局部分段多项式插值（如 Delaunay 三角剖分后的分段线性/三次插值），以保证局部问题始终是可解和适定的。

---

## 题型九：多元插值在深度学习中的应用（论述）

### 题目（期末真题 25分）

**谈谈多元插值方法在深度学习中的应用、深度学习方法如何从多元插值中获取营养。**

### 解答

#### 一、多元插值方法在深度学习中的应用

**1. 深度学习的本质即高维多元逼近/插值：**

深度神经网络（DNN）从数学本质上看，是在高维参数空间中拟合训练数据点，寻找一个极度复杂的多元函数映射 $f(\mathbf{x})$。在过参数化时代，"完美拟合"训练数据（即 training error 为0）被视为一种高维空间中的精准插值行为。

**2. 激活函数与分段样条插值的等价性：**

最常用的 **ReLU 激活函数网络**，本质上是在高维空间中构造了一种极其复杂的**连续多元分段线性插值**。网络的层数和宽度决定了该分段线性区域的折叠次数和空间划分密度。

具体地，一个有两层、宽度为 $N$ 的 ReLU 网络可以表示为：
$$f(\mathbf{x}) = \sum_{i=1}^{N} a_i \cdot \max(\mathbf{w}_i^T \mathbf{x} + b_i, 0) + c$$
这等价于对输入空间做了 $N$ 个超平面划分后，在每个区域上进行线性插值。

**3. 图像超分辨率与变形（Warping）：**

在计算机视觉中，多元插值被广泛应用于：
- **图像的几何变换**（双线性插值、双三次插值）
- **超分辨重建**
- **Spatial Transformer Networks（STN）**的采样层，其核心就是可微分的双线性插值

#### 二、深度学习方法如何从多元插值中获取营养

**1. 突破"维数灾难"的启发：**

传统的多元插值在维度增加时面临节点数量呈指数爆炸的灾难。深度学习通过利用数据在特征空间中往往聚集在**低维流形（Manifold）**这一事实，以层次化特征提取代替全空间插值。这为多元插值研究提出了"基于流形的插值逼近"新方向。

**2. 结构设计的灵感来源（如KAN）：**

多元插值理论深刻启发了网络架构的创新。例如，**KAN（Kolmogorov-Arnold Networks）** 完全建立在 Kolmogorov-Arnold 表示定理之上，利用单变量的样条插值（B-Splines）取代了传统网络中的线性权重：
$$f(x_1,\ldots,x_n) = \sum_{q=1}^{2n+1} \Phi_q\left(\sum_{p=1}^{n} \phi_{q,p}(x_p)\right)$$

这展示了经典插值理论如何直接孕育出颠覆性的前沿深度学习架构。

**3. 基于RBF与高斯过程的网络：**

- **径向基函数插值（RBF）** 直接演化成了 **RBF神经网络**
- 与插值高度同源的**高斯过程（Gaussian Process）** 理论，在贝叶斯深度学习、超参数优化（如克里金 Kriging 代理模型）中汲取了丰富的营养，提供了深度不确定性估计

---

## 题型十：径向基函数(RBF)插值 vs 多元多项式插值（论述）

### 题目（期末真题 15分）

**相对于多元多项式插值，谈谈径向基插值函数的优缺点。**

### 解答

#### 1. RBF插值的优点

**（1）完全无惧网格束缚（适应散乱数据）：**

这是RBF最大的优势。多元多项式插值极度依赖节点分布的几何构型（通常需要规则张量积网格），而RBF只需要有空间点坐标就能进行插值：
$$s(x) = \sum_{i=1}^{N} \lambda_i \phi(\|x - x_i\|)$$

极度适合高维散乱数据（Scattered Data）的处理。

**（2）保证存在唯一性：**

由**Micchelli定理**保障，对于许多常见RBF（如高斯核 $e^{-cr^2}$，多二次曲面 $\sqrt{r^2+c^2}$），只要数据点互异，其插值矩阵就是严格正定或非奇异的，从根本上避开了多元多项式插值的非适定性问题。

**（3）与维数无关的表达能力：**

RBF仅依赖于点之间的欧氏距离 $r = \|x - x_i\|$，因此其公式表达在二维、三维甚至更高维空间中具有完全一致的形式，极大地简化了高维逼近的数学建模。

#### 2. RBF插值的缺点

**（1）计算与存储复杂度极高：**

对于 $N$ 个数据点，RBF需要求解一个 $N \times N$ 的**全局稠密线性方程组**：
$$\begin{bmatrix} \phi(r_{11}) & \cdots & \phi(r_{1N}) \\ \vdots & \ddots & \vdots \\ \phi(r_{N1}) & \cdots & \phi(r_{NN}) \end{bmatrix} \begin{bmatrix} \lambda_1 \\ \vdots \\ \lambda_N \end{bmatrix} = \begin{bmatrix} f_1 \\ \vdots \\ f_N \end{bmatrix}$$

- 计算复杂度高达 $O(N^3)$
- 评估新点也需要 $O(N)$ 的计算量
- 相较于多项式插值的局部或递推算法，计算代价非常高昂

**（2）形状参数极其敏感：**

许多RBF包含一个"形状参数" $c$。这个参数的选择极其困难：
- 若 $c$ 太小，矩阵条件数会爆表（病态）
- 若 $c$ 太大，插值面将变得极其平坦，失去逼近意义

**（3）龙格现象与外推能力差：**

- 在全局RBF中，矩阵高度病态会导致类似多项式高次插值边缘的**龙格现象（Runge's Phenomenon）**
- 由于基函数随距离衰减，在数据覆盖区域之外的**外推能力通常极弱**

---

## 题型十一：矩形迭代法

### 题目描述

用矩形迭代法构造 $P_{n,n}$ 中的插值多项式。

### 算法步骤

1. **节点分布：** 四条直线 $x=x_1, x=x_2, y=y_1, y=y_2$ 交于四点 $A(x_1,y_1), B(x_1,y_2), C(x_2,y_2), D(x_2,y_1)$。在每条直线上除交点外另取 $n-1$ 个点，外加一个 $P_{n-2,n-2}$ 的适定结点组 $\Phi_{n-2,n-2}$。

2. **四个 $(n-1,n-1)$ 次插值：** 分别在四组结点上做 $f$ 的 $(n-1,n-1)$ 次插值：$P_{n-1}^{(1,1)}, P_{n-1}^{(1,2)}, P_{n-1}^{(2,2)}, P_{n-1}^{(2,1)}$。

3. **组合：** 将这四次插值通过一个**双线性插值**组合成 $(n,n)$ 次插值：
$$P_{n,n}(x,y) = \frac{1}{d} \begin{vmatrix}
P_{n-1}^{(1,1)} & x-x_1 & y-y_1 & (x-x_1)(y-y_1) \\
P_{n-1}^{(1,2)} & x-x_1 & y-y_2 & (x-x_1)(y-y_2) \\
P_{n-1}^{(2,2)} & x-x_2 & y-y_2 & (x-x_2)(y-y_2) \\
P_{n-1}^{(2,1)} & x-x_2 & y-y_1 & (x-x_2)(y-y_1)
\end{vmatrix}$$

其中 $d = \begin{vmatrix} 1 & x_1 & y_1 & x_1y_1 \\ 1 & x_1 & y_2 & x_1y_2 \\ 1 & x_2 & y_2 & x_2y_2 \\ 1 & x_2 & y_1 & x_2y_1 \end{vmatrix}$

4. **验证：** 对矩形格点网，将此降阶过程反复应用，可将双 $n$ 次插值化为有限步双一次插值的迭代。这是Aitken插值法的另一种二元推广。

---

## 题型十二：二元插值余项估计

### 12.1 Rolle定理方法

利用一元差商与导数的关系：
$$f[x_0, x_1, \ldots, x_n] = \frac{f^{(n)}(\xi)}{n!}$$

推广到二元：
$$f[x_0, \ldots, x_m; y_0, \ldots, y_n] = \frac{1}{m!n!} \frac{\partial^{m+n} f(\xi, \eta)}{\partial x^m \partial y^n}$$

其中 $\xi$ 在包含 $\{x_i\}$ 的最小区间内，$\eta$ 在包含 $\{y_j\}$ 的最小区间内。

### 12.2 矩形插值的余项

对于 Lagrange 插值公式，余项为：
$$r(x,y) = \frac{U(x)}{(m+1)!} f_x^{(m+1)}(\xi, y) + \frac{V(y)}{(n+1)!} f_y^{(n+1)}(x, \eta) - \frac{U(x)V(y)}{(m+1)!(n+1)!} f_{xy}^{(m+1,n+1)}(\bar{\xi}, \bar{\eta})$$

其中 $U(x) = \prod_{i=0}^{m}(x-x_i),\; V(y) = \prod_{j=0}^{n}(y-y_j)$

### 12.3 三角形插值的余项

对于三角形上的差商插值（空间为 $P_m$），余项为：
$$r(x,y) = \sum_{v=0}^{m} \frac{X_v}{(m+1)!} f_x^{(m+1)}(\xi_v, y) \cdot Y_{m-v} + \sum_{v=0}^{m} \frac{X_v Y_{m-v+1}}{(m+1)!} \frac{\partial^{m+1} f}{\partial x^{m-v+1} \partial y^v}(\xi_v, \eta_v)$$

---

# 第二章 超限插值法

## 预备知识：超限插值法概述

**核心区别：** 多项式插值的节点是**有限个点**，而超限插值法的节点是**不可数点集**（如整条曲线上的所有点），这是一类新的插值方法，也称为Blending Interpolation Method。

**应用领域：** 曲面造型、函数逼近、有限元分析。

---

## 题型一：插值算子的布尔和

### 1.1 基本概念

设 $F$ 是定义在点集 $D$ 上的实值函数类，$P$ 和 $Q$ 是定义在 $F$ 上的两个线性算子。

- **乘积：** $PQ$（先应用 $Q$，再应用 $P$）
- **布尔和：** $P \oplus Q = P + Q - PQ$

### 1.2 定理1（布尔和的插值性质）

**定理：** 设 $P, Q$ 如上定义，且在点集 $S_1$ 上对 $f \in F$ 有 $Pf = f$，又设对任意 $f \in F$，在 $S_2$ 上有 $Q f = f$，则：

1. 在 $S_1 \cup S_2$ 上有 $(P \oplus Q) f = f$（即布尔和具有 $P$ 的插值性质）
2. 若 $D^\alpha$ 是偏微分算子，且 $D^\alpha Q f = D^\alpha f$ 在 $S_2$ 上成立，则 $D^\alpha (P \oplus Q) f = D^\alpha f$ 在 $S_2$ 上（布尔和具有 $Q$ 的插值精度）

**证明：**

由假设 $Pf = f$ 在 $S_1$ 上，所以：
$$(P \oplus Q) f = Pf + Qf - P(Qf) = f + Qf - f = Qf \quad (\text{在} S_1 \text{上})$$
从而在 $S_1 \cup S_2$ 上有 $(P \oplus Q) f = f$。

令 $I$ 表示恒等算子，则 $P \oplus Q = P + Q - PQ = I - (I-P)(I-Q)$。

当 $D^\alpha$ 满足 $D^\alpha Q f = D^\alpha f$ 时：
$$D^\alpha (P \oplus Q) f = D^\alpha (P+Q-PQ) f = D^\alpha f + D^\alpha f - D^\alpha f = D^\alpha f$$

**推论：** 若 $Pf = f$ 在 $S_1$ 上，$Qf = f$ 在 $S_2$ 上，且 $Pf = f$ 在 $S_2$ 上，$Qf = f$ 在 $S_1$ 上，则 $P \oplus Q$ 具有 $P$ 和 $Q$ 的全部插值性质。

---

### 1.3 典型例题

**例1：** 设 $F$ 上 $P f(x,y)$ 在 $x=0,1$ 上插值，$Q f(x,y)$ 在 $y=0,1$ 上插值。

取：
$$P f(x,y) = (1-x)f(0,y) + x f(1,y)$$
$$Q f(x,y) = (1-y)f(x,0) + y f(x,1)$$

则在 $x=0,1$ 上对任意 $f$ 有 $Pf = f$，在 $y=0,1$ 上有 $Qf = f$。

故布尔和 $P \oplus Q$ ：
$$(P \oplus Q) f = Pf + Qf - P(Qf)$$

在 $x=0,1$ 和 $y=0,1$ 上都插值，即具有 $P$ 的插值性质。而且可以证明在 $y=0,1$ 上，$P \oplus Q$ 也具有 $Q$ 的插值精度。

**例2：** 设 $F$ 为曲边四边形上的函数类，四条曲边为 $l_1, l_2, l_3, l_4$。

取 $P$ 把 $l_1$ 和 $l_3$ 变成直线 $x=0$ 和 $x=1$，$Q$ 把 $l_2$ 和 $l_4$ 变成直线 $y=0$ 和 $y=1$。

则布尔和 $P \oplus Q$ 把正方形变成曲四边形。于是可将研究集中于单位正方形域上的插值问题。

---

## 题型二：Coons曲面构造

### 2.1 双线性Coons曲面片

给定单位正方形域 $[0,1] \times [0,1]$，算子 $P$ 在 $x=0,1$ 上插值，算子 $Q$ 在 $y=0,1$ 上插值。

布尔和：
$$(P \oplus Q) f = Pf + Qf - P(Qf)$$

在单位正方形的四条边上插值，即：
$$(P \oplus Q) f(0,y) = f(0,y), \quad (P \oplus Q) f(1,y) = f(1,y)$$
$$(P \oplus Q) f(x,0) = f(x,0), \quad (P \oplus Q) f(x,1) = f(x,1)$$

$$Pf(x,y) = (1-x)f(0,y) + x f(1,y)$$
$$Qf(x,y) = (1-y)f(x,0) + y f(x,1)$$

$$\begin{aligned}
P(Qf)(x,y) &= (1-x)[(1-y)f(0,0) + yf(0,1)] + x[(1-y)f(1,0) + yf(1,1)] \\
&= (1-x)(1-y)f(0,0) + (1-x)y f(0,1) + x(1-y)f(1,0) + xy f(1,1)
\end{aligned}$$

### 2.2 带法向导数的Coons曲面片

为在四条边上同时插值函数值**和法向导数**，取：
$$P f = \phi_0(x)f(0,y) + \phi_1(x)f(1,y) + \psi_0(x)f_x(0,y) + \psi_1(x)f_x(1,y)$$
$$Q f = \phi_0(y)f(x,0) + \phi_1(y)f(x,1) + \psi_0(y)f_y(x,0) + \psi_1(y)f_y(x,1)$$

其中 $\phi_0, \phi_1, \psi_0, \psi_1$ 是以区间两端点的**三次Hermite插值基函数**：
$$\phi_0(t) = (1-t)^2(1+2t),\; \phi_1(t) = t^2(3-2t),\; \psi_0(t) = t(1-t)^2,\; \psi_1(t) = -t^2(1-t)$$

#### 关键：$P$ 与 $Q$ 的张量积 $P Q$

先计算 $P(Q f)$：
$$P(Q f)(x,y) = \sum_{i=0}^{1}\sum_{j=0}^{1} \left[\Phi_i(x) \Phi_j(y) f(x_i, y_j) + \Phi_i(x) \Psi_j(y) f_y(x_i, y_j) + \Psi_i(x) \Phi_j(y) f_x(x_i, y_j) + \Psi_i(x) \Psi_j(y) f_{xy}(x_i, y_j)\right]$$

#### 信息矩阵（张量积矩阵）

$$[M] = \begin{bmatrix}
f(0,0) & f(0,1) & f_y(0,0) & f_y(0,1) \\
f(1,0) & f(1,1) & f_y(1,0) & f_y(1,1) \\
f_x(0,0) & f_x(0,1) & f_{xy}(0,0) & f_{xy}(0,1) \\
f_x(1,0) & f_x(1,1) & f_{xy}(1,0) & f_{xy}(1,1)
\end{bmatrix}$$

四个子块分别是：角点函数值、边界的 $y$ 向导数、边界的 $x$ 向导数、扭转向量。

#### Coons曲面公式

$$S(x,y) = (P \oplus Q) f = [\phi_0(x)\;\phi_1(x)\;\psi_0(x)\;\psi_1(x)] \cdot [M] \cdot \begin{bmatrix} \phi_0(y) \\ \phi_1(y) \\ \psi_0(y) \\ \psi_1(y) \end{bmatrix}$$

注意信息矩阵中的**扭转块**是二阶混合偏微商 $f_{xy}$，通常假定 $f_{xy}=f_{yx}$（求导次序无关）。

---

### 2.3 协调条件

要使插值四边及法向导数，必须在单位正方形的**四个顶点**上满足**协调条件**（也称为**相容性条件**）：

$$\boxed{ f_{xy}(0,0) = f_{yx}(0,0),\; f_{xy}(0,1) = f_{yx}(0,1),\; f_{xy}(1,0) = f_{yx}(1,0),\; f_{xy}(1,1) = f_{yx}(1,1) }$$

**注意：** 在实际问题中，不一定知道是否满足协调条件。如果不满足，则Coons曲面不严格插值四边的法向导数。

---

## 题型三：Gregory/Brown修正曲面

### 3.1 Gregory（格利高里）曲面

**目的：** 修正信息矩阵的扭转块，使得无需协调条件也能严格插值边界法向导数。

**做法：** 将信息矩阵的扭转块替换为：

$$\begin{bmatrix} f_{xy}(0,0) & \frac{u f_{xy}(0,1) + v f_{xy}(0,0)}{u+v} \\ \frac{u f_{xy}(1,0) + v f_{xy}(0,0)}{u+v} & \frac{u f_{xy}(1,1) + v f_{xy}(1,0)}{u+v} \end{bmatrix}$$

其中 $u,v$ 是适当选取的参数。

替换后记为 $[\tilde{M}]$，则Gregory曲面：
$$\tilde{S}(x,y) = [\Phi(x)]^T [\tilde{M}] [\Phi(y)]$$

可以**直接验证**：Gregory曲面在四条边上严格插值 $f$ 及法向导数值，**无需协调条件**。

**注意：** 当协调条件成立时，Gregory曲面退化为普通的Coons曲面（因为 $f_{xy}=f_{yx}$）。

---

### 3.2 Brown（布朗）曲面

**另一种修正方案：** 使用基于**非对称插值**的方法。

Brown注意到布尔和的非对称形式：
$$P Q f \neq Q P f$$

由此设计了一类基于单变量Hermite插值的修正曲面。Brown曲面在边界上插值函数值和法向导数，而且可以用一元Hermite基函数构造，不依赖于二阶混合偏微商的对称性。

构造形式：
$$S_B(x,y) = \sum_{i=0}^{1} [f(x_i, y)H_i^{(0)}(x) + f_x(x_i, y)H_i^{(1)}(x)] + \sum_{j=0}^{1} [f(x, y_j)\tilde{H}_j^{(0)}(y) + f_y(x, y_j)\tilde{H}_j^{(1)}(y)] - \text{边界重叠项}$$

其中 $H_i^{(0)}, H_i^{(1)}$ 是一元Hermite基函数。

---

### 3.3 协调条件小结

| 曲面类型 | 需要协调条件？ | 插值内容 | 特点 |
|---------|-------------|---------|------|
| 双线性Coons | 否 | 仅四条边上的函数值 | 最简单 |
| Coons曲面 | **是** | 四条边上函数值+法向导数 | 需要 $f_{xy}=f_{yx}$ |
| Gregory曲面 | **否** | 四条边上函数值+法向导数 | 修改了扭转块 |
| Brown曲面 | **否** | 四条边上函数值+法向导数 | 基于非对称插值 |

---

## 题型四：超限插值公式的离散化

### 4.1 离散化思想

在超限插值公式中含有边界函数（如 $f(0,y), f(1,y), f_x(0,y), f_x(1,y)$ 等）。离散化就是用**边界上的离散点值**去近似替代这些连续的边界函数。

### 4.2 示例：双线性Coons曲面的离散化

原始双线性Coons公式：
$$S(x,y) = (1-x)f(0,y) + x f(1,y) + (1-y)f(x,0) + y f(x,1) - [(1-x)(1-y)f(0,0) + (1-x)y f(0,1) + x(1-y)f(1,0) + xy f(1,1)]$$

将 $f(0,y), f(1,y)$ 用 $y$ 方向的一元Lagrange插值替代：
$$f(0,y) \approx \sum_{j=0}^{n} l_j(y) f(0, y_j)$$
$$f(1,y) \approx \sum_{j=0}^{n} l_j(y) f(1, y_j)$$

同理：
$$f(x,0) \approx \sum_{i=0}^{m} l_i(x) f(x_i, 0)$$
$$f(x,1) \approx \sum_{i=0}^{m} l_i(x) f(x_i, 1)$$

代入后得到的就是**矩形网点上的Lagrange插值公式**，插值空间为 $P_{m,n}$。

### 4.3 离散化的一般步骤

1. 写出超限插值公式
2. 将其中的边界函数用一元插值（Lagrange、Hermite等）替代
3. 代入整理即得离散的插值公式

---

## 题型五：三角形域上的超限插值（三角形Coons曲面）

### 5.1 三角形重心坐标

设三角形三顶点为 $A, B, C$，对三角形内任一点 $P$，定义重心坐标（面积坐标）$(u, v, w)$：
$$u = \frac{S_{PBC}}{S_{ABC}},\; v = \frac{S_{APC}}{S_{ABC}},\; w = \frac{S_{ABP}}{S_{ABC}}$$

满足 $u+v+w=1$，且 $u,v,w \geq 0$（在三角形内部）。

### 5.2 三角形域上的超限插值算子

在三角形三边上分别定义三个插值算子 $P_1, P_2, P_3$：
- $P_1$：在 $u=0$ 边（即BC边）上插值
- $P_2$：在 $v=0$ 边（即AC边）上插值
- $P_3$：在 $w=0$ 边（即AB边）上插值

对于函数值插值：
$$P_1 f = (1-u) \cdot f(0, v, 1-v)$$

（因为 $u=0$ 时 $v+w=1$，参数化BC边）

三个算子的布尔和：
$$S = P_1 \oplus P_2 \oplus P_3$$

展开为：
$$S = P_1 + P_2 + P_3 - P_1P_2 - P_2P_3 - P_3P_1 + P_1P_2P_3$$

### 5.3 三角形Coons曲面（三次Hermite型）

类似于矩形域，可以定义在三角形三条边上同时插值函数值和法向导数的Coons曲面。

取三次Hermite基函数 $\phi_0, \phi_1, \psi_0, \psi_1$（定义在 $[0,1]$ 上），则三角形Coons曲面为：

$$S(u,v,w) = \sum_{\text{cyclic}} \left[\phi_0(u)f(0,v,1-v) + \phi_1(u)f(1,0,0) + \psi_0(u)f_n(0,v,1-v)\right] - \text{顶点重叠项}$$

其中 $\sum_{\text{cyclic}}$ 表示对 $(u,v,w)$ 循环求和。

### 5.4 与矩形Coons曲面的比较

| 特性 | 矩形Coons曲面 | 三角形Coons曲面 |
|------|-------------|---------------|
| 参数域 | $[0,1] \times [0,1]$ | $u+v+w=1,\; u,v,w \geq 0$ |
| 边界 | 4条参数曲线 | 3条参数曲线 |
| 协调条件 | 4个顶点需满足 | 3个顶点需满足 |
| 布尔和公式 | $P \oplus Q$ | $P_1 \oplus P_2 \oplus P_3$ |

---

## 附录A：核心公式速查卡

### A.1 二元差商定义

$$f[x_i, x_j; y] = \frac{f(x_i,y) - f(x_j,y)}{x_i - x_j}$$
$$f[x; y_i, y_j] = \frac{f(x,y_i) - f(x,y_j)}{y_i - y_j}$$
$$f[x_0,\ldots,x_l; y_0,\ldots,y_p] = \sum_{k=0}^{l}\sum_{j=0}^{p} \frac{f(x_k, y_j)}{\prod_{h \neq k}(x_k-x_h) \prod_{i \neq j}(y_j-y_i)}$$

### A.2 插值空间维数

| 空间 | 含义 | 维数 |
|------|------|------|
| $P_n$ | 所有次数 ≤ n 的二元多项式 | $\frac{(n+1)(n+2)}{2}$ |
| $P_{m,n}$ | x次数≤m, y次数≤n 的二元多项式 | $(m+1)(n+1)$ |

### A.3 适定结点组构造（总点数=空间维数）

| 类型 | 符号 | 构造方式 | 适用空间 |
|------|------|---------|---------|
| 直线型 | $D_n$ | 每步在一条新直线上取点 | $P_n$ |
| 弧线型 | $D2_n$ | 每步在一条新二次曲线上取点 | $P_{2n}$ |
| 圆型 | 同上 | 弧线型的特例（同心圆） | $P_{2n}$ |
| 竖线型 | $U_{m,n}$ | 在m+1条竖线上各取n+1点 | $P_{m,n}$ |
| 十字型 | $B_{n,n}$ | 每步在一个新十字上取点 | $P_{n,n}$ |

### A.4 Coons曲面信息矩阵结构

$$[M] = \begin{bmatrix} \text{函数值} & \text{y向导数} \\ \text{x向导数} & \text{扭转向量} \end{bmatrix}_{4 \times 4}$$

- 左上 $2 \times 2$：四角点的 $f$ 值
- 右上 $2 \times 2$：四角点的 $f_y$ 值
- 左下 $2 \times 2$：四角点的 $f_x$ 值
- 右下 $2 \times 2$：四角点的 $f_{xy}$ 值（扭转向量）

---

> **整理完成时间：** 2026年7月7日
> **覆盖内容：** 第一章 §0—§10 + 第二章 §1—§4 + 期末试卷全部6道大题
