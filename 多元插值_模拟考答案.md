# Day 6 模拟考试 — 参考答案

---

## 第一题：差商表 + Newton 插值

```
数据：
       y=0  y=1  y=2  y=3
x=0    1    2    5    10
x=1    2    3    6    
x=2    5    6        
x=3    10            
```

> 观察数据：f(x,y)=x²+y²+1？验证：(0,0):1✓, (1,0):2✓, (2,0):5✓, (3,0):10✓, (0,1):2✓, (0,2):5✓, (0,3):10✓, (1,1):3✓, (1,2):6✓, (2,1):6✓

### 差商表（y=0列，x方向）

| xᵢ | f(xᵢ,0) | 一阶差商 | 二阶差商 | 三阶差商 |
|----|---------|---------|---------|---------|
| 0  | 1       |         |         |         |
| 1  | 2       | 1       |         |         |
| 2  | 5       | 3       | 1       |         |
| 3  | 10      | 5       | 1       | 0       |

### 差商表（x=0行，y方向）

| yⱼ | f(0,yⱼ) | 一阶差商 | 二阶差商 | 三阶差商 |
|----|---------|---------|---------|---------|
| 0  | 1       |         |         |         |
| 1  | 2       | 1       |         |         |
| 2  | 5       | 3       | 1       |         |
| 3  | 10      | 5       | 1       | 0       |

### 混合差商

需要 f[x₀,x₁; y₀,y₁]：
f[x₀,x₁; y₀] = (2-1)/1 = 1
f[x₀,x₁; y₁] = (3-2)/1 = 1
→ f[x₀,x₁; y₀,y₁] = (1-1)/1 = 0

同理所有混合差商均为0（因为 f(x,y)=x²+y²+1 是变量分离的）

### Newton插值公式

取对角元（或三角形区域内的差商）：

```
P(x,y) = f₀₀ 
       + f[x₀,x₁;y₀]·x
       + f[x₀,x₁,x₂;y₀]·x(x-1)
       + f[x₀,x₁,x₂,x₃;y₀]·x(x-1)(x-2)
       + f[x₀;y₀,y₁]·y
       + f[x₀;y₀,y₁,y₂]·y(y-1)
       + f[x₀;y₀,y₁,y₂,y₃]·y(y-1)(y-2)
```

```
= 1 + 1·x + 1·x(x-1) + 0·x(x-1)(x-2) + 1·y + 1·y(y-1) + 0·y(y-1)(y-2)
= 1 + x + x² - x + y + y² - y
= x² + y² + 1 ✓
```

---

## 第二题：Lagrange 插值

```
数据：
       y=0  y=1
x=0    2    3
x=1    4    5
x=2    6    7
```

### x方向基函数（x₀=0, x₁=1, x₂=2）

```
l₀(x) = (x-1)(x-2)/[(0-1)(0-2)] = (x²-3x+2)/2
l₁(x) = (x-0)(x-2)/[(1-0)(1-2)] = -x(x-2) = -x²+2x
l₂(x) = (x-0)(x-1)/[(2-0)(2-1)] = x(x-1)/2 = (x²-x)/2
```

### y方向基函数（y₀=0, y₁=1）

```
l₀(y) = (y-1)/(0-1) = 1-y
l₁(y) = (y-0)/(1-0) = y
```

### 构造二元插值

```
L(x,y) = 2·(x²-3x+2)/2·(1-y) + 3·(x²-3x+2)/2·y
       + 4·(-x²+2x)·(1-y)     + 5·(-x²+2x)·y
       + 6·(x²-x)/2·(1-y)     + 7·(x²-x)/2·y
```

按 y=0 和 y=1 分组：

**y=0 部分（乘 (1-y)）**：
```
= [2(x²-3x+2)/2 + 4(-x²+2x) + 6(x²-x)/2] · (1-y)
= [(x²-3x+2) + (-4x²+8x) + (3x²-3x)] · (1-y)
= [x²-3x+2-4x²+8x+3x²-3x] · (1-y)
= [0x² + 2x + 2] · (1-y)
= (2x+2)(1-y)
```

**y=1 部分（乘 y）**：
```
= [3(x²-3x+2)/2 + 5(-x²+2x) + 7(x²-x)/2] · y
= [1.5(x²-3x+2) + (-5x²+10x) + 3.5(x²-x)] · y
= [1.5x²-4.5x+3 - 5x²+10x + 3.5x²-3.5x] · y
= [0x² + 2x + 3] · y
= (2x+3)y
```

**最终**：
```
L(x,y) = (2x+2)(1-y) + (2x+3)y
       = 2x+2 - 2xy - 2y + 2xy + 3y
       = 2x + y + 2
```

验证：L(0,0)=2✓, L(0,1)=3✓, L(1,0)=4✓, L(1,1)=5✓, L(2,0)=6✓, L(2,1)=7✓

---

## 第三题：Aitken 法

```
三点：A(0,0):f=1, B(1,0):f=3, C(0,1):f=2
```

### 分析

数据：
- y=0 行：(0,0):1, (1,0):3
- y=1 行：(0,1):2（只有一点！）

因为 y=1 行只有一个点，先用 x 方向在 y=0 行插值，再在 y 方向插值。

### 第1步：对 y=0 做 x 方向一维 Aitken

```
P_{y=0}(x) = det([1  0-x; 3  1-x]) / (1-0)
           = [1(1-x) - 3(0-x)] / 1
           = 1 - x + 3x
           = 1 + 2x
```

### 第2步：对 y 方向做 Aitken

y=0: P = 1+2x, y=1: f(0,1) = 2

```
P(x,y) = det([1+2x  0-y; 2  1-y]) / (1-0)
       = [(1+2x)(1-y) - 2(0-y)] / 1
       = (1+2x)(1-y) + 2y
       = 1 + 2x - y - 2xy + 2y
       = 1 + 2x + y - 2xy
```

验证：
- P(0,0) = 1 ✓
- P(1,0) = 1+2=3 ✓
- P(0,1) = 1+1=2 ✓

**答案**：P(x,y) = 1 + 2x + y - 2xy

---

## 第四题：论述 — 多元插值存在唯一性

### 参考答案（完整版）

1. **一元多项式插值**：给定n+1个互异节点，总存在唯一的≤n次插值多项式。原因是Vandermonde行列式不为零（Haar条件成立）。

2. **多元多项式插值**：在Rᵈ(d≥2)中，不存在对所有点集都适定的有限维插值空间。例如：
   - 三节点共线时，二元一次插值无唯一解（无限多解）
   - 六节点共圆锥曲线时，二元二次插值可能无解
   
   根本原因：多元多项式的零点集是代数曲线/曲面，具有几何结构，节点的几何分布会影响插值矩阵的可逆性。

3. **解决办法**：
   - **节点几何约束法**：要求节点满足特定的几何构型（矩形张量积网格用Lagrange、三角形网格用Newton等）
   - **径向基函数(RBF)法**：用φ(||x-xᵢ||)代替多项式基，Micchelli定理保证正定性
   - **分片插值/有限元法**：将定义域剖分成小单元，每单元内用低次多项式

---

## 第五题：论述 — 多元插值与深度学习（25分）

### 参考答案（完整版）

**一、深度学习的数学本质**

深度学习（DNN）从数学角度是一个**高维函数逼近器**。给定训练数据 {(xᵢ,yᵢ)}，DNN 试图寻找函数 f(x; θ) 使拟合误差最小。在过参数化时代，训练误差趋近于零，这相当于在高维空间中做精确插值。

**二、激活函数与插值的关系**

ReLU激活函数 DNN = 连续多元**分段线性插值**。每层ReLU将输入空间划分为多面体区域，每区域内是线性的。层数越深，划分越细，逼近能力越强。这与样条函数的分段多项式本质相通。

**三、具体应用中的多元插值**

- 图像超分辨率：双线性/双三次插值是基础算法
- Spatial Transformer Networks (STN)：使用双线性采样核做空间变换
- 神经辐射场 (NeRF)：沿射线做数值积分 = 插值采样

**四、深度学习如何从插值理论获益**

1. **KAN网络**：用可学习的B样条函数替代传统线性权重，数学上严格基于Kolmogorov-Arnold表示定理
2. **流形假设**：数据分布在高维空间的低维流形上 → 启发基于流形的插值方法
3. **RBF网络**：径向基函数插值的神经网络实现
4. **高斯过程**：与插值同源，提供不确定性估计

**五、总结**

多元插值理论为深度学习提供了数学基础和架构设计灵感，深度学习则通过层次化特征提取突破了传统插值的维数灾难。两者相辅相成。

---

## 第六题：RBF优缺点（15分）

### 参考答案

**优点**：
1. **无网格约束**：RBF只需要节点坐标，不需要任何网格结构，天然适合散乱数据插值
2. **存在唯一性保证**：Micchelli定理证明，对高斯核、多二次曲面等常见RBF，插值矩阵严格正定，解总存在且唯一
3. **与维数无关**：RBF只取决于点间欧氏距离 r=||x-xᵢ||，公式形式在不同维度下保持一致
4. **灵活性**：通过选择不同基函数可适应不同数据特征

**缺点**：
1. **计算复杂度高**：N个节点需解N×N全局稠密线性方程组，直接法O(N³)，存储O(N²)
2. **形状参数敏感**：形状参数c选择极端困难。c太小→病态矩阵，c太大→插值面过于平坦
3. **龙格现象**：全局RBF在边缘区域容易出现振荡（类似高次多项式插值）
4. **外推能力弱**：基函数随距离衰减，数据覆盖范围外的预测通常不可靠

---

**全部模拟题答案结束！祝你考试顺利！🎉**
