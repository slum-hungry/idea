# RISC-V 上实用高效的 x86-64 模拟

> **论文标题:** Practical and Efficient x86-64 Emulation on RISC-V
> **会议:** EUROSYS '26, April 27–30, 2026, Edinburgh, Scotland UK
> **DOI:** https://doi.org/10.1145/3767295.3803574
> **项目地址:** https://github.com/ptitSeb/box64
> **许可:** Creative Commons Attribution 4.0 International License

---

## 作者与单位

| 作者 | 邮箱 | 单位 |
|------|------|------|
| Xiongchuan Tan ∗ | txc24@mails.tsinghua.edu.cn | RIOS Lab, 清华大学（深圳） |
| Yang Liu ∗ | liuyang22@iscas.ac.cn | 中国科学院软件研究所（北京） |
| Sebastien Chevalier | sebastien.chev@gmail.com | 独立研究者（法国） |
| Yangyu Chen | cyy@cyyself.name | 重庆大学计算机学院 |
| Xiaoyi Liu | xiaoyi-l23@mails.tsinghua.edu.cn | 清华大学计算机系（北京） |
| Haohuan Fu | haohuan@tsinghua.edu.cn | RIOS Lab, 清华大学（深圳） |

> ∗ 共同第一作者

---

## 摘要

随着 RISC-V 在各个计算领域日益普及，**在 RISC-V 平台上运行 x86-64 应用程序**的需求变得至关重要。

二进制翻译在确保不同指令集架构(ISA)之间的软件可移植性方面发挥着关键作用。然而，广泛使用的 **x86-64 与新兴的 RISC-V 平台之间的架构差异**给在保持性能和兼容性的同时实现有效翻译带来了挑战。

为应对这一挑战，我们**扩展了 Box64**，使其能够在 RISC-V 系统上高效执行 x86-64 软件。我们的方法利用了一系列通用和 RISC-V 特定的优化技术，包括：

- 创新的 **TSO（全序存储）模拟方法**
- 改进的间接分支与条件分支优化
- **动态库加速**

评估结果表明，我们的方案**相比原生执行的性能损失平均不到 40%**，比 QEMU-TCG 快 **3.3 倍**，并能稳健支持从办公套件到视频游戏的各种应用。

**CCS 概念:** 即时编译器；虚拟机；精简指令集计算；复杂指令集计算；处理器与内存架构
**关键词:** 动态二进制翻译, RISC-V, x86-64, 内存一致性模型

---

## 1. 引言

### 背景与动机

RISC-V 作为开放标准 ISA 的迅速崛起正在重塑硬件格局（从边缘计算到 HPC）。然而其最大障碍仍是**"软件鸿沟"**：大量面向 x86 等成熟架构构建的应用程序缺乏原生支持。尽管二进制翻译提供了有前景的解决方案，但现有的通用方案（如 QEMU）往往**性能开销过高**或兼容性不足。

### 核心挑战：x86 与 RISC-V 之间的架构鸿沟

与 x86→Arm 的差距相比，x86→RISC-V 的差距更为显著（见图 1）。关键差异包括：

| 特性 | x86-64 | RISC-V RV64G |
|------|--------|-------------|
| 内存模型 | TSO（全序存储） | 弱内存模型（WMM） |
| 通用寄存器 | 16 个 | 32 个 |
| 原子操作 | 1B/2B/16B，支持非对齐 | 仅 4B/8B，要求对齐 |
| 浮点 | x87 80-bit 扩展精度 | IEEE 754 单/双精度 |
| 状态寄存器 | FLAGS（含 OF/SF/ZF/AF/PF/CF） | NZCV（仅 4 位） |
| 架构类型 | 寄存器-内存架构 | 加载-存储架构 |

### 本文贡献

1. **首个实用 DBT 系统**：扩展 Box64，构建了首个能在 RISC-V 上运行真实复杂 x86-64 应用（如 WPS Office、桌面游戏）的动态二进制翻译系统
2. **高性能翻译引擎**：达到 QEMU 的 **3.3 倍**性能和原生的 **67%** 性能，JIT 编译器与 API 直接包装机制的协同设计
3. **统一翻译框架**：弥合 x86-64 与碎片化 RISC-V 硬件之间的架构差距
4. **务实高效的 TSO 模拟方法**：基于真实应用行为，使用选择性内存屏障确保多线程正确性

---

## 2. 背景与动机

### 2.1 二进制翻译

二进制翻译分为**静态（SBT）**和**动态（DBT）**两类：

- **SBT（静态二进制翻译）**：在程序执行前提前翻译（AOT 编译），但准确解析完整控制流极为困难，尤其对 x86 的变长指令、自修改代码（SMC）、代码/数据混合等复杂情况
- **DBT（动态二进制翻译）**：以 JIT 编译方式运行，采用**发现→翻译→执行**循环，使用翻译缓存复用结果。面临严格的实时性能约束

### 2.2 内存一致性模型与 TSO

- **x86-64 采用 TSO（Total Store Ordering，全序存储）**：所有 CPU 核心看到的内存写入顺序一致，仅允许 Store-Load 重排序
- **RISC-V 采用 WMM（Weak Memory Model，弱内存模型）**：允许多种重排序类型

> 💡 **关键问题**：在 WMM 架构上直接翻译 x86-TSO 的内存操作而不插入屏障，会导致多线程数据竞争或崩溃。由于大多数 WMM 架构缺少 TSO 的硬件支持（如 Apple Silicon 的硬件 TSO 模式），二进制翻译器必须插入显式内存屏障。

### 2.3 x86 在 RISC-V 上模拟的挑战

**浮点单元（FPU）模拟**：x86 的 x87 指令集支持 80-bit 扩展精度，且运行时由状态寄存器动态选择精度。许多 32 位 x86 应用依赖特定 x87 行为。NaN 生成和舍入行为的不匹配也是重要的边界情况。

**FLAGS 寄存器模拟**：Arm 和 LoongArch 有硬件扩展辅助 x86 标志位生成，而 RISC-V 完全依赖软件模拟。标准 RISC-V ISA 的位操作指令限制使高效 FLAGS 模拟极具挑战。

**原子内存操作**：x86 允许 `LOCK CMPXCHG` 等指令的非对齐访问，而 RISC-V A 扩展严格要求对齐。128 位 `CMPXCHG16B` 在 RISC-V 上没有直接等价指令。

**TSO 模拟**：由于 RISC-V 缺乏硬件辅助，必须完全依赖内存屏障指令。

**SIMD/FP 寄存器管理**：x86 使用统一的 XMM 寄存器，我们的设计需要在三个位置之间同步：内存（模拟状态）、标量 FP 寄存器、向量寄存器。

---

## 3. 相关工作

### 3.1 其他平台上的 x86 模拟

| 系统 | 特点 |
|------|------|
| **QEMU** | 通用模拟器，TCG 后端速度较慢 |
| **FEX-Emu** | x86→Arm64 模拟器，利用 RCpc 指令优化 TSO |
| **Rosetta 2** | Apple 的 x86→Arm 转译器，利用硬件 TSO 模式 |
| **HQEMU** | 多线程化 QEMU |
| **ExaGear** | 华为的商业 x86→Arm 模拟器 |
| **CrossDBT** | 基于 LLVM 的用户级 DBT |

**动态库加速**是开源模拟器的区分特征：标准 QEMU 仅实现系统调用模拟，而 Box64 和 FEX 实现了将 x86 调用者与原生库函数桥接的转发机制。

### 3.2 TSO 模拟研究

| 方案 | 方法 | 特点 |
|------|------|------|
| **QEMU** | 保守软件方法：每个内存访问都插屏障 | 性能差 |
| **Risotto** | 优化指令映射，消除冗余屏障 | 改善 QEMU |
| **CrossMapping** | 指令间分析，用 DFA 推导最优屏障类型 | 严格软件 TSO 模拟中性能最高 |
| **AtoMig** | 编译时检测隐式同步模式 | 需要源码 |
| **MSVC /volatile** | 编译器生成元数据标记 volatile 访问 | 商业方案 |
| **Apple Silicon** | 硬件 TSO 模式（控制寄存器切换） | 硬件辅助，无软件开销 |
| **Arm RCpc** | LDAPR/STLR 指令遵循 TSO 约束 | FEX 利用此特性 |

---

## 4. 系统设计概述

### 核心设计原则

Box64 采用**应用级模拟**策略，只关注用户空间，简化了实现复杂度。架构包含：

- **JIT 引擎**：将 x86 指令转换为 RISC-V 指令
- **动态链接器**：处理跨架构依赖解析
- **系统调用转换层**：比 QEMU 薄得多
- **库转发机制**：将 guest 库调用重定向到 host 原生库

### 高层模拟策略

> 🔑 **关键设计**：优先考虑**库级互操作性**而非低级系统调用翻译。

自定义动态链接器处理跨架构依赖解析，生成轻量级"粘合代码"管理 guest/host 调用约定转换。这带来两个主要好处：
1. 避免常用库函数的冗余翻译
2. 避免不稳定设备 ioctl 的复杂性——GPU 驱动通信直接由 host 原生库处理

### 务实的模拟哲学

- 基于观察到大多数应用不需要完整 80-bit 精度，x87 模拟使用 32/64-bit 硬件浮点指令
- 内存模型方面：不追求所有边界情况的严格完整模拟，而是在绝大多数真实多线程软件上确保**正确且高性能**
- 为信号处理的准确性，保持 guest/host 指令的**顺序映射**，放弃部分重排序优化

---

## 5. 高性能 JIT 引擎设计

### 5.1 分支优化

**间接分支**是 DBT 系统中的主要性能瓶颈。每个间接跳转需要在映射 guest 地址到 host 地址的数据结构中查找。

**多级跳转表**：
- 类似页表结构
- 内存占用与查找延迟之间的权衡
- 其他方案：哈希表（FEX）、平坦表（Tiaozhuan）

**CALL/RET 优化——利用硬件 RAS**：

核心思想（源自 MAMBO-X64）：将 guest `CALL`/`RET` 对翻译为 host 原生 `JAL`/`JALR`，让 host CPU 的**返回地址栈（RAS）**进行高精度分支预测。

实现方式：
- 维护软件**影子栈**（shadow stack），与 guest 栈并行运行
- guest `CALL` → host `JAL`，将翻译后的返回地址压入影子栈
- guest `RET` → 从影子栈弹出地址执行 `JALR`

> 📊 效果：利用 host 硬件 RAS 预测，大幅降低间接跳转开销

### 5.2 FLAGS 寄存器的惰性求值

x86 指令普遍设置 FLAGS 寄存器中的 6 个状态位（OF/SF/ZF/AF/PF/CF），但大部分计算结果很少被后续指令使用。

**惰性求值策略**：
1. 标记指令而非计算：翻译 FLAGS 定义指令时，只保存必要的中间操作数
2. 推迟到消费方：将最终标志值的计算推迟到真正需要它的后继基本块
3. **跨函数边界优化**：基于 ABI 约定（状态标志不跨函数保留），在 CALL/RET 边界不生成 FLAGS 相关代码

**TF（陷阱标志）处理**：
- 监控 FLAGS 寄存器的所有写入
- 若写入设置了 TF 位，则**去优化**执行，切换到更慢但精确的解释器
- 逐条执行直到 TF 清零，再恢复 JIT 编译执行

### 5.3 寄存器映射与分配

**静态 ABI 感知映射**：

利用 RISC-V 较大的寄存器文件，为每个 x86 通用寄存器、FLAGS 寄存器（rflags）、指令指针（rip）分配专用 host 寄存器。

| 寄存器类型 | x86 寄存器 | RISC-V 映射 |
|-----------|-----------|------------|
| 被调用者保存 | rbx, rbp | s0-s11 |
| 参数传递 | rdi-r9 | a0-a5 |

这种设计的好处：
1. **精确重建机器状态**：从 RISC-V 上下文即时重建 x86 的 ucontext，对信号处理至关重要
2. **ABI 对齐**：最小化模拟代码到原生库调用边界的寄存器移动

**OF 位的特殊处理**：
- RISC-V 的 `ORI` 指令使用 12-bit 符号扩展立即数，无法独立设置第 11 位（恰好是 x86 OF 位位置）
- 解决方案：内部将 OF 重定位到 host FLAGS 寄存器的未使用位

### 5.4 RISC-V 上的条件操作处理

**两级优化策略**：

**第一级：惰性 FLAGS 求值**
- 仅当标志位被确认存活时才计算
- 但仍需 3-15 条 RISC-V 指令来物化条件操作所需的标志位

**第二级：宏操作融合（Macro-Op Fusion）**

> 💡 核心洞察：`CMP rdi, rsi` 后跟 `JAE target` 的真正语义只是"若 rdi ≥ rsi 则跳转"

实现：
- JIT 的数据流分析检测相邻的定义-使用对
- 将其**融合**为单个逻辑操作
- 直接生成对应 RISC-V 条件分支（如 `BGEU rdi, rsi, target`）
- 完全**绕过** FLAGS 位的计算和存储

| 示例 | x86 原始 | RISC-V 优化后 |
|------|---------|-------------|
| 逐条翻译 | `CMP rdi, rsi` + `JAE target` | 15+ 条指令 |
| 宏操作融合 | 同上 | `BGEU rdi, rsi, target`（1 条指令） |

---

## 6. 弥合架构差异：兼容性设计

### 6.1 RISC-V 扩展的统一处理

RISC-V 模块化 ISA 带来的挑战：碎片化的硬件生态。

**两级代码生成策略**：

**第一级：通用基线（RV64G）**
- 确保在任何常见 RISC-V 处理器上均可运行
- 刻意**不采用 C（压缩）扩展**（其限制性指令编码和有限寄存器访问使 JIT 编译器设计复杂化）

**第二级：可选扩展的动态利用**
- 动态检测硬件扩展
- **Zba/XuanTie 扩展**：使用融合的移位+加法指令生成更紧凑的地址计算代码
- **条件移动指令**：替代多指令分支序列，减少对硬件分支预测器的污染
- **Zbb/Zbs 扩展**：`POPCNT` 等指令直接映射到高效单指令
- **V（向量）扩展**：x86 SIMD 操作映射到 RVV 指令集

**RVV 的复杂性**：
- 变长向量寄存器和运行时可配置的元素宽度
- 需要在向量操作前插入配置指令（`VSETVLI`），通过缓存向量配置状态减轻开销
- RISC-V 中 FP 和向量寄存器文件分离：标量 FP→F/D 扩展，SIMD→RVV，在交界面插入显式寄存器传输指令

### 6.2 x87 FPU 的务实模拟

**策略**：使用原生 RISC-V 64-bit 双精度 FP 扩展

- **有意避免**代价高昂的 80-bit 扩展精度完整模拟
- 接受对绝大多数目标应用无足轻重的精度损失

**FILD-FISTP 模式的特殊处理**：
- 某些软件用 `FILD`（加载整数）后跟 `FISTP`（存储整数并弹栈）来实现快速 64-bit 内存复制
- 这依赖于 80-bit 格式的 64-bit 有效位
- 解决方案：**静态检测**无中间算术操作的 FILD-FISTP 序列，翻译为整数加载/存储操作

**x87 栈状态**：静态编译时跟踪，避免运行时栈模拟开销。在不常见的边界情况可能引入不准确。

### 6.3 内存操作的软件模拟

#### 6.3.1 x86 原子原语模拟

| x86 特性 | RISC-V 限制 | 解决方案 |
|---------|-----------|---------|
| 1/2 字节原子操作 | 仅 4/8 字节 | 在对齐内存字上使用更宽的 RISC-V 原语 |
| 16 字节 CMPXCHG16B | 无直接等价指令 | 备选设计可讨论，性能与正确性权衡 |
| 非对齐原子访问 | 严格对齐要求 | 按需降级为安全但更慢的逐字节序列 |

#### 6.3.2 非对齐访问的动态处理

**两阶段动态方案**：

- **阶段 1：按需修复**。非对齐访问触发硬件异常 → 信号处理器将其分解为单字节存储序列 → 标记故障地址并失效对应 JIT 缓存块
- **阶段 2：主动重编译**。再次执行该代码块时，JIT 发现缓存失效 → 将问题指令直接翻译为安全的逐字节存储序列

> 🎯 优势：不对全局性能造成惩罚，仅在证明确实需要的地址应用更慢的模式

### 6.4 自修改代码（SMC）的低开销处理

- x86 采用**一致的指令/数据缓存**，便于编程
- RISC-V 偏好**显式缓存管理**，需要缓存刷新指令

**Box64 的策略**：

1. 翻译前将 guest 代码页设为**只读**
2. 任何对代码页的写入触发陷阱信号
3. 信号处理器将该代码页内所有代码块标记为"脏"（dirty），将页设为可写，重新执行写入
4. 标记为脏而非直接失效，避免同页中其他未修改指令序列的不必要重编译
5. 再次执行脏指令序列时，将校验和与元数据中的预期值比较
6. 极端情况（指令修改自身）：回退到 **x86 解释器**，逐条取指执行直到遇到分支指令

### 6.5 动态库加速

DBT 面临生成代码质量难以匹敌高级编译器的挑战。解决方案：**库函数转发**

**利用开源 Linux 生态**：
- 常用库（glibc、pthreads、SDL 等）在 host 系统上已有原生版本
- guest 的库调用转发到 host 实现

**实现方式**：
- 主要通过填充**过程链接表（PLT）**实现
- 自定义动态链接器处理跨架构依赖解析
- ABI 转换：在调用前后进行参数翻译

**特殊处理**：
- `setjmp`/`longjmp`：创建异常控制流，需特殊关注
- 信号处理器：不能包装（host CPU 无法解释 guest 指令）

---

## 7. 弱序架构上的高效 TSO 模拟

### 核心洞察

许多复杂的多线程应用**无需显式 TSO 模拟即可正确运行**，因为：
- POSIX Threads 等库来自 host 系统
- `LOCK` 前缀的 x86 操作码有隐含的内存屏障
- 额外的屏障变得无用

### 三组件框架

#### 7.1 注解引导的屏障放置

- 利用 MSVC 的 `volatile` 元数据（PE 二进制中的 `/volatile:iso`）
- 从 PE 二进制提取元数据，精确标记内存位置周围放置屏障
- 特别适用于 Windows 游戏

#### 7.2 上下文感知的屏障插入启发式

**5 级 TSO 模拟策略**：

| 级别 | 名称 | 描述 |
|------|------|------|
| **0** | None（默认） | 最少的屏障 |
| **1** | Pragmatic（务实） | 启发式屏障（标量操作） |
| **2** | Prag.+SIMD | 级别 1 + SIMD 操作 |
| **3** | Conservative（保守） | 更多屏障，接近逐条 |
| **4** | Verbatim（逐条） | QEMU TCG 风格的屏障 |

**关键启发式**：
- SIMD 操作通常用于数据并行计算，不涉及同步 → 默认不加屏障
- 标量内存操作更可能涉及同步模式 → 重点加屏障
- 兼容性配置文件索引每个可执行文件/库，自动选择合适级别（目前 **201 条**记录）
- **自动提升机制**：检查依赖库，将 TSO 级别提升到任何依赖所需的最保守级别（如检测到 `UnityPlayer.dll` 或 `libjvm.so` 则自动选择 Pragmatic 级别）

### 局限性

作者坦承这种"最佳努力"方法不提供理论上的正确性保证，需要通过大量测试进行经验验证。对未知应用的开箱即用兼容性仍是未来工作方向。

---

## 8. 评估

### 测试平台

| 平台 | ISA | 核心数 | 特点 |
|------|-----|--------|------|
| SOPHGO SG2044 | rv64gcvb_zvl128 | 64 核 | 有向量扩展，PCIe 支持 GPU |
| Xiangshan Nanhu（香山南湖） | rv64gcb | 1 核 | 无向量扩展 |

### 8.1 单线程性能

**NBench 结果（SG2044 平台）**：
- 整体几何平均性能达到**原生的 67%**
- 相比 QEMU-TCG 快 **3.3×**

**按工作负载分析**：
- **内存密集型**：近原生性能，超过 QEMU 3×（内存操作几乎无需翻译逻辑）
- **整数密集型**：模拟开销较大，仍超过 QEMU 2×
- **浮点密集型**：
  - SG2044（有向量扩展）：开销与整数负载相当
  - Nanhu（无向量扩展）：x86 的 SSE 指令需去向量化，性能下降 **16×**

> 🎯 这一性能差距恰好体现了设计的灵活性：在 Nanhu 上虽慢但能运行，展示了基线兼容层的稳健性

**优化级别预设**：

| 级别 | 包含优化 |
|------|---------|
| Low | 惰性 FLAGS 求值 |
| CallRet | Low + CALL/RET 配对 |
| Default | Low + 宏操作融合 |
| High | CallRet + Default |

### 8.2 多线程性能与 TSO 开销

使用 **PARSEC 3.0** 测试套件（8 线程，SG2044）：

- **Pragmatic 级别（Level 1）** 相比 Verbatim TSO（Level 4）几何平均性能提升 **50%**
- 远超先前文献报道的 7.3%
- Level 1 相比 None（Level 0）仍有显著开销，说明需要更细粒度的方法

### 8.3 开销分解

**代码生成开销**：
- NBench 中 JIT 引擎占比 **< 0.01%**（可忽略）
- LuaJIT (SciMark) 中占约 **1%**

**动态库加速**：
- bzip2 使用原生 `libbz2.so.1`：性能提升 **1.38×**
- NBench 使用原生 `libm.so.6`：仅为模拟版本的 **0.8×**（RISC-V 数学库实现质量+转发层开销）

### 8.4 应用兼容性与稳健性

**桌面游戏测试**（SG2044，1080p，最高画质，MangoHud 统计 1 小时游玩）：

| 类别 | 游戏 | 平台 | 平均 FPS | 1% Low | 97% High |
|------|------|------|---------|--------|---------|
| **3D** | The Witcher 3: Wild Hunt | Win 64-bit | 18 | 7 | 23 |
| | FlatOut 2 | Win 32-bit | **60** | 44 | 67 |
| | Besiege | Linux 64-bit | 14 | 1 | 29 |
| | OpenArena | Linux 64-bit | **47** | - | - |
| **2D** | Hades | Win 64-bit | 52 | 11 | 77 |
| | 东方红魔乡 | Win 32-bit | 59 | 27 | 63 |
| | Celeste | Linux 64-bit | 59 | 33 | 60 |
| | LIMBO | Linux 32-bit | 57 | 44 | 60 |

**关键发现**：
- AAA 大作《巫师3》可达 15-20 FPS 的可玩帧率
- 老游戏《FlatOut 2》稳定 60 FPS（垂直同步上限）
- OpenArena 模拟版达到原生 RISC-V 版本的 **67%**
- Celeste 使用 Mono .NET 运行时（频繁 JIT 编译），验证了 SMC 处理的正确性
- 成功运行了未修改的闭源 x86 游戏，展示的兼容性在此前 RISC-V DBT 方案中未曾实现

---

## 9. 结论

本文呈现了 Box64 的 RISC-V 实现——一个高性能、高兼容性的动态二进制翻译器。三大贡献：

1. **高级 JIT 引擎**：融合分支优化、惰性 FLAGS 求值、宏操作融合、ABI 感知寄存器映射
2. **统一兼容框架**：弥合 x86 与碎片化 RISC-V 硬件之间的架构差距
3. **务实 TSO 模拟**：确保真实多线程应用正确性且开销最小

评估表明，Box64 使在 RISC-V 上运行未修改的复杂 x86 桌面软件成为现实。TSO 模拟原则可指导未来其他弱序架构的翻译器设计。

---

## 10. 致谢

感谢匿名审稿人及 shepherd Diogo Behrens 的宝贵意见。本研究部分受国家自然科学基金资助。

---

## 附录：核心架构概念速查

### 关键术语

| 术语 | 全称 | 解释 |
|------|------|------|
| **DBT** | Dynamic Binary Translation | 动态二进制翻译，运行时逐块翻译执行 |
| **SBT** | Static Binary Translation | 静态二进制翻译，执行前提前翻译 |
| **JIT** | Just-in-Time Compilation | 即时编译，运行时动态编译 |
| **TSO** | Total Store Ordering | 全序存储，x86 的强内存一致性模型 |
| **WMM** | Weak Memory Model | 弱内存模型，RISC-V/Arm 使用的模型 |
| **SMC** | Self-Modifying Code | 自修改代码，运行时修改自身指令 |
| **RAS** | Return Address Stack | 返回地址栈，CPU 分支预测硬件结构 |
| **RVV** | RISC-V Vector Extension | RISC-V 向量扩展，用于 SIMD 操作 |
| **PLT** | Procedure Linkage Table | 过程链接表，动态链接的关键机制 |
| **FPU** | Floating-Point Unit | 浮点单元 |
| **ISA** | Instruction Set Architecture | 指令集架构 |

### 核心优化技术一览

1. **CALL/RET RAS 优化**：利用硬件返回地址栈加速函数返回
2. **惰性 FLAGS 求值**：只在需要时计算标志位
3. **宏操作融合**：将 CMP+Jcc 融合为单条原生条件分支
4. **ABI 感知寄存器映射**：对齐 x86 和 RISC-V 调用约定
5. **动态库加速**：绕过翻译直接将库调用转发至原生实现
6. **5 级 TSO 模拟**：可配置的内存屏障策略
7. **SMC 两阶段处理**：内存保护 + 按需重编译
8. **非对齐原子自适应**：硬件异常触发降级方案

---

> 📝 **精读建议**
> 
> 1. **重点章节**: Section 5（JIT 引擎）和 Section 7（TSO 模拟）是最具技术深度的部分
> 2. **关键图**: Figure 1（ISA 对比）和 Figure 3（宏操作融合示例）值得细看
> 3. **工程价值**: Section 6 的兼容性设计展示了在实际系统中"务实"比"完美"更重要
> 4. **评估亮点**: 8.4 节的游戏兼容性测试是全文最佳说服力来源
> 5. **局限性**: 作者坦承 TSO 方案无理论正确性保证，这是一个重要的研究诚实性体现
