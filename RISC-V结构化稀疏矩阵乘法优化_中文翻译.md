# 在 RISC-V 向量处理器中优化结构化稀疏矩阵乘法

> **原文标题**：Optimizing Structured-Sparse Matrix Multiplication in RISC-V Vector Processors  
> **发表**：IEEE Transactions on Computers (CCF-A)  
> **作者**：Vasileios Titopoulos, Kosmas Alexandridis, Christodoulos Peltekis, Chrysostomos Nicopoulos, Giorgos Dimitrakopoulos  
> **机构**：Democritus University of Thrace (希腊) / University of Cyprus (塞浦路斯)  
> **arXiv**：2501.10189

---

## 摘要

结构化稀疏已被提出作为一种高效的方法，用于剪枝机器学习（ML）应用的复杂度并简化硬件中稀疏数据的处理。无论是训练还是推理，加速 ML 模型都高度依赖矩阵乘法，这些乘法可以高效地在向量处理器或定制矩阵引擎上执行。本工作旨在将结构化稀疏的简洁性融入向量执行，以加速相应的矩阵乘法。首先，我们全面探索了使用当前 RISC-V 指令集向量扩展实现结构化稀疏矩阵乘法的方法。对于影响性能的关键参数——如数据在标量寄存器和向量寄存器文件之间的分布、数据局部性以及循环展开的有效性——我们进行了定性和定量的分析。此外，我们证明了添加一条新的指令可以获得更高的性能。这条新提出的指令称为 **vindexmac**（即向量索引-乘-累加）。它允许从向量寄存器文件中进行间接读取，减少了每次矩阵乘法迭代执行的指令数量，且不会引入限制循环展开的额外依赖。该新指令以可忽略的硬件成本集成到了一个解耦的 RISC-V 向量处理器中。实验结果表明，所引入的优化和新指令在执行最先进的卷积神经网络时具有运行时效率和可扩展性。具体而言，与仅使用当前定义的 RISC-V 指令的高度优化向量化内核相比，加入自定义指令后，运行时分别提升了 25% 和 33%。

**关键词**：结构化稀疏、矩阵乘法、向量处理器、自定义指令、机器学习加速器

---

## I. 引言

机器学习应用在各个领域取得了非凡的成功。为减少内存存储和计算成本，ML 模型的权重通常被剪枝，从而产生稀疏模型 [1]。这意味着零权重不被存储，相应的计算被跳过。虽然稀疏性提供了高性能和低存储开销，但在某些情况下，它可能导致 ML 模型精度下降 [1], [2]。在这种情况下，可以通过重新训练模型来适应特定权重的移除，从而减轻精度损失，这一点已经在 CNN [3], [4] 和 Transformer [5], [6] 上得到了验证。

所达到的稀疏性可以是**非结构化**的 [7]，也可以是**结构化**的 [3], [4]。在非结构化稀疏中，零元素的位置没有任何约束，如图 1(a) 所示。在这种情况下，除了非零元素外，还需要多个索引来标识每个非零元素的原始位置。

相反，在结构化稀疏中，一个连续元素块内可能存在的非零元素数量有上限。例如，在图 1(b) 中，每一行中每 4 个元素中最多有 2 个非零元素。在大多数实际应用中 [4], [8]，块很小，通常支持 1:2、1:4 或 2:4 的 N:M 稀疏模式，即每个包含 M 个元素的块中最多可以有 N 个非零元素。如图 1(b) 所示，对于这种基于块的结构，用于标识每个非零元素在块内位置的索引（col_idx）只需要很少的几位（因为每个块的尺寸 M 很小）。最近的研究表明，非结构化稀疏矩阵可以通过行（列）重排序和零填充 [9] 或矩阵分割 [10] 离线转换为结构化稀疏矩阵。GPU 上的张量单元可以加速具有结构化稀疏数据的 ML 应用 [3], [4]。我们的目标是**将这种硬件能力扩展到具有向量引擎的 CPU**，使其适用于更广泛的当前和未来应用。

非结构化稀疏已在向量处理器中得到支持。各种稀疏向量矩阵乘法算法已经被开发出来 [11], [12]，并且最近得到了扩展 [13], [14]，其目标是通过高效处理不可预测的稀疏模式来提高性能。更进一步，VIA [15] 添加了暂存存储器和新的自定义向量指令来处理非结构化稀疏数据的复杂性。

为了避免利用非结构化稀疏所带来的大量硬件开销，本工作与 [16] 类似，论证了**结构化稀疏**——它频繁出现在最先进的 CNN 应用 [4] 中——可以非常有效地在向量处理器中被利用，且硬件成本可忽略不计，从而实现高性能的稀疏×稠密矩阵乘法（A×B）。

**首先**，通过广泛的分析，我们展示了如何为结构化稀疏数据重新组织和优化最先进的行式矩阵乘法算法 [13], [17], [18]，以适用于长向量指令集架构（ISA），如 RISC-V。为了更好地适应 RISC-V ISA 向量扩展中可用的向量乘累加指令，我们提出了一种混合数据放置策略，即将非零数据及其对应的列索引分布在向量寄存器和标量寄存器文件之间，这减少了寄存器命名依赖并简化了循环展开。

**其次**，我们提出了一条新的自定义向量-索引-乘-累加指令，称为 **vindexmac**，它可以带来显著的进一步性能提升。这条指令实现了从向量寄存器文件进行低成本间接读取，消除了稀疏矩阵乘法算法中常见的不必要内存流量。所提出的向量指令以可忽略的硬件成本集成到了一个高端解耦的 RISC-V 向量处理器中。解耦向量处理器 [19]–[22] 具有独立的译码和执行流水线，与负责取指并转发相应向量指令的标量核心分离。此外，解耦向量处理器可以独立于标量核心访问内存（或缓存层次结构），这与共享内存访问路径的集成向量引擎 [23] 不同。

所呈现的评估使用 gem5 模拟器 [24], [25]，实现了连接至超标量乱序核心的解耦向量处理器的精确模型。执行的基准测试应用包括经过结构化稀疏剪枝的最先进卷积神经网络（CNN）。获得的结果表明：(a) 在符合 RISC-V 标准的向量化结构化稀疏矩阵乘法内核中，优化寄存器文件间的数据放置可以实现的显著加速；(b) 当向量 ISA 扩展了新的 vindexmac 自定义乘-索引-累加向量指令后，可以获得的额外加速。

额外的实验表明，随着硬件向量长度的增加，或者将 CNN 层的执行分割到多核设置中的多个向量处理器上，性能可以实现良好扩展。然而，在后一种情况下，超过 8 核后，由于内存带宽的限制，提升趋于饱和。

**本工作的贡献总结如下：**

- **混合数据放置**：将结构化稀疏数据混合放置在向量和标量寄存器文件之间，减少寄存器命名依赖，并允许对向量处理器中的稀疏矩阵乘法进行循环展开优化。所呈现的设计空间探索确定了在 RISC-V ISA 当前定义下哪种配置能实现最低的执行时间。
- **新的向量索引-乘-累加指令**：实现了对从向量寄存器文件中直接和间接读取的向量进行乘法和累加操作。该指令可以以可忽略的成本无缝集成到向量处理器中。其使用促进了向量寄存器文件中的数据局部性，并减少了每次迭代的指令数量。
- **显著的性能提升**：所提出的优化和自定义指令显著改善了最先进卷积神经网络的运行时效率和可扩展性。具体而言，自定义指令相比仅使用现有 RISC-V 指令的高度优化向量化内核，实现了 25%–33% 的运行时改进。

本文其余部分组织如下：第 II 节介绍了使用结构化稀疏数据的向量化稀疏矩阵乘法的最先进算法的公式化表达及其优化变体。第 III 节介绍了所提出的向量索引-乘-累加指令及其在结构化稀疏矩阵乘法内核中的使用。实验结果在第 IV 节中呈现，结论在第 VI 节中得出。

---

## II. 向量稀疏×稠密矩阵乘法的设计空间探索

具有稀疏数据的向量化矩阵乘法可以用多种方法实现 [11], [13], [14]。行式方法 [17], [26], [27]（也称为 Gustavson 算法 [28]）已被证明在计算矩阵乘积 A×B 方面非常高效，并且更适合目标结构化稀疏场景。其他方法 [11], [13], [14] 针对极高稀疏度，在中等结构化稀疏条件下效果不佳。

假设矩阵 **A** 是结构化稀疏的，矩阵 **B** 是稠密的。在算法上，矩阵 A 单行中的所有非零元素应与矩阵 B 的对应行并行相乘，其中矩阵 B 的行索引由矩阵 A 中非零值的列索引确定。乘法结果是逐行产生的，如下所示：

> **C[i,:] = Σₖ A[i,k] · B[k,:]**　　　　(1)

图 2 展示了结果矩阵 C 的第 0 行是如何产生的。行式矩阵乘法可以很容易地被向量化，因为 A 的每个元素都与矩阵 B 的一行中的**所有**元素相乘。这为结果矩阵 C 中的每一行生成一组部分结果向量。

### A. 探索向量行式矩阵乘法中的数据放置

行式矩阵乘法的向量化版本可以采用三种等价形式，但具有不同的性能特征。

**方法一（算法 1）**：假设矩阵 A 和 B 的元素完全放置在向量寄存器文件（VRF）中。算法 1 为清晰起见忽略了矩阵 A 中的任何稀疏性，并假设向量长度任意大。在第 3 行，矩阵 A 第 i 行的所有元素被加载为一个向量到 VRF 中。类似地，矩阵 B 对应行的所有元素在第 6 行也被加载为向量。A 加载行的第一个元素与矩阵 B 整行之间的向量乘法在第 8 行执行，同时累加部分结果。按照 RISC-V 向量 ISA 格式，第 8 行的乘加操作在向量寄存器和来自标量寄存器（第 7 行）的标量值上执行。A 的行在第 9 行向右移动一个元素，以便对该行其余元素重复上述过程。整行结果矩阵 C 的最终值在第 11 行存回内存。重复此过程直到矩阵 C 的所有行都被产生出来。

然而，算法 1 中矩阵 A 元素在 VRF 中的放置具有许多 VRF 和标量 RF 之间不必要的数据传输（第 7 行）。为了解决这种低效问题，我们探索了两种替代方法：

**(a)** 将矩阵 B 的元素保留在 VRF 中，将矩阵 A 的元素**完全放置在标量 RF** 中（算法 2）。具体来说，不是将矩阵 A 的行加载到 VRF 中，而是将 A 行中的每个元素逐个通过标量 RF 加载和使用。因此，第 7 行的乘加操作直接使用放置在标量寄存器 'S0' 中的数据，从而消除了向量到标量的寄存器移动和向量滑动。注意，元素的数据类型决定了将存储非零值的寄存器文件类型：如果算法 1 和算法 2 中的非零值是 'float' 类型，它们可以存储到浮点寄存器文件中，从而减轻标量寄存器文件的总体压力。

**(b)** 将矩阵 A 保留在 VRF 中并利用更多的向量寄存器（算法 3）。第 3 行显示矩阵 A 的一行被加载到 VRF 中。我们需要逐个访问这些元素并将它们与对应的 B 行相乘。这通过使用 **vrgather.vx** 指令来实现。该指令将 'v0' 的第 j 个元素复制到向量寄存器 'v2' 的每个元素中。然后后者被用作乘累加操作的操作数。使用 vrgather.vx 指令使得乘累加操作（第 8 行）能够用 3 个向量操作数执行，而算法 2 中使用的是 1 个标量和 2 个向量操作数。

### B. 面向结构化稀疏数据的向量行式矩阵乘法

算法 1–3 可以被重构以支持结构化稀疏矩阵 A。在这种情况下，只存储 A 每行的非零元素以及它们对应的索引，即它们在矩阵 A 行中大小为 M 的块内的原始位置。算法 1–3 的重构分别显示为算法 1-S、2-S 和 3-S，其中所有必要的变化/添加以颜色高亮显示。

以算法 3-S 为例来强调新增的变化。在这种情况下，矩阵 A 的非零值被加载到 VRF 中，如算法 3-S 的第 3 行所示。相反，列索引存储在标量 RF 的标量寄存器中（如算法第 7 行），因为它们需要在使用前进行一些预处理。预处理步骤源于结构化稀疏格式本身，它限制了列索引的范围。具体地，列索引指的是非零元素在大小为 M 的块内的位置。因此，所有列索引的范围为 [0, M−1]，尽管矩阵 A 的每一行通常由多个大小为 M 的块组成。

因此，虽然算法 3 中的列索引 'j' 跨越矩阵 A 的整行（即其所有列），但同一索引在算法 3-S 中具有不同的含义。现在列索引 'j' 只跨越矩阵 A 特定行中非零元素的数量，如算法 3-S 第 5 行所示。在这种形式下，索引不能直接用于加载矩阵 B 的对应行。相反，必须先检索指向矩阵 A 整行中特定列的实际列索引。为了计算实际列索引，我们必须识别对应非零元素所在的特定块。这在算法的第 6 行执行，其中块索引通过非零元素位置 'j' 与每个大小为 M 的块中最大允许非零元素数量 N 的**整数除法**来计算。一旦块 ID 被识别出来，就将其乘以每个块的大小 M 并加上从内存加载的原始列索引，如第 8 行所示。此操作完全检索出跨越矩阵 A 整行的实际列索引。当 N 和 M 是 2 的倍数时，乘法和除法分别通过左移和右移来执行。

图 3 展示了一个如何检索实际列索引的示例，假设使用 3:4（N:M）结构化块稀疏。该示例聚焦于元素 'e'。首先，通过对 'e' 在非零值数组中的位置（即位置 4）与每个大小为 M 的块中允许的非零元素数量 N=3 进行整数除法，识别出元素 'e' 所在的块（块 ID）。接下来，将块 ID 乘以每个块的大小 M=4 来计算实际列索引。最后，将该值与存储在内存中的元素 'e' 的列索引（即值 1）相加，以检索出在压缩前元素 'e' 在 A 行中所在的**实际列**。

为避免此类索引计算，可以在行级别存储全宽列索引。然而，如 [4] 和第 IV-B 节所示，对于 1:4 或 2:4 的目标稀疏水平，这种方法会显著增加存储开销。

类似地，在稀疏-稠密乘法（本工作的重点）的上下文中，低到中等水平的稀疏性使得使用索引加载指令 [14] 收集 A 的非零值不太可取。这种方法会引入额外的运行时开销，因为索引向量加载/存储指令可能从少量向量指令中产生大量缓存缺失。

继续算法 3-S，在第 8 行检索的实际列索引用于在第 9 行加载矩阵 B 的对应行到 VRF 中。在第 10 行使用 **vrgather** 指令将当前处理的非零元素复制到向量 'v2' 的所有向量寄存器元素中。注意，只有与 A 的非零元素列索引对应的 B 行参与乘累加操作（第 11 行）。内层循环的每次迭代移动到 A 当前行中的下一个非零元素，直到到达行尾。

算法 1-S 和 2-S 采用类似的方法来检索 A 的非零值及其对应的列索引。第 IV-B 节突出了算法 1-S、2-S 和 3-S 的相对性能，重点关注优化的展开实现。以下部分展示了如何为算法 3-S（它从这种优化中获益最大）优化循环展开。

### C. 行式向量稀疏×稠密矩阵乘法的循环展开

遵循在其他基于向量的并行矩阵乘法内核中探索的实践 [29], [30]，并为了增加指令级并行性的范围，算法 3-S 的两个循环（外层和内层）都可以被展开。算法 4 示范性地将两个循环展开 2 倍。

外层循环的展开允许处理器**同时处理矩阵 A 的两个连续行**，即同时产生输出矩阵 C 的两个连续行。类似地，内层循环的展开允许处理器**同时处理矩阵 A 的两个连续行中的两个连续非零元素**。

算法 4 的第 8 至 18 行涉及使用矩阵 A 连续行 'i' 和 'i+1' 的非零元素 'j' 执行的操作。可以看出，由于循环展开，算法 3-S 内层循环中的每条指令（除了计算块 ID 的那一条）都在此处被复制。第 17 和 18 行的两条乘累加指令为输出矩阵 C 的行 'i' 和 'i+1' 生成两组部分结果向量。类似地，第 19 至 29 行为矩阵 A 相同两行中的**下一个**非零元素重复相同的模式；即连续行 'i' 和 'i+1' 的非零元素 'j+1'。

由于每个循环被展开 2 倍，人们可能预期对标量和向量寄存器的最终影响是四倍的。然而，通过**交错**两个循环，这一成本得以避免，交错允许**重用**相同的标量和向量寄存器并减少写后读依赖。如算法 4 所示，来自不同循环迭代的指令以交错方式执行。属于同一循环的指令以相同颜色表示。在所有情况下，同一循环中没有两条指令是先后连续执行的。

交错允许第 8 至 18 行使用的完全相同的标量和向量寄存器在第 19 至 29 行中被重用。这种方法最小化了正在使用的寄存器数量，同时保持了非依赖指令固有的物理并行性。此外，交错允许不同的循环迭代共享列索引计算。这有效地减少了展开后的指令数量。

总体而言，算法 3-S 需要 1 个标量和 4 个向量寄存器，而循环展开的算法 4 需要 2 个标量和 8 个向量寄存器。一般来说，(a) 寄存器文件压力的增加，以及 (b) 代码膨胀（可能对缓存性能产生不利影响）是循环展开的两个众所周知的副作用。因此，循环展开应**谨慎应用**，直到展开的副作用开始导致收获的性能提升出现边际递减为止。

---

## III. 使用自定义指令加速稀疏-稠密矩阵乘法

稀疏-稠密乘法的实现由于 A 的结构化稀疏格式而消除了不必要的乘法。然而，计算的一个关键瓶颈是对于矩阵 B 的行从内存进行大量向量加载（算法 3-S 第 9 行）。为了解决这个问题，我们利用矩阵 A 的结构化稀疏性来减少内存流量，并允许计算使用已经驻留在向量寄存器文件中的本地数据。

所提出的优化结合了：(a) 将矩阵 B 的行以**分块**方式加载到向量寄存器文件中；以及 (b) 一条**自定义索引-乘-累加指令**，用向量寄存器文件的间接读取取代矩阵 B 的向量加载。

能够将矩阵 B 以**多行组**（而非逐行方式）加载到寄存器文件中的关键属性是矩阵 A 格式中定义良好的、规则的结构。由于 A 的稀疏性在构造上就是**结构化**的，该矩阵中的块具有恒定且已知的大小。反过来，这意味着 A 中非零元素值的列索引被块大小 M 所**"界定"**，即所有 col_idx 值都在范围 [0, M−1] 内。回忆一下，块大小 M 是 A 行中可以包含特定数量（N）非零元素的连续元素的数量。利用结构化稀疏的这一特性，我们可以预先加载尽可能多的矩阵 B 行，只要向量寄存器文件能够容纳（有一些限制，如第 III-B 节所述），并确保矩阵 A 中的列索引只会指向这些本地行。相反，对于非结构化稀疏，列索引本质上是**"无界"**的，可能指向 B 的任何行（从而使特定 B 行在寄存器文件中的预加载变得徒劳）。

预加载到向量寄存器文件中的矩阵 B 的分块大小为 **L × VectorLength**；即 L 行，每行有 VectorLength 列。行数 L 必须是 M 的倍数。

### A. 所提出的向量索引-乘-累加 vindexmac 指令

由于矩阵 B 的多行现在被加载到 VRF 中，需要索引到寄存器文件以访问特定行。这通过所提出的新指令来实现，该指令有效地执行标量-向量乘累加操作，并结合 VRF 中的索引。标量值指矩阵 A 行的一个非零元素，向量指通过提供的列索引间接读取的、预加载在 VRF 中的矩阵 B 的一行。

总体而言，新指令旨在将稀疏-稠密矩阵乘法过程从对矩阵 B 行的连续向量加载中解放出来。相反，自定义指令直接从向量寄存器文件中读取对应的预加载 B 行并对其进行操作。基于这一目标，新的 **vindexmac** 指令定义如下：

> **vindexmac.vx vd, vs2, rs**  
> **vd[i] += vs2[0] · vrf[rs[4:0]][i]**

术语 vrf 指向量寄存器文件。该指令有三个操作数：一个向量目的寄存器（vd）和两个源寄存器。一个源寄存器是向量（vs2），另一个是标量（rs），符合 RISC-V ISA 对 .vx 指令的要求。为执行 vindexmac，读取标量寄存器 rs，其内容（实际上只需要 5 个最低有效位）用作向量寄存器文件的地址。通过 rs 中包含的地址读取的向量寄存器的内容（即值）与向量寄存器 vs2 的最低有效元素相乘。vindexmac 的操作如图 4 所示。

### B. 使用 vindexmac 的稀疏-稠密矩阵乘法

算法 5 展示了新 vindexmac 指令对一个 B 分块的使用，其中矩阵 A 单行的非零值存储在一个向量寄存器中。算法 5 与先前算法之间的本质区别以高亮显示。如第 2 和 4 行所示，矩阵 B 的一个分块被预加载到 VRF 中。在第 6 行，矩阵 A 一行的非零值被加载到 VRF 中，而对应的列索引保留在标量 RF 中。新 vindexmac 指令的使用在第 12 行展示。最终结果是**消除了对矩阵 B 行的连续向量加载**，这些加载在算法 3-S 的第 9 行发生。由于 vindexmac 只使用向量寄存器 vs2 的元素 0，将 vs2 中的向量滑动一个元素位置就足以继续处理矩阵 A 当前行的下一个非零元素（算法 5 第 13 行）。

预加载到 VRF 中并由 vindexmac 指令索引的矩阵 B 分块的行数 L 受可用向量寄存器数量的限制。在实践中，我们认为矩阵 B 的一个分块可能消耗一半的可用向量寄存器，即 **L=16**。由于矩阵乘法的对称性，B 的 L 行对应 A 的一组 L 列。由于矩阵 A 遵循 N:M 结构化稀疏，这意味着在这 L 列中只能找到 L·(N/M) 个非零元素。实际上，这一属性导致算法 5 外层循环（第 5–16 行）的迭代次数大幅增加，因为内层循环（第 8–14 行）只对少量非零元素执行。

为了改进利用率，并利用矩阵 A 一行（算法 5 第 6 行）的向量加载可以一次将 VectorLength 个元素加载到向量寄存器 vs1 中的事实，我们需要考虑更多的 B 分块。这种方法导致了算法 5 的重构，如**算法 6** 所示。插入一个中间循环（使用索引 m），负责获取并操作与 A 的完整一行非零元素对应的 B 分块。算法 6 第 3 行加载的矩阵 A 一行的非零元素通过**分阶段**重新加载对应的 B 行来全部处理。具体来说，在每个阶段中，矩阵 B 的适当分块在第 6–8 行加载。

所需重新加载的阶段数 m 由以下因素决定：(a) 可用于加载 B 分块的向量寄存器 L；(b) 稀疏模式 N:M；以及 (c) 处理器的最大向量长度 VL。如前所述，在 A 的每 L 列中，我们可以找到 L(N/M) 个非零元素。因此，我们需要取 m 组 A 的 L 列来达到可出现在一个向量寄存器中的 VectorLength（VL）个非零元素，即：

> **m · L·(N/M) = VL　　　　→　　　　m = (M/N) · (VL/L)**　　　　(2)

此限制用于算法 6 第 5 行中 m 的边界。

对于 1:4 结构化稀疏的情况，并假设我们将向量寄存器文件的一半分配给 B 分块（即 L=16），则 m = (4/1)(VL/16) = VL/4。需要指出的是，当 m 的最大值为 1 时，算法 5 优于算法 6。当 m=1 时，不需要交换加载在向量寄存器文件中的 B 分块。相反，算法 5 在构造上将避免那些涉及矩阵 B 相同行的连续加载。

为了进一步提高性能，使用新提出的 vindexmac 指令的稀疏×稠密矩阵乘法内核（如算法 6 所示）可以在**三个维度**上展开：(a) 矩阵 A 同一行的元素（内层循环）；(b) 矩阵 B 的分块（中间循环）；(c) 跨不同输出行（外层循环）。预加载 B 分块的方法利用了 VRF 的相当大部分，因此限制了循环展开的深度。为了优化应用带有 vindexmac 指令的展开稀疏-稠密矩阵乘法，需要一种**平衡的方法**，结合展开和 B 的适当分块来高效利用 RISC-V 架构的 32 个向量寄存器。

### C. 面向更大矩阵 A 和 B 的分块执行

算法 6 可以处理任意行数的矩阵 A。然而，它假设每个步骤加载到向量寄存器中的 A 的非零元素最多对应 m×L 列。这就是每个轮次加载 m 个各含 L 行的 B 分块的原因。此外，算法 6 假设 B 的所有列可以适配到一个向量寄存器中。

在一般情况下，当 A 的列（以及因此 B 的行）超过 m×L，且 B 的列超过 VL 时，我们需要使用图 5 所示的数据流重复应用算法 6。对于 B 的 VL 列的一个**垂直段**，我们按 m×L 列的组处理 A 的所有行。特别是，我们首先处理属于**同一 m×L 列的垂直段**的 A 的所有行，然后再移到下一个段。在 A 的每个垂直段内，我们遵循算法 6 的执行顺序，保持 m×L 行的一组 B 固定。一旦 A 的所有垂直段处理完毕，我们通过获取下一个 VL 列并对 B 的后续垂直段重复该过程。

这种分块数据流也可以用于在多核环境中执行稀疏-稠密矩阵乘法。对 B 的每个包含 VL 列的垂直段的计算可以分配给不同的核心。这种划分允许每个核心在输出矩阵的独立段上操作，不会发生冲突。然而，在这种数据流下，结构化稀疏矩阵 A 的元素被所有核心并行访问。在此数据流下使用 vindexmac 在多核设置中实现稀疏-稠密矩阵乘法的好处在第 IV-E 节中量化。

### D. 支持 vindexmac 执行的硬件

RISC-V ISA 中的向量寄存器文件在支持高效向量处理方面起着重要作用。它由 32 个向量寄存器组成，每个都能存储大量数据。每个寄存器的长度是由处理器设计者决定的实现定义常量参数。为支持所有可用的向量指令，包括三操作数指令（如 vmacc、vmadd 等），向量寄存器文件需要具有**三个读端口和一个写端口**。

为执行 vindexmac 指令，我们需要访问标量和向量寄存器文件。这是所有后缀中带有字母 'x' 的 RISC-V 标量-向量指令的固有属性，例如 .vx、.vxm、.wx 等。

在本工作中，我们针对遵循所谓**解耦架构**的向量处理器，该架构由一个负责指令取指和执行编排的标量核心，以及一个执行从标量核心接收的向量操作的向量引擎组成 [19]–[22], [31], [32]。由于在任何给定时间，可能有许多需要标量寄存器值的向量指令，标量核心负责将这些值与向量指令本身一起传输到向量处理器。因此，**vindexmac 寻址向量寄存器文件所需的标量寄存器 rs 的值已经由标量核心提供**。给定的地址驱动向量寄存器文件的一个读端口，该端口输出所请求的向量操作数。另一个读端口用于读取 vs2 的元素，第三个读端口读取 vd，如 RISC-V ISA 中已经存在的所有乘累加标量-向量指令所要求的那样。

因此，新 vindexmac 指令的**唯一硬件要求**是在向量寄存器文件一个读端口的地址总线上**增加一个多路选择器**，该选择器在用于正常向量算术操作的 vs1 和 rs 的 5 个最低有效位（vindexmac 所要求）之间进行选择。换句话说，通过**重用现有标量-向量乘加指令的硬件基础设施**，新指令**不需要**向量寄存器文件中的额外读端口。相反，它只需要在现有读端口前面增加一个 5 位 2-to-1 多路选择器。

具体来说，一个 32 寄存器 VRF，每个寄存器宽 512 位（32 位字的 VL=16），具有 3 读 1 写端口，在 28nm 技术节点下使用 Cadence Genus 综合以 1 GHz 时钟周期为目标，需要 **56,527 µm²** 的硬件面积。容纳地址多路选择逻辑所需的额外面积仅为 **4 µm²**。

类似地，在完全集成的标量-向量架构 [33], [34] 中，vindexmac 指令将以与完全集成标量-向量设置中任何其他 .vx 指令完全相同的方式实现。唯一的区别是提供的标量值将用于驱动向量寄存器文件的一个读端口，而不是直接参与计算。

作为一条 .vx 指令，vindexmac 遵循 RISC-V ISA 为标量-向量指令规定的标准编码 [35]。然而，在指令功能方面，与典型的标量-向量操作有轻微偏差。向量寄存器标识符 vs2 仅用于其最低有效元素 vs2[0]，即它扮演标量值的角色。实际向量是通过使用 rs 的 5 个最低有效位作为向量寄存器文件中的地址进行间接读取来获取的。

---

## IV. 实验评估

本节呈现的实验评估有两个目标。**第一个**是确定使用仅 RISC-V ISA 当前定义（即仅使用现有指令）的引入的行式公式化稀疏-稠密矩阵乘法的性能极限。评估包括在配备解耦向量单元的超标量乱序处理器上，在各种循环展开场景下执行三个剪枝的最先进 CNN——Resnet50 [36]、DenseNet121 [37] 和 InceptionV3 [38]。所有三个 CNN 中的所有计算均以 fp32 数据格式执行。剪枝后的 CNN 采用 1:4 和 2:4 的结构化块稀疏。注意，如 [3] 和 [4] 所示，所选的结构化稀疏模式不会改变特定 CNN 的分类精度，与它们完全稠密的对应版本相比。

在确定使用当前 RISC-V ISA 提供最大加速的配置后，将获得的性能与采用所提出的新 vindexmac 指令的结构化稀疏矩阵乘法内核所达到的性能进行比较。对于后一种情况，也探索了各种替代方案，并且实现最低执行时间的方案被用于比较。

### A. 实验设置

对于所有实验，我们使用一个连接到乱序超标量处理器（即 [32] 中的模型 1bDV）的**完全实现的解耦向量单元**。选择高端超标量核心（连接到向量引擎）旨在确保处理器的非向量侧不会扭曲向量引擎的性能结果。换句话说，超标量处理器有助于通过确保非向量侧没有**"瓶颈"**来"隔离"向量单元的性能。因此，报告的结果纯粹突出了新向量框架的功效，没有标量侧的任何间接影响/噪音。

该设计在 gem5 模拟器 [24], [25] 中建模，模拟处理器设置的关键设计参数总结在表 I 中。新的 vindexmac 指令被纳入 RISC-V GNU 工具链，其操作在 gem5 的解耦向量引擎中实现。

**模拟处理器配置（表 I）：**
- **标量核心**：RISC-V ISA (RV64GC)，8 发射乱序，16 条目 LSQ，90 个物理整数和 90 个物理浮点寄存器，60 条目 ROB
- **L1I 缓存**：1 周期命中延迟，4 路组相联，64KB
- **L1D 缓存**：2 周期命中延迟，4 路组相联，64KB
- **向量引擎**：512 位向量引擎，16 通道配置（32 位元素 × 16 执行通道）；向量引擎通过 16 个存储队列和 16 个加载队列直接连接到 L2 缓存
- **L2 缓存**：8 路组相联，8 体；8 周期命中延迟，512KB，由大核心和向量引擎共享；64B 缓存行
- **主内存**：DDR4-2400，19.2 GB/s 内存带宽

所检查 CNN 每一层的卷积被映射到稀疏-稠密矩阵乘法 A×B [4]。矩阵 A 包括结构化稀疏权重，矩阵 B 包括对应 CNN 层的输入特征。输入特征被认为是稠密的，因为没有明确的统计属性可以被利用。即使部分输入特征包含由对应层中 ReLU 激活函数生成的零值，其数量对实际输入值和滤波器权重高度敏感。

### B. 使用仅当前 RISC-V ISA 指令识别峰值性能

我们的初始目标是检查算法 1S–3S 中突出的哪种方法（仅使用当前 RISC-V ISA 定义）在各种稀疏-稠密矩阵乘法中提供最佳的执行时间扩展，并也将其中最佳者与其他最先进方法进行比较。

如第 II-C 节所讨论的，所有算法的内层和外层循环都可以被展开以提高性能。外层循环展开对应于并行处理稀疏矩阵 A 的多行，而内层循环展开使 A 同一行的多个非零元素能够以平铺形式同时处理。

为了突出展开如何改善运行时，我们使用来自 DenseNet121 CNN [37] 的三个代表性层进行了若干实验，假设 1:4 稀疏。

第 II-C 节中呈现的优化展开的效果首先针对算法 3-S 进行检查。每种情况下相对于算法 3-S 未展开版本的加速比如图 6 所示。每个柱子对应一个不同的内层循环展开因子，而外层循环展开因子由沿 x 轴的不同柱子组表示。例如，第 II-C 节算法 4 中展示的展开示例对应于内层和外层循环都使用 2 倍展开因子的情况。

如图 6 所示，增加展开在所有情况下都改善了性能，相对于基线实现达到了**超过 3 倍**的加速。

当内层循环可以并行处理 16 个非零元素且外层循环展开 8 倍时，加速最大化。这些展开因子直接与硬件关联；即因子受向量单元"大小"的约束。内层循环展开因子 16 对应于表 I 中显示的硬件 VectorLength。类似地，外层循环的 8 倍展开因子受向量寄存器总数的限制。

这一显著增益是以下因素的综合结果：(a) 行式矩阵乘法算法的结构；(b) 跨向量和标量寄存器文件的混合数据放置；以及 (c) 第 II-C 节所示交错循环展开的效率，它减少了不必要的命名依赖并允许共享索引计算。

展开内层循环操作交错的优势被单独量化并与 Clang [39] 可以自动执行的**传统展开**进行比较。针对相同 CNN 层和相同内外层循环展开因子的结果如图 7 所示。在所有情况下，本工作中呈现的交错实现了**50% 更高**的性能。

算法 1-S、2-S 和 3-S 都可以同样受益于带交错的展开。图 8 说明了当内层循环处理 16 个非零元素并行且外层循环展开 8 倍时，相同 CNN 层每个算法的执行时间。**算法 1-S** 表现最低，因为每次操作在向量和标量寄存器之间传输数据的开销。相比之下，**算法 2-S 和 3-S**（它们在标量和向量寄存器之间采用混合索引和数据放置）实现了更好的性能。算法 2-S 使用标量寄存器存储矩阵 A 的非零值和列索引，增加了标量寄存器维护这些本地数据的需求。相反，算法 3-S 将 A 的非零元素存储在向量寄存器中。这种方法消除了算法 2-S 所要求的为 A 的每个非零元素使用独立标量加载指令的需要。这一变化是算法 3-S 相对于算法 2-S 略有优越性的原因。

为了识别一个最先进的稀疏-稠密基线来与 vindexmac 的实现进行比较，我们首先将目前为 Alg-3S 识别的最佳配置与各种其他替代方案进行比较：

- **Alg-3S-FC（全列）**：通过利用全列索引来增强 Alg-3S，以避免结构化稀疏块要求的生成列本身的额外开销。这种方法实际上等同于 CSR 实现，并增加了每行元素数量预定的简化（如同结构化稀疏）。这些更宽的列索引预计将改善运行时，但会增加存储需求。
- **Alg-3S-FC-FI（全列+快速索引）**：通过纳入 [16] 中提出的将地址生成阶段集成/融合到标量和向量加载指令中的自定义指令，进一步增强了 Alg-3S-FC。这种在硬件中加速索引计算的方法与本工作中呈现的所有方法正交，并且可以同样应用于 vindexmac 扩展。
- **SPA**：SPA 是处理当两个数组 A 和 B 都明显稀疏时的稀疏矩阵乘法的最广泛算法 [11], [13], [14]。SPA 为完整性而被纳入，即使其主要目标是具有高度稀疏性的稀疏-稀疏矩阵乘法。

如图 9 所示，Alg-3S-FC 和 Alg-3S-FC-FI 都产生了性能改进，尽管相对于 Alg-3S 并不显著。这主要是因为 Alg-3S 要求的索引算术——由结构化稀疏格式的紧凑列索引所必需——可以被**共享**。结合循环展开下减少的命名依赖，在乱序（OoO）超标量核心上执行时这仅对性能有轻微影响。然而，**更重要的是**，使用全列（如 Alg-3S-FC 和 Alg-3S-FC-FI）导致第 5、23 和 87 层稀疏矩阵的存储开销分别显著增加 14.7%、26.5% 和 23.5%，这是一个不希望看到的副作用。SPA 实现的有限性能归因于其使用向量索引内存操作来管理非零元素。这些操作对整体性能产生不利影响；它们导致大量缓存缺失，即使只有相对少量的向量指令。

鉴于 (a) 结构化稀疏的目标，(b) 更小的列索引，以及 (c) 保持完全 RISC-V ISA 合规性的要求，我们选择 **Alg-3S 作为参考基线**，在实验记录中此后称为 **'SpMM'**。更具体地说，我们将很快描述最佳设置为 **SpMM(16,8)**，指内层循环展开 16 和外层循环展开 8。

需要注意的是，如果我们选择 Alg-3S-FC-FI 作为基线（它采用全列和 [16] 提出的自定义指令），使用所提出方法的性能加速仅会略微较小（如 Alg-3S-FC-FI 相对于 Alg-3S 在图 9 中的边际性能改进所示）。与 Alg-3S-FC-FI 相比，所提出方法实现的加速将在下一小节中实验量化。

### C. 使用新 vindexmac 指令识别最佳配置

使用新 vindexmac 指令的稀疏-稠密矩阵乘法已在各种配置下进行评估，执行 DenseNet121 [37] 的相同三个层，采用 1:4 结构化稀疏。评估的所提出内核是算法 6 中所示的内核，它扩展了行式矩阵乘法，采用向量和标量寄存器文件中数据的混合放置以及操作本地数据和减少内存流量的新指令。算法 6 由三个不同的循环（内层、中间和外层）组成，可以任意展开。

为了识别最佳配置，我们选择总是完全展开内层循环，然后分别研究展开另外两个循环的影响。在所有检查的情况下，矩阵 B 的分块由 L=16 行组成，占据向量寄存器文件的一半。L 的值和稀疏模式 N:M 决定了中间循环的迭代次数和内层循环的有效展开。

每个检查配置实现的加速如图 10 所示。为了对实现的加速有一致的视角，图 6 和图 10 的结果已被归一化到相同的基础，即使用当前 RISC-V ISA 定义指令的算法 3-S 未展开版本的运行时。

从图 10 报告的加速中，我们可以观察到外层和中间循环的展开**显著改善了性能**。在所有情况下，当外层循环展开因子为 8 且中间循环展开因子为 4 时，加速最大化。外层循环展开对应于并行计算的稀疏矩阵 A 的行数。由于矩阵 B 的一个分块占据向量寄存器文件的一半，只有另一半向量寄存器文件可用于促进更高度数的循环展开。因此，基于可用向量寄存器数量，**8 倍的展开因子是最大可能的**。中间循环展开因子 4 对应于所检查配置的**完全展开**，即 VectorLength=16 元素（见表 I），L=16 且 N:M=1:4。

在其余实验结果中，我们使用所提出方法的最佳配置，如图 10 所识别：中间循环展开 4 和外层循环展开 8。我们将此设置定义为 **Proposed(8,4)**。

### D. 比较

为 SpMM（在 IV-B 节）和为所提出方法（在 IV-C 节）推导的最佳配置在此处通过执行三个完整 CNN 模型进行比较。图 11 说明了所提出方法（'Proposed(8,4)'）相对于 'SpMM(16,8)' 在三个评估的 CNN 中实现的加速，假设 1:4 和 2:4 稀疏模式。显然，'Proposed(8,4)' 的**性能在所有情况下都显著更好**。在所有三个 CNN 中，1:4 稀疏实现的平均加速为 **1.25×**，2:4 稀疏增加到 **1.33×**。正如预期，与 Alg-3S-FC-FI 相比（对于内外层循环都采用相同展开因子），所提出方法的平均加速仅略小：1:4 稀疏为 1.20×，2:4 稀疏为 1.28×。

需要强调的是，所提出的新 vindexmac 指令提供的这额外 25%–33% 改进是**额外的加速**，是在本文引入的已经高度优化的行式内核之上的。总体改进对应于超过基线的 **4 倍以上加速**，与执行稀疏-稠密矩阵乘法的基线方法相比。这一结果突出了混合数据放置（在标量和向量寄存器文件中）以及新指令实现的减少内存流量所共同提供的改进。

一般来说，使用 'Proposed(8,4)' 方法时的上述性能改进来自 (a) 每次迭代向量操作的减少，以及 (b) 消除不必要的从内存的向量加载并将其转换为从向量寄存器文件的间接读取（由 vindexmac 指令实现）。通过将矩阵 B 的分块预加载到向量寄存器文件中，所提出的方法**非常有效地利用了数据局部性**，从而降低了内存流量。

图 12 中呈现的结果量化了使用所提出的 vindexmac 指令时总内存访问的减少。呈现的结果被归一化到相应稀疏度的 'SpMM(16,8)' 观察到的内存访问数量。可以看出，总内存访问**显著减少**。对于 1:4 稀疏，内存访问平均减少 **42%**，而对于 2:4 稀疏，平均减少 **63%**。

### E. 性能扩展：增加硬件向量长度或使用多核

为了进一步探索围绕新 vindexmac 指令构建的所提出方法的可扩展性，我们使用不同的硬件配置进行实验。具体来说，我们调查了扩展向量单元可用并行性时对性能的影响。回忆一下，基本配置（如表 I 所示）使用 512b 向量寄存器在 16 个并行通道上操作，假设 32 位字。因此，在基本配置中，每个向量寄存器有 16 个向量元素，即 VectorLength=16。

除了基本硬件配置外，我们在此检查另外两种配置：256b 向量寄存器与 8 通道并行（VectorLength=8），以及 1024b 向量寄存器与 32 通道并行（VectorLength=32）。

为三个 CNN 测量的执行时间——假设相同两种稀疏模式——如图 13 所示。每种情况下的执行时间被归一化到相应稀疏度的 VectorLength=8 配置的执行时间。正如预期，当向量长度增加时，获得的加速**成比例增加**。由于更长的向量长度伴随更多的执行通道，这意味着输出矩阵 C 的更多列在任何给定时间被并行处理。反过来，这导致**显著更低**的执行时间。

如图 13 所示，在大多数情况下，相对于 VectorLength=8 实现的加速是**接近完美的**，即 VectorLength=16 时加速接近 2×，VectorLength=32 时加速接近 4×。这种期望行为得益于 L2 缓存，它在当前处理器配置中足够大，以容纳随着向量长度增加而增加的更大量数据。自然地，如果工作数据大小接近 L2 缓存容量，性能改进将会饱和。

除了硬件向量长度的扩展外，还可以通过利用多核设置提取额外的并行性。在这种环境中，每个 CNN 层将跨多个核心执行。分配给每个核心的数据对应于 B 的 VL 列的一个垂直段和整个结构化稀疏矩阵 A。这种划分在 III-C 节中展示。

每个核心由标量单元和解耦向量单元组成，如第 IV-A 节所示。所有核心共享对 DDR 主内存 [40] 的访问，通过缓存一致性互连（具体来说，ARM AMBA 一致性集线器接口 (CHI) [41]）在缓存行级别传输数据。

在多核设置中执行所检查的 CNN 时，与单核实现相比实现的加速如图 14 所示。我们测试了各种多核 CPU 大小，在每次实验中翻倍核心数量。获得的结果表明，性能随着核心数量**显著改善**。获得的加速增益持续到 8 核。超过 8 核后，**性能饱和**。这归因于内存子系统的限制。随着更多核心同时请求内存访问，内存带宽不足以处理不断升级的需求；即带宽不可避免地饱和。这种饱和导致核心之间对内存资源的争用，限制了算法的可扩展性并阻止了进一步的加速。

这种内存带宽饱和在图 15 中也表现明显，该图比较了随着核心数量增加，所提出方法（Proposed(8,4)）相对于具有未修改 ISA 的基线多核设置（SpMM(16,8)）的加速。可以看出，获得的加速在超过 8 核后开始下降，并随着核心增加到 16 及以上而趋向于 1（即性能对等）。换句话说，所提出的方法在瓶颈是计算而非通信的系统中最有效，正如具有许多核心的多核设置中的典型情况。

**关键启示**是，所提出的新 vindexmac 指令通过促进每个核心更好的内存带宽利用，允许使用具有更少核心的更节能设计。

---

## V. 相关工作

高效矩阵乘法的硬件加速——也涉及稀疏矩阵——一直是计算机体系结构社区广泛研究的焦点。稀疏矩阵乘法的优化主要旨在减少涉及零值元素的不必要操作数量。作为第一步，像压缩稀疏行（CSR）[12] 和压缩稀疏列（CSC）[14] 这样的数据存储格式有助于减少内存占用和数据移动成本。

向量处理器 [19]–[22], [31], [32] 由于其单指令多数据（SIMD）能力已被广泛用于加速稠密矩阵乘法。在稀疏数据的背景下，各种向量化矩阵乘法实现也已被提出 [11]–[14]。使用自定义向量指令也已被提出作为加速稀疏计算的手段 [16]。这些新的 RISC-V 向量数据传输指令将内存地址生成操作与标准加载操作集成/融合以减少能耗 [16]。

最近的研究主要关注专门的类似向量的硬件架构，以解决处理稀疏数据访问模式时遇到的挑战，例如在科学计算、AI 和数据分析中遇到的挑战。张量编组单元（TMU）[42] 架构利用可编程的数据流引擎来高效处理稀疏张量操作，从而将数据编组与计算分离。旨在改善稀疏计算的基于向量的硬件实现是向量索引架构（VIA）[15]。VIA 利用专门的暂存存储器通过优化内存流量和处理稀疏计算核心的索引匹配过程来提高稀疏矩阵操作效率。这种方法在稀疏矩阵-向量乘法、矩阵加法和矩阵乘法中产生了性能提升。

总的来说，这些架构代表了在向量处理器中将数据访问与计算解耦的重大进步。最终目标是减轻内存访问的低效率并增强处理稀疏数据时的并行性，稀疏数据反过来可以被分类为非结构化 [7] 或结构化 [3], [4]。与矩阵乘法的行式方法 [17], [18], [26], [27] 一起（这种方法与向量处理器的能力很好地对齐），向量处理器中的结构化稀疏为进一步探索呈现了一个有趣的领域。

在向量处理领域之外，有关于处理稀疏数据的一般挑战的广泛研究。SpChar 框架 [43] 利用决策树来表征各种稀疏内核的工作负载模式，如稀疏矩阵-向量乘法（SpMV）、稀疏通用矩阵-矩阵乘法（SpGEMM）和稀疏矩阵加法（SpADD）在 ARM 架构上的表现。这种方法提供了关于关键架构特征（如缓存大小、内存延迟和分支预测）对性能影响的洞察。Gupta 等人 [44] 突出了基于 DNN 的推荐系统的独特需求。与传统的 DNN 工作负载不同，推荐系统的特征是**不规则的内存访问模式**——由于稀疏数据——和高内存容量需求，这是使用大型嵌入表的后果。最后，基于 FPGA 的加速器 [45] 在稀疏计算中显示出前景，尽管存在潜在的带宽限制，但它们在能效和性能之间取得了平衡。

---

## VI. 结论

向量处理器可以高效处理现代 ML 应用中可用的丰富数据级并行性。ML 模型的可扩展性要求进行适当的模型剪枝，以减少其内存占用并使它们适用于边缘应用。剪枝可以使各种神经网络层受益，例如全连接层、卷积层、循环层和归一化层。此外，剪枝也可以有益于注意机制。在这种背景下，我们旨在将结构化稀疏的简洁性与增强了一条所提出新指令的主流向量架构无缝集成。

**最初**，我们重新组织和优化了最先进的行式矩阵乘法算法，用于 RISC-V ISA 当前定义的结构化稀疏数据。我们对非零数据元素及其对应列索引采用了跨向量和标量寄存器文件的**混合放置**。这种方法减少了寄存器命名依赖并简化了循环展开，从而显著改善了运行时。

**随后**，这种优化方法通过所提出的新 **vindexmac** 指令进一步增强，该指令操作**预加载**到向量寄存器文件中的本地数据。使用这条新指令减少了执行的指令总数并消除了不必要的向量加载。**最重要的是**，新指令可以以**可忽略的硬件成本**实现。评估结果展示了最先进 CNN 应用执行延迟的**显著加速**。

---

## 参考文献（共 45 篇）

[1] T. Hoefler et al., "Sparsity in deep learning," JMLR, 2021  
[2] M. Zhu et al., "Sparse tensor core," IEEE MICRO, 2019  
[3] A. Zhou et al., "Learning N:M fine-grained structured sparse neural networks from scratch," ICLR, 2021  
[4] A. Mishra et al., "Accelerating sparse deep neural networks," arXiv:2104.08378, 2021  
[5] N.-C. Huang et al., "ELSA: Exploiting layer-wise n:m sparsity for vision transformer," CVPRW, 2024  
[6] P. Jian et al., "Exposing and exploiting fine-grained block structures," NeurIPS, 2022  
[7] U. Evci et al., "Rigging the lottery," ICML, 2020  
[8] Z.-G. Liu et al., "S2TA: Exploiting structured sparsity for energy-efficient mobile CNN," HPCA, 2022  
[9] K. Shiba et al., "Polyomino: A 3d-sram-centric accelerator," IEEE TCAS-I, 2023  
[10] F. Gremse et al., "GPU-accelerated sparse matrix-matrix multiplication," SIAM JSC, 2015  
[11] J. R. Gilbert et al., "Sparse matrices in MATLAB," SIAM JMAA, 1992  
[12] N. Bell et al., "Exposing fine-grained parallelism," SIAM JSC, 2012  
[13] J. Li et al., "Generalized sparse matrix-matrix multiplication for vector engines," MCHPC, 2019  
[14] V. Le Fèvre & M. Casas, "Efficient execution of SpGEMM on long vector," HPDC, 2023  
[15] J. Pavon et al., "VIA: A smart scratchpad for vector units," HPCA, 2021  
[16] J. G. Min et al., "Energy-efficient RISC-V-based vector processor," ISLPED, 2023  
[17] N. Srivastava et al., "Matraptor: A sparse-sparse matrix multiplication accelerator," MICRO, 2020  
[18] Z. Liu et al., "Processor for sparse matrix computation," US Patent 11,392,376, 2022  
[19] M. Cavalcante et al., "Ara: A 1-GHz+ scalable RISC-V vector processor," IEEE TVLSI, 2020  
[20] K. Patsidis et al., "RISC-V2: A scalable RISC-V vector processor," ISCAS, 2020  
[21] F. Minervini et al., "Vitruvius+: an area-efficient RISC-V decoupled vector coprocessor," ACM TACO, 2023  
[22] R. Espasa et al., "Tarantula: a vector extension to the alpha architecture," ISCA, 2002  
[23] M. Stanic et al., "Towards low-power embedded vector processor," CF, 2016  
[24] N. Binkert et al., "The gem5 simulator," SIGARCH CAN, 2011  
[25] J. Lowe-Power et al., "The gem5 simulator: Version 20.0+," arXiv:2007.03152, 2020  
[26] A. Morad et al., "Efficient dense and sparse matrix multiplication on gp-simd," PATMOS, 2014  
[27] H. Takayashiki et al., "A new sparse general matrix-matrix multiplication method," SC'23 Workshops, 2023  
[28] F. G. Gustavson, "Two fast algorithms for sparse matrices," ACM TOMS, 1978  
[29] D. A. Iliescu & F. Petrogalli, "ARM scalable vector extension," ARM Whitepaper, 2017  
[30] G. Alaejos et al., "Micro-kernels for portable and efficient matrix multiplication," J. Supercomputing, 2023  
[31] C. Chen et al., "Xuantie-910," ISCA, 2020  
[32] T. Ta et al., "big.VLITTLE," MICRO, 2022  
[33] J. Rupley et al., "Samsung m3 processor," MICRO, 2019  
[34] N. Stephens et al., "The ARM scalable vector extension," MICRO, 2017  
[35] "RISC-V vector extension (Version 1.0)," github.com/riscv/riscv-v-spec  
[36] K. He et al., "Deep residual learning for image recognition," CVPR, 2016  
[37] G. Huang et al., "Densely connected convolutional networks," CVPR, 2017  
[38] C. Szegedy et al., "Rethinking the inception architecture," CVPR, 2016  
[39] C. Lattner, "LLVM and Clang: Advancing compiler technology," FOSDEM, 2011  
[40] N. Srivastava, "A tutorial on the gem5 memory model"  
[41] "AMBA 5 CHI Architecture Specification," ARM, 2018  
[42] M. Siracusa et al., "A tensor marshaling unit for sparse tensor algebra," MICRO, 2023  
[43] F. Sgherzi et al., "Spchar: Characterizing the sparse puzzle," arXiv:2304.06944, 2023  
[44] U. Gupta et al., "The Architectural Implications of Facebook's DNN-Based Personalized Recommendation," HPCA, 2020  
[45] T. Nguyen et al., "FPGA-based HPC accelerators," CCPE, 2022

---

## 作者简介

- **Vasileios Titopoulos**：色雷斯德谟克利特大学（希腊）博士研究生，研究方向为高效能向量处理器架构设计。
- **Kosmas Alexandridis**：色雷斯德谟克利特大学博士研究生，研究方向为利用机器学习技术进行集成电路验证与设计自动化。
- **Christodoulos Peltekis**：色雷斯德谟克利特大学博士研究生，研究方向为面向 ML 应用的脉动阵列架构设计及高层次综合设计流程。获 IEEE AICAS 2024 最佳学生论文奖。
- **Chrysostomos Nicopoulos**：塞浦路斯大学电子与计算机工程系副教授，研究方向为片上网络、计算机体系结构、多/众核微处理器设计。
- **Giorgos Dimitrakopoulos**：色雷斯德谟克利特大学电子与计算机工程系副教授，研究方向为数字集成电路设计、高效能数据并行加速器、功能安全架构。获 DATE 2015 和 2019 最佳论文奖、HiPEAC 2015 技术转化奖。

---

> **翻译说明**：本文由 Slum 翻译，尽可能保持了原文的学术严谨性和技术准确性。核心术语（如 vindexmac、VRF、vector length 等）保留原文，以便对照阅读。
