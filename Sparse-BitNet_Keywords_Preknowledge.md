# Sparse-BitNet 关键词 & 前置知识

> 论文: *1.58-bit LLMs are Naturally Friendly to Semi-Structured Sparsity*
> 作者: Di Zhang et al. (Microsoft Research + 北京大学 + 华南理工)
> arXiv: [2603.05168](https://arxiv.org/abs/2603.05168) (2026.03)

---

## 目录

1. [模型量化 (Quantization)](#1-模型量化-quantization)
2. [三值量化与 BitNet b1.58](#2-三值量化与-bitnet-b158)
3. [BitLinear 层](#3-bitlinear-层)
4. [模型稀疏化 (Sparsity)](#4-模型稀疏化-sparsity)
5. [半结构化 N:M 稀疏](#5-半结构化-nm-稀疏)
6. [Sparse Tensor Core](#6-sparse-tensor-core)
7. [Straight-Through Estimator (STE)](#7-straight-through-estimator-ste)
8. [Dual STE（本文核心创新）](#8-dual-ste本文核心创新)
9. [Magnitude-based Pruning（幅度剪枝）](#9-magnitude-based-pruning幅度剪枝)
10. [Dynamic Mask Recomputation](#10-dynamic-mask-recomputation)
11. [Dense-to-Sparse Training Schedule](#11-dense-to-sparse-training-schedule)
12. [量化谷结构 (Quantization-Valley)](#12-量化谷结构-quantization-valley)
13. [Perplexity (PPL)](#13-perplexity-ppl)
14. [BF16 (Brain Floating Point 16)](#14-bf16-brain-floating-point-16)
15. [Qwen-2.5 架构](#15-qwen-25-架构)
16. [Sparse-BitLinear 层（本文方法）](#16-sparse-bitlinear-层本文方法)

---

## 1. 模型量化 (Quantization)

### 是什么

将神经网络的权重和/或激活值从高精度浮点数（如 FP32、BF16）映射到低精度表示（如 INT8、INT4、甚至二值/三值）的过程。

### 为什么需要

- **减少显存占用**：FP32 一个参数 4 字节 → INT8 只需 1 字节，显存直接降为 1/4
- **加速推理**：整数运算比浮点运算快得多，且功耗更低
- **降低部署门槛**：让大模型能在手机、边缘设备上跑

### 核心公式

```
W_q = round(W / s) * s + z
```

- `s`（scale）：量化步长
- `z`（zero-point）：零点偏移
- `W_q`：量化后的整数权重

### 分类

| 类型 | 说明 | 代表方法 |
|------|------|----------|
| **训练后量化 (PTQ)** | 模型训完后再量化，不需要重训 | GPTQ, AWQ, GGUF |
| **量化感知训练 (QAT)** | 训练时就模拟量化效果 | BitNet, LLM-QAT |
| **极低比特量化** | ≤2 bit，如二值、三值 | BitNet b1.58, BiBERT |

---

## 2. 三值量化与 BitNet b1.58

### 是什么

BitNet b1.58（Ma et al., 2024/2025）将 LLM 的权重约束为 **三值集合 {−1, 0, +1}**，每个参数理论信息量为 log₂3 ≈ **1.58 比特**。相比 FP16 的 16 比特，存储压缩约 10 倍。

### 为什么叫 b1.58

log₂3 = 1.58496... ≈ 1.58，表示每个三值权重只需要约 1.58 比特来表示。

### 核心思路

1. **维持高精度主权重 W**（如 BF16），用优化器更新它
2. **前向传播时**将 W 量化为三值 W_q ∈ {−1, 0, +1} 做计算
3. **反向传播时**用 STE 将梯度传回 W

### 量化公式

```
W_q = RoundClip(W / (γ + ε), -1, +1)

其中：
  γ = ||W||_1 / (d_out × d_in)   ← 权重的平均绝对值作为缩放因子
  ε = 小常数，防除零
```

**直觉**：用权重的平均幅度归一化，绝对值大的变 ±1，接近零的变 0。

### 激活量化

激活值通常量化为 8-bit 整数：

```
x̃ = Clip(x × 127 / max(|x|) + ε, -128, 127)
```

即 absmax 量化：用当前激活张量的最大值做缩放。

### 前向计算

```
y ≈ γ · matmul(W_q, x̃)
```

乘法和加法全部是整数运算，极大降低计算开销。只有最后的 γ 缩放是浮点数。

### 关键论文

- *BitNet: Scaling 1-bit Transformers for Large Language Models* (Wang et al., 2023)
- *1.58-bit FLOP-Efficient Large Language Models* (Ma et al., 2024)
- *BitNet a4.8: 4-bit Activations for 1-bit LLMs* (Wang et al., 2025)

---

## 3. BitLinear 层

### 是什么

BitNet 架构的基础构建块，替代标准 Transformer 中的 nn.Linear。

### 结构

```
输入 x
  ↓
RMSNorm (归一化)
  ↓
激活量化 (absmax → INT8)
  ↓
权重量化 (→ {-1, 0, +1})
  ↓
整数矩阵乘法 (matmul)
  ↓
× γ (缩放因子)
  ↓
输出 y
```

### 与标准 Linear 的区别

| | 标准 Linear | BitLinear |
|---|---|---|
| 权重 | FP16/BF16 | {−1, 0, +1} |
| 激活 | FP16/BF16 | INT8 |
| 矩阵乘 | FP 乘加 | INT 加法 + 少量缩放 |
| 计算开销 | 高 | 极低 |

---

## 4. 模型稀疏化 (Sparsity)

### 是什么

将神经网络中一部分权重置零，使权重矩阵变稀疏。稀疏权重不参与计算 → 减少计算量和内存。

### 分类

| 类型 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| **非结构化稀疏** | 任意位置可以为零 | 压缩率灵活 | 难以硬件加速 |
| **结构化稀疏** | 整行/列/块清零 | 硬件友好 | 精度损失大 |
| **半结构化稀疏** | 每 M 个连续权重中保留 N 个 | 兼顾硬件和精度 | 有约束 |

### 训练方式

- **事后剪枝 (Post-training Pruning)**：训完再剪，再微调恢复
- **稀疏预训练 (Sparse Pretraining)**：从头就以稀疏约束训练（本文采用）
- **Dense-to-Sparse**：先密集训练，中途切换到稀疏训练（本文也实验了）

---

## 5. 半结构化 N:M 稀疏

### 是什么

一种**细粒度但有规律**的稀疏模式：在每 M 个连续权重中，最多保留 N 个非零值。NVIDIA Ampere 架构（A100 等）原生支持。

### 常见模式

| 模式 | 稀疏率 | 说明 |
|------|--------|------|
| **2:4** | 50% | NVIDIA 官方支持，最广泛使用 |
| **4:8** | 50% | 等价于 2:4 |
| **6:8** | 25% | 本文主要使用的模式，平衡精度和压缩 |

### 工作原理

```
权重矩阵 (每 4 个一组):
[0.8, -0.1, 0.3, 0.9]  →  保留最大的 2 个  →  [0.8, 0, 0, 0.9]

→ 矩阵乘法时跳过零值，利用 Sparse Tensor Core 加速
```

### 为什么更硬件友好 vs 非结构化稀疏

- **非结构化稀疏**：零的位置随机 → 需要间接索引查找 → 访存不连续 → 快不起来
- **N:M 稀疏**：每 M 个的零的位置可预测 → 硬件可以设计专门的电路来跳过 → 真正的加速

---

## 6. Sparse Tensor Core

### 是什么

NVIDIA 从 Ampere 架构（A100, 2020）开始在 GPU 中引入的硬件单元，专门加速 **2:4 半结构化稀疏**矩阵乘法。

### 核心原理

- 在存储时只保存非零权重及其 2-bit 索引（标识每个非零值在 4 个中的位置）
- 计算时自动跳过零 → 理论 2× 吞吐量提升
- 无需修改模型架构，只需在训练时施加 2:4 约束

### 本文的扩展

本文自己实现了 **6:8 模式**的稀疏算子（标准 CUDA 不支持 6:8），在 NVIDIA A100 和 B200 上实测训推加速最高 **1.30×**。

### 为什么是 1.30× 而不是接近 2×

- 6:8 稀疏率只有 25%，比 2:4（50%）低
- 稀疏算子的开销（元数据读取、非连续访存）抵消了部分收益
- 内存带宽通常是瓶颈，不是纯粹的计算吞吐

---

## 7. Straight-Through Estimator (STE)

### 是什么

一种训练技巧，用于处理**不可微操作**的反向传播。核心思想：前向正常计算，反向时假装该操作是恒等映射。

### 为什么需要

量化函数 `round()` 的导数几乎处处为 0（阶梯函数），会导致梯度消失，无法训练。

### 公式

```
前向:  y = Q(x)        # Q 是无法求导的量化操作
反向:  dL/dx ≈ dL/dy   # 假装 Q 没发生过，梯度直通
```

### 图示

```
前向传播:  x ──→ [round/clip] ──→ y
反向传播:  x ←── [  恒等映射  ] ←── y   ← 梯度直通！
```

### 为什么能 work

虽然"作弊"了，但实践中：
1. 高精度主权重 W 持续接收正确的梯度方向信号
2. 前向计算仍使用量化值，模型适应量化行为
3. 大量训练步骤下，噪声被平均

---

## 8. Dual STE（本文核心创新）

### 是什么

Sparse-BitNet 中**同时**对量化操作和稀疏 mask 操作使用 STE。

### 传统的 Mask 梯度处理（其他方法）

```
dL/dW ≈ M ⊙ dL/dW_eff
```
即：**只让被保留的权重接收梯度**，被剪掉的权重梯度被截断。

⚠️ **问题**：被剪权重永远收不到更新 → 结构过早锁定 → 无法恢复

### 本文的 Dual STE

```
dL/dW ≈ dL/dW_eff    （不乘 mask！）
```

即：**所有权重（包括被剪掉的）都接收完整梯度**。

### 为什么这样更好

```

前向:
  权重 W ─→ [量化→三值] ─→ W_q ─→ [× mask] ─→ W_eff
  
反向:
  权重 W ←── [STE 直通] ←────── [STE 直通] ←────── W_eff
         ↑                                        ↑
    两次直通，梯度不受阻挡                  所有权重都能更新
```

- **被剪权重有机会"复活"**：如果梯度持续推高某个被剪权重的幅度，它可能在下个 step 进入 Top-N
- **避免过早结构锁定**：网络拓扑随训练动态演化
- **实证验证**：消融实验中 Dual STE 明显优于 mask-gated STE

---

## 9. Magnitude-based Pruning（幅度剪枝）

### 是什么

按权重绝对值大小决定保留/剪除：**绝对值大的保留，小的归零**。

### 公式

```
对每组 M 个权重，取 |W| 最大的 N 个 → mask = 1，其余 → mask = 0
```

### 为什么有效

- **直觉**：接近零的权重对输出的贡献小，剪掉影响不大
- **理论**：在一阶泰勒近似下，|W| 越小，去掉该权重对损失的影响越小
- **简单高效**：只需排序/TopK，无需额外超参数

### 本文的细节

Mask 基于**量化前的 BF16 主权重**计算，而不是量化后的三值权重：
- 三值只有 {−1,0,+1}，无法区分幅度细微差异（大量 tie）
- BF16 主权重保留了精细的幅度排序 → 更合理的剪枝选择

---

## 10. Dynamic Mask Recomputation

### 是什么

每个训练步都**重新计算稀疏 mask**，而不是固定不变。

### 为什么需要动态

传统的静态 mask（训完后不变）：
```
Step 100: 选 Top-N → mask_A
Step 101: 用 mask_A（可能已过时）
...
→ 权重已变化，但 mask 没变 → 次优
```

动态 mask：
```
Step 100: 选 Top-N → mask_A
Step 101: 选 Top-N → mask_B（基于最新权重）
→ mask 始终跟踪当前最优稀疏模式
```

### 配合 Dual STE 的效果

动态 mask + 全部权重可更新 → 网络拓扑持续学习优化 → 最终的稀疏结构质量更高

---

## 11. Dense-to-Sparse Training Schedule

### 是什么

一种训练策略：先以**全密集**（无稀疏约束）训练若干步，然后在某个时刻**切换到稀疏训练**。

### 与 Sparse Pretraining 的区别

```
Sparse Pretraining (本文主实验):
  从头到尾都施加 N:M 稀疏约束

Dense-to-Sparse:
  Phase 1: Dense 训练 (如 80% 总步数)
  Phase 2: 切换到 Sparse 训练 (如 20% 总步数)
```

### 优势

- 密集阶段让模型学习好的权重表示
- 稀疏阶段在已有基础上做结构化约束
- 实践中常比从头稀疏效果更好（代价是总训练开销大）

---

## 12. 量化谷结构 (Quantization-Valley)

### 是什么

BitNet 训练后的归一化隐权重（W/γ）呈现的特殊分布：大量值集中在 `[−0.5, 0.5]` 区间内。

### 为什么叫"谷"

```
分布形态示意:
      /\
     /  \        ← 少量权重大于 0.5，被量化到 +1
    /    \
___/      \______  ← "谷"：大量权重在 [−0.5, 0.5]，被量化到 0
           \    /
            \  /   ← 少量权重小于 −0.5，被量化到 −1
             \/
```

RoundClip 函数以 ±0.5 为分界 → 落入中间区间的全变 0，形成自然稀疏。

### 实证数据

BitNet 2B 训练后，三值权重中：
- **0 占比约 42.3%**
- +1 和 −1 各约 28.9%

→ **天然的约 42% 非结构化稀疏**，不需要任何显式剪枝！

### 本文的核心洞察

这种天然的零值分布意味着：
- 做 magnitude-based N:M 剪枝时，BitNet 比 BF16 更"从容"
- BF16 要强行把精细的浮点值清零 → 精度损失大
- BitNet 本来就很多零，剪几个影响不大

---

## 13. Perplexity (PPL)

### 是什么

语言模型最核心的评价指标之一，衡量模型对文本的"惊讶程度"。

### 公式

```
PPL = exp(交叉熵损失) = exp(−1/N Σᵢ log P(wᵢ | w<ᵢ))
```

### 直觉

- **PPL = 10**：平均每次预测时，模型在 10 个候选词中犹豫 → 很好
- **PPL = 100**：模型在 100 个词中犹豫 → 一般
- **PPL 越低越好**

### 在本文中的使用

本文对比了各配置的 PPL 退化（Δ）：
- Dense BF16 → Sparse BF16：PPL 涨 +0.45~+1.20
- Dense BitNet → Sparse BitNet：PPL 仅涨 +0.17~+0.32
- **BitNet 对稀疏化的 PPL 退化仅为 BF16 的 1/3~1/4**

---

## 14. BF16 (Brain Floating Point 16)

### 是什么

Google 提出的 16 位浮点格式，专为深度学习设计。

### 与 FP16 的对比

| | FP16 | BF16 |
|---|---|---|
| 总位数 | 16 | 16 |
| 指数位 | 5 | **8** |
| 尾数位 | 10 | 7 |
| 动态范围 | ±65504 | **±3.4×10³⁸** |
| 精度 | 高 | 低 |

### BF16 的优势

- **更大的动态范围**（8 位指数 vs 5 位）：不易溢出
- **与 FP32 的指数位相同**：转换简单（直接截断尾数）
- **训练更稳定**：不需要 loss scaling

### 在本文中的角色

作为"全精度"基线，与 1.58-bit BitNet 对比。

---

## 15. Qwen-2.5 架构

### 是什么

阿里巴巴通义千问团队发布的开源 LLM 系列，基于标准 Decoder-only Transformer。

### 本文使用的规模

| 模型 | 参数量 | 使用场景 |
|------|--------|----------|
| Qwen2.5-0.5B | 0.5B | 最小规模验证 |
| Qwen2.5-1.5B | 1.5B | 中等规模 |
| Qwen2.5-3B | 3B | 最大规模 |

### 为什么选 Qwen2.5

- 开源、架构清晰、文档齐全
- 多尺度可用（方便 scaling 实验）
- 中文社区关注度高

### 训练配置

- 数据：RefineWeb（约 50B tokens/模型）
- 训练目标：标准因果语言建模（next-token prediction）
- 优化器/学习率/数据在所有变体间保持一致

---

## 16. Sparse-BitLinear 层（本文方法）

### 核心公式链

```
输入 x
  ↓
1. 归一化 + 激活量化
   x̃ = Q_a(Norm(x))                    # → INT8

2. 从主权重生成 mask
   M_{N:M} = TopN(|W|, N, M)           # 每 M 个选 |W| 最大的 N 个

3. 权重量化为三值
   W_q = RoundClip(W / γ, -1, +1)       # → {-1, 0, +1}

4. 对量化权重施加 mask
   W_eff = W_q ⊙ M_{N:M}                # → 稀疏的三值矩阵

5. 整数矩阵乘
   y ≈ s · W_eff @ x̃                    # INT 加法运算

反向：
   dL/dW ≈ dL/dW_eff                   # Dual STE：梯度不乘 mask
```

### 与 BitLinear 的区别

| | BitLinear | Sparse-BitLinear |
|---|---|---|
| 权重 | 三值 | **三值 + N:M 稀疏** |
| 训练 | QAT | QAT + 每步动态 mask |
| 梯度 | 单 STE | **Dual STE** |
| 稀疏 | 非结构化（天然） | **半结构化（显式约束）** |
| 硬件加速 | 依赖专用芯片 | **可用 Sparse Tensor Core** |

---

## 总结：技术栈关系图

```
                    Sparse-BitNet
                         │
          ┌──────────────┼──────────────┐
          │              │              │
    三值量化          半结构化稀疏      训练策略
  (BitNet b1.58)    (N:M Sparsity)   (Dual STE)
          │              │              │
     ┌────┴────┐   ┌────┴────┐    ┌────┴────┐
     │BitLinear│   │2:4 / 6:8│    │动态 Mask │
     │ 权重量化│   │ 幅度剪枝 │    │稠密梯度  │
     │ 激活量化│   │Sparse TC │    │每步重算  │
     └─────────┘   └─────────┘    └─────────┘
          │              │              │
          └──────────────┴──────────────┘
                         │
               Sparse-BitLinear 层
            (量化 + mask 一体化)
                         │
                   前向: 整数加法
                   反向: Dual STE 直通
```

---

*整理时间: 2026-06-27 · 论文提交时间: 2026-03-05*
