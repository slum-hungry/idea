# 🎀 废话诗人 — RISC-V 完整实现指南

> **目标：** 用纯 RISC-V 架构（Milk-V Duo）搭建一台"按按钮→AI生成废话→吐出小票"的美丽小废物  
> **难度：** ⭐⭐⭐（需要 Linux 命令行基础，不需要写驱动）  
> **工期：** 一个周末（2天）  
> **总成本：** ~60 元  

---

## 📦 硬件采购清单

| 序号 | 物品 | 规格/关键词 | 参考价 | 用途 |
|------|------|------------|--------|------|
| 1 | **Milk-V Duo** | **256MB 版本**（必须 256M，64M 内存不够跑模型） | ~40 元 | RISC-V 主控 |
| 2 | **USB-TTL 串口模块** | CH340G / CP2102 均可 | ~5 元 | 烧录系统、串口调试 |
| 3 | **MicroSD 卡** | 8GB 以上，Class 10 | ~15 元 | 系统+模型存储 |
| 4 | **热敏打印机** | 58mm 迷你热敏打印模块，TTL 串口 | ~15 元 | 吐出小票 |
| 5 | **热敏纸** | 58mm×30mm 热敏纸卷 | ~2 元 | 打印耗材 |
| 6 | **轻触按钮** | 6×6mm 四脚轻触开关 | ~0.5 元 | 触发按钮 |
| 7 | **杜邦线** | 母对母 10cm，10 根 | ~3 元 | 接线 |
| 8 | **Type-C 数据线** | 普通安卓数据线 | 自备 | 供电 |
| 9 | **5V 2A 电源** | 手机充电头 | 自备 | 打印机供电 |

> **淘宝关键词：**  
> - 主板：`Milk-V Duo 256M`  
> - 打印机：`58mm 热敏打印机模块 TTL 串口`  
> - 串口模块：`CH340G USB转TTL`

---

## 🧠 硬件接线图

```
        ┌──────────────────────────────────┐
        │          Milk-V Duo               │
        │         (256M 版本)               │
        │                                   │
        │  Pin 2  (5V)  ──────────── 打印机 VCC  │
        │  Pin 6  (GND) ──────────── 打印机 GND  │
        │  Pin 8  (GPIO4/TX) ──────── 打印机 RX  │
        │  Pin 10 (GPIO5/RX) ──────── 打印机 TX  │
        │  Pin 3  (GPIO2) ─┬──────── 按钮一脚     │
        │  Pin 14 (GND)  ─╯─ 10kΩ ── 按钮另一脚  │
        │                                   │
        │  USB-C ◀──── 5V 供电               │
        └──────────────────────────────────┘

        打印机独立供电：
        打印机 VCC ──── 5V 2A 电源适配器
        打印机 GND ──── 电源适配器 GND
```

> ⚠️ **重要：** 热敏打印机打印瞬间电流可达 1-2A，**不要**从 Duo 的 5V pin 供电。给打印机单独供电，但 GND 必须和 Duo 共地（接在一起）。

---

## 📋 接线速查表

| 信号 | Milk-V Duo Pin | 目标 |
|------|---------------|------|
| 5V | Pin 2 | 可给按钮上拉供电（不用于打印机！） |
| GND | Pin 6 / Pin 14 | 打印机 GND + 按钮 GND |
| UART1_TX | Pin 8 (GPIO4) | 打印机 RX |
| UART1_RX | Pin 10 (GPIO5) | 打印机 TX |
| GPIO2 | Pin 3 | 按钮（一端接 GPIO2，另一端接 GND） |

### 按钮接法详解

```
GPIO2 (Pin 3) ──────┬────── 按钮 ────── GND (Pin 14)
                     │
                   10kΩ (可选，GPIO2 有内部上拉)
                     │
                   3.3V (Pin 1)
```

> 使用内部上拉：代码里设置 `pinMode(2, INPUT_PULLUP)` 等效于 gpio 内部上拉。按下时读到 0，松开读到 1。

---

## 🖥️ 第一步：搭建开发环境（30 分钟）

### 1.1 安装编译工具链

在你电脑（Windows/Linux/Mac）上安装 RISC-V 交叉编译工具链：

**Windows 用户（你的情况）：**

```powershell
# 方式一：用 WSL2（推荐）
wsl --install -d Ubuntu-22.04

# 进入 WSL 后：
sudo apt update
sudo apt install -y gcc-riscv64-unknown-elf build-essential git python3
```

**Linux 用户：**
```bash
sudo apt install -y gcc-riscv64-unknown-elf build-essential git
```

**Mac 用户：**
```bash
brew install riscv64-unknown-elf-gcc
```

验证安装：
```bash
riscv64-unknown-elf-gcc --version
# 应输出类似: riscv64-unknown-elf-gcc (GCC) 12.2.0
```

### 1.2 安装串口工具

```bash
# WSL2
sudo apt install -y screen minicom

# Windows 原生（备选）
# 下载 MobaXterm 或使用 PuTTY
```

---

## 📥 第二步：烧录系统到 Milk-V Duo（1 小时）

### 2.1 下载镜像

从官方下载最新 BuildRoot 镜像：

```bash
# 在你的电脑上
wget https://github.com/milkv-duo/duo-buildroot-sdk/releases/latest/download/milkv-duo256m-v1.1.1.img.zip
unzip milkv-duo256m-v1.1.1.img.zip
```

> 如果官方链接失效，去 https://github.com/milkv-duo/duo-buildroot-sdk/releases 找最新的 `milkv-duo256m-*.img.zip`

### 2.2 烧录到 MicroSD 卡

**Windows：** 用 [balenaEtcher](https://www.balena.io/etcher/) 或 [Rufus](https://rufus.ie/)  
**Linux/Mac：**
```bash
# 找到 SD 卡设备（假设是 /dev/sdb，务必确认！）
sudo fdisk -l

# 烧录
sudo dd if=milkv-duo256m-v1.1.1.img of=/dev/sdb bs=4M status=progress
sync
```

### 2.3 首次启动

1. 把 SD 卡插入 Duo
2. **不要把 USB-TTL 模块接上**（先让系统自己启动）
3. Type-C 供电（接电脑 USB 口即可）
4. 等 30 秒蓝色 LED 闪烁 → 系统启动完成

### 2.4 串口连接

接好 USB-TTL 模块：

```
USB-TTL       Milk-V Duo
──────────────────────────
GND     ────  Pin 6  (GND)
TX      ────  Pin 10 (GPIO5 / UART1_RX)  
RX      ────  Pin 8  (GPIO4 / UART1_TX)
```

> 先不接 TX/RX 也可以，先接 GND，等确认系统启动后再接信号线。

**连接串口终端：**
```bash
# 找到 USB-TTL 的串口设备
ls /dev/ttyUSB*    # Linux/WSL
# Windows: 设备管理器 → 端口 → COMx

# 打开终端（WSL 需要先挂载 USB）
screen /dev/ttyUSB0 115200
# 或
minicom -D /dev/ttyUSB0 -b 115200
```

按回车，应该看到登录提示：
```
milkv-duo login: root
password: milkv
```

> 默认用户名 `root`，密码 `milkv`

---

## 🌐 第三步：配置网络（20 分钟）

Duo 需要联网来下载代码，两种方式：

### 方式 A：USB RNDIS 共享网络（推荐）

1. Type-C 线连接电脑和 Duo
2. Duo 启动后，电脑上会出现一个 USB 网卡
3. 在 Duo 上：
```bash
# Duo 上执行
ifconfig usb0 192.168.42.2
route add default gw 192.168.42.1
echo "nameserver 8.8.8.8" > /etc/resolv.conf
```

4. 在你的电脑上设置共享网络（把电脑的网络共享给 USB RNDIS 网卡）

### 方式 B：USB-TTL 串口 + 手动传文件（无网络备选）

如果实在连不上网，可以在电脑上下载好代码，用 `sz`/`rz` 或 SD 卡传文件：
```bash
# 电脑上下载好后，拔 SD 卡插电脑，复制文件到 SD 卡
# 然后把 SD 卡插回 Duo，mount 就能看到
```

---

## 📝 第四步：在 Duo 上编译 llama2.c（1 小时）

### 4.1 安装 Duo 上的编译依赖

```bash
# 在 Duo 上（通过串口）
# Duo 的 BuildRoot 系统自带 gcc，但不一定有 make
# 先测试一下：
gcc --version

# 如果没有 gcc，说明需要交叉编译（在电脑上编好传过去）
# 这个情况最常见，见下面的交叉编译方案
```

### 4.2 交叉编译 llama2.c（在你的电脑上）

**这是关键步骤：在电脑上用 RISC-V 工具链编译，生成能在 Duo 上运行的二进制文件。**

```bash
# 在你的电脑上（WSL/Linux/Mac）
cd ~
git clone https://github.com/karpathy/llama2.c.git
cd llama2.c

# 下载 TinyStories 模型（15M 参数版本）
wget https://huggingface.co/karpathy/tinyllamas/resolve/main/stories15M.bin

# 编译为 RISC-V 可执行文件
riscv64-unknown-elf-gcc -O3 -march=rv64gc -static -o poet run.c -lm

# 检查文件类型
file poet
# 应输出: poet: ELF 64-bit LSB executable, UCB RISC-V, ...

# 查看大小
ls -lh poet
# 约 200-500KB
```

### 4.3 上传到 Duo

```bash
# 方式一：通过 RNDIS 网络上传（如果配好了网络）
scp poet root@192.168.42.2:/root/
scp stories15M.bin root@192.168.42.2:/root/

# 方式二：SD 卡复制（最稳妥）
# 拔下 SD 卡 → 插电脑 → 复制文件 → 插回 Duo
# 然后在 Duo 上：
mount /dev/mmcblk0p2 /mnt
cp /mnt/poet /root/
cp /mnt/stories15M.bin /root/
```

### 4.4 在 Duo 上测试

```bash
# 在 Duo 上
cd /root
chmod +x poet

# 跑一次测试
./poet stories15M.bin -n 80 -t 0.8
```

**预期输出类似：**
```
Once upon a time, there was a little cat named Luna.
Luna loved to play in the garden with her friend, a tiny butterfly.
Every day, they would dance under the big old tree.
The end.
```
（用时约 5-15 秒）

> ✅ 这一步通了，核心闭环就完成了！

### 4.5 优化：打造废话诗人专用 Prompt

TinyStories 模型是讲故事的，我们需要给它一个"废话诗人"的 prompt 前缀来引导输出：

**修改 `run.c` 的生成逻辑：**

在 `run.c` 中找到生成函数（通常是 `generate` 函数），在 `sampler` 之前添加 prompt 前缀。

关键修改点（在 `run.c` 中搜索 `generate` 函数）：

```c
// 原来可能是这样启动生成的：
// ...

// 修改为在 token 序列开头插入 prompt token
// Prompt: "A little poet machine says:"
// 这句 prompt 会被 encode 成 token 序列，给模型提供上下文

// 具体实现方式：
// 在 run.c 大约第 500 行附近找到生成循环
// 把 prompt 字符串用 tokenizer encode 后填入 tokens 数组开头
```

**更简单的做法：** llama2.c 本身支持 `-i` 参数输入 prompt：

```bash
echo "Write one short poetic sentence:" | ./poet stories15M.bin -i -n 60 -t 0.9
```

> 或者直接魔改 `run.c`，把 prompt 硬编码进去。

**这里提供一份完整的魔改版 `poet.c`（基于 run.c 修改）：**

在 `run.c` 的主函数中，找到生成部分，确保 prompt 正确注入。关键代码位置通常在 `main` 函数底部的生成循环中。如果你的 `run.c` 版本较新（2024+），可以使用 `-i` 选项来输入 prompt。

---

## 🖨️ 第五步：驱动热敏打印机（1.5 小时）

### 5.1 热敏打印机型号说明

市面上最常见的 58mm TTL 热敏打印模块：

| 品牌/型号 | 接口 | 波特率 | 中文支持 |
|-----------|------|--------|----------|
| GOOJPRT QR203 | TTL 串口 | 9600/115200 | 需 ESC/POS 命令 |
| 58mm 迷你打印模块 | TTL 串口 | 9600 | 通常只支持 ASCII |
| Xprinter XP-58 | TTL 串口 | 9600/115200 | 支持 |

**建议：先买最便宜的 58mm TTL 迷你打印模块（~15 元），只打英文。**

### 5.2 在 Duo 上测试打印机

```bash
# 在 Duo 上，先做一个最基础的测试——直接往串口写数据

# 1. 配置 UART1（对应 GPIO4/GPIO5）
# Milk-V Duo 上 UART1 的设备文件是 /dev/ttyS1
# 先设置波特率
stty -F /dev/ttyS1 9600

# 2. 发一行文字
echo "Hello from RISC-V!" > /dev/ttyS1

# 3. 如果打印机没反应，试试：
echo -e "Hello from RISC-V!\n\n\n" > /dev/ttyS1

# 4. 打印机需要足够的换行才能吐纸（热敏打印机通常缓存一行再打）
```

> 如果这一步打印出来了 → 硬件接线正确，庆祝！  
> 如果没反应 → 检查接线（TX/RX 有没有接反），检查波特率，检查打印机是否独立供电

### 5.3 常见热敏打印机 ESC/POS 命令速查

```c
// 初始化打印机
printf("\x1B\x40");  // ESC @  → 复位

// 字体大小
printf("\x1D\x21\x11");  // 双倍宽高
printf("\x1D\x21\x00");  // 正常大小

// 对齐
printf("\x1B\x61\x01");  // 居中
printf("\x1B\x61\x00");  // 左对齐

// 走纸
printf("\x1B\x64\x03");  // 走纸 3 行

// 加粗
printf("\x1B\x45\x01");  // 加粗开
printf("\x1B\x45\x00");  // 加粗关

// 切纸（如果打印机支持）
printf("\x1D\x56\x00");  // 部分切纸
```

测试：
```bash
# 打印一行大号居中的测试文字
printf "\x1B\x40\x1B\x61\x01\x1D\x21\x11HELLO RISC-V\x1D\x21\x00\x1B\x61\x00\x1B\x64\x05" > /dev/ttyS1
```

> 如果中文乱码：热敏打印机通常需要专门的汉字库芯片。请先确认你的打印机模块是否支持 GB2312。不支持的话，第一步先打英文。

---

## 🔗 第六步：整合代码——put it all together（2 小时）

### 6.1 完整的主程序 `poet_printer.c`

在电脑上创建这个文件，交叉编译后传到 Duo 上：

```c
/*
 * 废话诗人 - RISC-V 版完整实现
 * 在 Milk-V Duo (RV64GC) 上运行
 * 
 * 功能：
 *   1. 监听 GPIO2 按钮
 *   2. 按钮按下 → 运行 llama2.c 生成废话
 *   3. 将生成的废话发送到热敏打印机
 *   4. 打印精美小票
 *
 * 编译命令（在电脑上）：
 *   riscv64-unknown-elf-gcc -O3 -march=rv64gc -static \
 *     -o poet_printer poet_printer.c printer.c tinyllm.c -lm
 *
 * 运行（在 Duo 上）：
 *   ./poet_printer stories15M.bin
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include <time.h>

/* ================================================================
 * 第一部分：GPIO 按钮驱动（通过 sysfs）
 * ================================================================ */

#define BUTTON_GPIO  2   // GPIO2, Pin 3
#define BUTTON_PATH  "/sys/class/gpio/gpio2/value"

int gpio_init(int gpio_num) {
    FILE *fp;
    char path[64];

    // 导出 GPIO
    fp = fopen("/sys/class/gpio/export", "w");
    if (fp) {
        fprintf(fp, "%d", gpio_num);
        fclose(fp);
    }

    // 设置方向为输入
    snprintf(path, sizeof(path), "/sys/class/gpio/gpio%d/direction", gpio_num);
    // 等一下让系统完成 export
    usleep(100000);
    fp = fopen(path, "w");
    if (!fp) {
        printf("[ERROR] Cannot set GPIO%d direction\n", gpio_num);
        return -1;
    }
    fprintf(fp, "in");
    fclose(fp);

    printf("[GPIO] GPIO%d initialized as input\n", gpio_num);
    return 0;
}

int gpio_read(int gpio_num) {
    char path[64];
    char value;
    FILE *fp;

    snprintf(path, sizeof(path), "/sys/class/gpio/gpio%d/value", gpio_num);
    fp = fopen(path, "r");
    if (!fp) return -1;
    value = fgetc(fp);
    fclose(fp);
    return (value == '1') ? 1 : 0;
}

/* ================================================================
 * 第二部分：热敏打印机驱动（ESC/POS）
 * ================================================================ */

#define PRINTER_DEV  "/dev/ttyS1"  // UART1, GPIO4/GPIO5
#define PRINTER_BAUD 9600

int printer_fd = -1;

int printer_init() {
    printer_fd = open(PRINTER_DEV, O_RDWR | O_NOCTTY);
    if (printer_fd < 0) {
        printf("[ERROR] Cannot open printer at %s\n", PRINTER_DEV);
        return -1;
    }
    printf("[PRINTER] Opened %s\n", PRINTER_DEV);

    // 初始化打印机
    write(printer_fd, "\x1B\x40", 2);  // ESC @  复位
    usleep(50000);
    return 0;
}

void printer_send(const char *text) {
    if (printer_fd < 0) return;
    write(printer_fd, text, strlen(text));
}

void printer_center(const char *text) {
    /* 居中 + 打印 + 换行 */
    printer_send("\x1B\x61\x01");   // 居中
    printer_send(text);
    printer_send("\n");
    printer_send("\x1B\x61\x00");   // 恢复左对齐
}

void printer_big(const char *text) {
    /* 放大字体打印 */
    printer_send("\x1D\x21\x22");   // 双倍宽 + 双倍高
    printer_send(text);
    printer_send("\n");
    printer_send("\x1D\x21\x00");   // 恢复
}

void printer_line(int length) {
    /* 打印分隔线 */
    char line[64];
    memset(line, '-', length);
    line[length] = '\n';
    line[length+1] = '\0';
    printer_send(line);
}

void printer_feed(int lines) {
    /* 走纸 */
    char cmd[8];
    snprintf(cmd, sizeof(cmd), "\x1B\x64\x%c", (char)lines);
    write(printer_fd, cmd, 3);
}

void print_ticket(const char *poem) {
    /* 
     * 打印完整的废话小票
     * 格式：
     *   ┌──────────────────┐
     *   │   🌸 废话小票 🌸   │
     *   │ ──────────────── │
     *   │   (生成的废话)     │
     *   │ ──────────────── │
     *   │ Powered by        │
     *   │ RISC-V & Milk-V   │
     *   └──────────────────┘
     */
    
    printer_feed(1);
    printer_big("  Flower Poet  ");
    printer_feed(1);
    printer_center("~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~");
    printer_feed(1);
    
    /* 打印废话正文 */
    printer_send("  ");
    printer_send(poem);
    printer_send("\n");
    
    printer_feed(2);
    printer_center("~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~");
    printer_center("Powered by RISC-V");
    printer_center("Milk-V Duo + llama2.c");
    printer_feed(3);
}

/* ================================================================
 * 第三部分：LLM 生成封装
 * 调用外部 llama2.c 的生成函数
 * ================================================================ */

/* 前向声明 llama2.c 的函数（需要与 llama2.c 中的声明一致）*/
/* 如果你的 llama2.c 版本不同，请根据需要调整 */

/* 
 * 这里我们用最简单的方式：system() 调用编译好的 poet 二进制
 * 把输出捕获到文件，然后读取
 * 
 * 生产环境中建议链接 llama2.c 的源码直接调用 generate 函数
 */

#define MODEL_PATH   "stories15M.bin"
#define TEMP_FILE    "/tmp/poem_output.txt"
#define MAX_POEM_LEN 512

int generate_poem(char *output, int max_len) {
    /*
     * 调用 llama2.c 生成废话
     * 用 system() 调用编译好的 poet 二进制
     */
    
    /* 构建命令：prompt 引导生成一句有诗意的废话 */
    char cmd[512];
    const char *prompt = "Write one short poetic sentence about a little poet machine:";
    
    snprintf(cmd, sizeof(cmd),
        "echo '%s' | ./poet %s -i -n 60 -t 0.95 > %s 2>/dev/null",
        prompt, MODEL_PATH, TEMP_FILE);
    
    printf("[LLM] Generating poem...\n");
    int ret = system(cmd);
    if (ret != 0) {
        printf("[LLM] Generation failed with code %d\n", ret);
        strncpy(output, "The poet is sleeping... zzz", max_len);
        return -1;
    }
    
    /* 读取生成的文本 */
    FILE *fp = fopen(TEMP_FILE, "r");
    if (!fp) {
        strncpy(output, "The poet is sleeping... zzz", max_len);
        return -1;
    }
    
    /* 跳过 prompt 的回显，找到实际生成内容 */
    char line[256];
    int found_output = 0;
    output[0] = '\0';
    
    while (fgets(line, sizeof(line), fp)) {
        /* 移除行尾的换行 */
        line[strcspn(line, "\n")] = 0;
        
        if (strlen(line) > 10 && !strstr(line, "Write one short")) {
            /* 这是模型生成的内容，把它拼起来 */
            if (strlen(output) + strlen(line) + 2 < max_len) {
                if (strlen(output) > 0) strcat(output, " ");
                strcat(output, line);
                found_output = 1;
            }
        }
    }
    fclose(fp);
    
    if (!found_output) {
        strncpy(output, "The universe whispers... nothing.", max_len);
    }
    
    /* 截断过长文本 */
    if (strlen(output) > max_len - 1) {
        output[max_len - 4] = '.';
        output[max_len - 3] = '.';
        output[max_len - 2] = '.';
        output[max_len - 1] = '\0';
    }
    
    printf("[LLM] Generated: %s\n", output);
    return 0;
}

/* ================================================================
 * 第四部分：主循环
 * ================================================================ */

int main(int argc, char **argv) {
    srand(time(NULL));
    
    printf("\n");
    printf("  ================================\n");
    printf("      Flower Poet Machine\n");
    printf("      RISC-V Edition\n");
    printf("  ================================\n\n");
    
    /* 初始化 GPIO 按钮 */
    if (gpio_init(BUTTON_GPIO) < 0) {
        printf("[FATAL] GPIO init failed\n");
        return 1;
    }
    
    /* 初始化打印机 */
    if (printer_init() < 0) {
        printf("[FATAL] Printer init failed\n");
        return 1;
    }
    
    /* 打印开机票 */
    print_ticket("I woke up.\n  Ready to write\n  some nonsense.");
    
    printf("[MAIN] Waiting for button press... (GPIO%d)\n", BUTTON_GPIO);
    printf("[MAIN] Press the button to get a poem!\n");
    
    int last_state = 1;  // 上拉，默认高电平
    
    while (1) {
        int state = gpio_read(BUTTON_GPIO);
        
        /* 检测下降沿（按钮按下：1 → 0）*/
        if (state == 0 && last_state == 1) {
            printf("\n[BTN] Button pressed! Generating poem...\n");
            
            /* 防抖延时 */
            usleep(50000);
            
            /* 生成废话 */
            char poem[MAX_POEM_LEN];
            generate_poem(poem, MAX_POEM_LEN);
            
            /* 打印 */
            print_ticket(poem);
            
            printf("[BTN] Ticket printed! Waiting for next press...\n");
            
            /* 防止连续触发 */
            sleep(1);
        }
        
        last_state = state;
        usleep(100000);  // 100ms 轮询间隔
    }
    
    close(printer_fd);
    return 0;
}
```

### 6.2 编译和部署

```bash
# 在你的电脑上交叉编译
riscv64-unknown-elf-gcc -O3 -march=rv64gc -static \
    -o poet_printer poet_printer.c -lm

# 上传到 Duo
# (通过 SD 卡或 scp)
scp poet_printer root@192.168.42.2:/root/

# 在 Duo 上
chmod +x /root/poet_printer
/root/poet_printer
```

---

## 🔄 第七步：设置开机自启动（20 分钟）

让废话诗人插电就自己跑起来，不需要手动启动：

```bash
# 在 Duo 上创建启动脚本
cat > /etc/init.d/S99poet << 'EOF'
#!/bin/sh
# 废话诗人 开机自启

case "$1" in
  start)
    echo "Starting Flower Poet..."
    cd /root
    # 确保有 ttyS1
    stty -F /dev/ttyS1 9600 2>/dev/null
    # 启动主程序
    /root/poet_printer > /var/log/poet.log 2>&1 &
    echo "Poet PID: $!"
    ;;
  stop)
    killall poet_printer 2>/dev/null
    ;;
  *)
    echo "Usage: $0 {start|stop}"
    ;;
esac
EOF

chmod +x /etc/init.d/S99poet
```

重启验证：
```bash
reboot
# 等 30 秒，按按钮 → 应该直接出票
```

---

## 🎨 第八步：外壳设计（可选但强烈推荐）

"美丽小废物"的核心是**小+美丽**。几个外壳方案：

### 方案 A：3D 打印外壳（最精致）

```openscad
// 废话诗人外壳 - OpenSCAD 模型
// 尺寸：80mm × 60mm × 40mm

difference() {
    cube([80, 60, 40]);  // 主体
    // 掏空内部
    translate([2, 2, 2])
        cube([76, 56, 36]);
    // 出纸口
    translate([10, 0, 10])
        cube([60, 5, 3]);
    // 按钮孔
    translate([40, 30, 38])
        cylinder(h=5, r=8);
    // USB 口
    translate([25, 58, 15])
        cube([10, 5, 12]);
}
```

### 方案 B：硬纸板/亚克力（最快）

- 透明亚克力板 + 热熔胶
- 淘宝搜`亚克力展示盒`，买现成的 80×60×40mm 盒子，自己开口

### 方案 C：木盒复古风

- 淘宝搜`木质收纳盒 小号`
- 打孔：出纸口 + 按钮孔 + USB 口

---

## 🐛 常见问题排查

| 问题 | 可能原因 | 解决方法 |
|------|---------|----------|
| Duo 不启动 | SD 卡烧录失败 | 重烧镜像，用 balenaEtcher |
| 串口没输出 | USB-TTL 接反 | RX→TX, TX→RX, 一定接 GND |
| 打印机没反应 | 供电不足 | 打印机独立 5V 2A 供电，共地 |
| 打印机乱码 | 波特率不对 | 试 9600 和 115200 |
| `poet` 报 segfault | 内存不够 | 确认是 256M 版本 Duo |
| 按钮不响应 | GPIO 号不对 | `cat /sys/kernel/debug/gpio` 查看 |
| 模型生成全是乱码 | 模型文件损坏 | 重新下载 `stories15M.bin` |

---

## 📊 性能数据参考

| 指标 | 数值 |
|------|------|
| 模型大小 | 15M 参数 |
| 模型文件 | ~60MB（fp32）/ ~30MB（fp16）|
| 内存占用 | ~70MB |
| 生成速度 | ~5-15 token/s |
| 一首废话耗时 | 5-15 秒 |
| 打印耗时 | ~1 秒 |
| 总体功耗 | ~3W |
| 总成本 | ~60 元 |

---

## 🚀 进阶方向（做好基础版后再搞）

1. **中文支持：** 换用通义千问 API（需要 WiFi 模块），中文废话更有味道
2. **更多按钮：** 不同按钮 → 不同 prompt 模式（星座废话 / 彩虹屁 / 毒鸡汤 / 运势）
3. **OLED 屏幕：** 在等打印的时候，屏幕上先滚动显示生成中的文字
4. **声音反馈：** 加个小蜂鸣器，按按钮→"叮"一声
5. **换更强的模型：** 在 Milk-V Duo 上编译 llama.cpp，跑 TinyLlama Q4
6. **INT4 + N:M 稀疏：** 这不就是你的 research 方向嘛——在这个平台上做实验

---

## ✅ 检查清单（做完打勾）

- [ ] 买到所有硬件
- [ ] 烧录 Duo 系统，能串口登录
- [ ] 交叉编译 llama2.c，在 Duo 上能跑通
- [ ] 接好打印机，能打印测试文字
- [ ] 接好按钮，能读到按下事件
- [ ] 整合代码编译通过
- [ ] 按按钮 → 生成废话 → 打印小票 串联成功
- [ ] 做好外壳，可以拍照发朋友圈了 📸
- [ ] （可选）开机自启动

---

> **最后一句：** 这台机器没有任何实用价值，但它会是你桌上最可爱的东西。  
> *— 因为你亲手赋予了它灵魂。*
