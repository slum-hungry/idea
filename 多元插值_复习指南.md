# 多元插值法及应用 — 期末复习指南

> 基于讲义 + 期末试卷整理，涵盖考试重点

---

## 📋 考试题型分布（共100分）

| 题号 | 内容 | 分值 |
|------|------|------|
| 1 | Newton 差商表 + 插值公式构造 | 15 |
| 2 | Lagrange 插值公式（矩形张量积网格） | 15 |
| 3 | Aitken 逐次迭代插值 | 15 |
| 4 | 多元多项式插值的存在唯一性（论述） | 15 |
| 5 | 多元插值与深度学习（论述） | 25 |
| 6 | 径向基函数 RBF 优缺点（论述） | 15 |

---

## 第一章 二元多项式插值

### 核心问题：为什么多元插值比一元更复杂？

**一元插值**：任意 n+1 个互异点，一定存在唯一的 ≤n 次插值多项式。  
**多元插值**：点的分布和多项式的类型互相限制。

例：平面四点 (0,0), (1,0), (1,1), (0,1)：
- P(x,y)=a₀+a₁x+a₂y+d₁x² → **无解**（行列式=0）
- P(x,y)=a₀+a₁x+a₂y+d₃xy → **有唯一解**

### §2 Bezout 定理

> 若 m 次代数曲线 P₁(x,y)=0 与 n 次曲线 P₂(x,y)=0 的交点多于 mn 个，则存在非零多项式 Q(x,y) 使 P₁=Q·R₁, P₂=Q·R₂（R₁,R₂ 次数分别 <m, <n）

**直观理解**：两条次数为 m 和 n 的代数曲线，如果交点超过 mn 个，则它们必有公共因子（即"共享"一条公共曲线）。

### §3 适定结点组 — 核心概念

**插值空间 P**：由基函数 {P₁,…,P_k} 张成的线性空间。  
**适定结点组 {q₁,…,q_k}**：对任意被插函数 f，插值问题解存在且唯一。

**基本引理**：{q_i} 是 P 的适定结点组 ⟺ {q_i} 不全部落在 P 中的任何一条代数曲线上。

**构造方法**（必须掌握）：

1. **直线型结点组 D_n**：从一点出发，每次加一条不含已选点的直线，选 n+1 个点
2. **弧线型结点组 D_{2n}**：用不可约二次曲线逐步加点
3. **竖线/横线型结点组 U_{m,n}**：m+1 条竖直线 × n+1 个横坐标点（张量积网格的特殊形式）
4. **十字形结点组 B_{n,n}**：用十字形逐步加点

### §4 差商算法（Newton型）

**一元差商回顾**：
```
f[x₀,x₁] = (f(x₁)-f(x₀))/(x₁-x₀)
f[x₀,x₁,x₂] = (f[x₁,x₂]-f[x₀,x₁])/(x₂-x₀)
```

**二元差商定义**：
- **固定 y 的一阶差商**：f[xᵢ,xⱼ; y] = (f(xⱼ,y) - f(xᵢ,y))/(xⱼ-xᵢ)
- **固定 x 的一阶差商**：f[x; yᵢ,yⱼ] = (f(x,yⱼ) - f(x,yᵢ))/(yⱼ-yᵢ)
- **混合差商**：f[xᵢ,xⱼ; yₐ,y_b] = (f[xᵢ,xⱼ; y_b] - f[xᵢ,xⱼ; yₐ])/(y_b-yₐ)
  = (f[xⱼ; yₐ,y_b] - f[xᵢ; yₐ,y_b])/(xⱼ-xᵢ)

**可整序格点网**上的插值公式（重点）：
```
P(x,y) = Σᵥ₌₀ᵐ Σᵤ₌₀ⁿᵛ Xᵥ·Yᵤ·fᵥᵤ

其中：
  Xᵥ = (x-x₀)(x-x₁)...(x-xᵥ₋₁)
  Yᵤ = (y-y₀)(y-y₁)...(y-yᵤ₋₁)
  fᵥᵤ = f[x₀,...,xᵥ; y₀,...,yᵤ]
```

### §5 Lagrange 方法

**张量积网格插值**（考试重点！）：
在矩形网格上：{x₀,...,x_m} × {y₀,...,y_n}，使用基函数的张量积：
```
L(x,y) = Σᵢ₌₀ᵐ Σⱼ₌₀ⁿ f(xᵢ,yⱼ) · lₓ,ᵢ(x) · lᵧ,ⱼ(y)

其中 lₓ,ᵢ(x) = Πₖ≠ᵢ (x-xₖ)/(xᵢ-xₖ)  是x方向的一元Lagrange基函数
     lᵧ,ⱼ(y) = Πₖ≠ⱼ (y-yₖ)/(yⱼ-yₖ)  是y方向的一元Lagrange基函数
```

### §6 Aitken 方法（考试重点！）

**核心思想**：逐点迭代，用低次插值组合得到高次插值。

**一元 Aitken 递推**：
```
P₀,₁(x) = | f₀  x₀-x |
          | f₁  x₁-x | / (x₁-x₀)

P₀,₁,₂(x) = | P₀,₁(x)  x₀-x |
             | P₁,₂(x)  x₁-x | / (x₂-x₀)
```

**二元 Aitken**：先在 x 方向对每个 y 层做一维插值，再在 y 方向对结果插值。

### §7 迭加插值法

基于 §3 的适定结点组构造理论，逐层叠加插值多项式。

---

## 第二章 超限插值法

> ⚠️ 文档格式无法提取，以下是超限插值法的核心概念

**超限插值**（Transfinite Interpolation）：
- 不仅仅在离散点上插值，而是在整条曲线/边界上匹配函数值
- 常用于计算机辅助几何设计（CAGD）中的曲面构造
- 典型方法：Coons 曲面、Gordon 曲面

**Coons 曲面**（双线性混合）：
```
S(u,v) = (1-u)f(0,v) + u·f(1,v) + (1-v)f(u,0) + v·f(u,1)
         - (1-u)(1-v)f(0,0) - (1-u)v·f(0,1) - u(1-v)f(1,0) - uv·f(1,1)
```
即：两条边界的线性插值 + 两条边界 = 四个角点的修正

---

## 第三章 多元连分式插值与逼近

> ⚠️ 该章讲义为扫描件，无法直接提取文本。以下是关键知识点

**连分式插值**（Thiele型）：
- 一元形式：f(x) ≈ b₀ + (x-x₀)/(b₁ + (x-x₁)/(b₂ + ...))
- 多元推广：用分支连分式（branched continued fractions）逼近多元函数

**优点**：
- 对极点函数逼近效果好
- 收敛速度可能优于多项式
- 自然地处理有理逼近

**多元连分式的构造**：
```
R(x,y) = b₀(y) + (x-x₀)/(b₁(y) + (x-x₁)/(b₂(y) + ...))
```
其中 bᵢ(y) 本身也是连分式。

---

## 第四章 有理函数插值方法

### §1 有理插值问题的一般提法

**形式**：R_{m,n}(x) = P(x)/Q(x)，其中 ∂P≤m, ∂Q≤n

给定 m+n+1 个型值点 (xᵢ, yᵢ)，求满足 R(xᵢ)=yᵢ 的有理函数。

**关键区别**：
- 多项式插值 → 解线性方程组，有 m+n+1 个点就确定
- 有理插值 → 解非线性方程组，有解是有条件的

**方程组**：a₀+a₁xᵢ+...+aₘxᵢᵐ - yᵢ(b₀+b₁xᵢ+...+bₙxᵢⁿ) = 0

这是关于 a_i, b_j 的**齐次线性方程组**，总有非平凡解，但不一定满足原始插值条件。

**不可达点**：解出的 P(xᵢ)/Q(xᵢ) 不满足原始条件的点（可能 Q(xᵢ)=0 导致 0/0 型）。

### §2 存在唯一性

**定理 4.4**：有理插值问题若有解，则解必唯一（在等价意义下）。

**等价定义**：P₁/Q₁ ~ P₂/Q₂ ⟺ P₁Q₂ ≡ P₂Q₁

**定理 4.3**：若 P(x),Q(x) 有 k 次公因子，则除去公因子后至少在 N-k+1 个点上满足插值条件。

### §3 混合有理插值方法

将有理插值转化为更易处理的形式，处理不可达点的问题。

### §4 有理插值的算法

- **Thiele 型连分式算法**：利用逆差商构造有理插值
- **Salzer 方法**：基于Newton差商的推广

---

## 考试重点速查表

### 计算题核心公式

**1. 差商（Newton）插值**：
```
一阶差商: f[x₀,x₁] = (f(x₁)-f(x₀))/(x₁-x₀)
二阶差商: f[x₀,x₁,x₂] = (f[x₁,x₂]-f[x₀,x₁])/(x₂-x₀)

二元混合差商: f[x₀,x₁; y₀,y₁] = (f[x₀,x₁; y₁] - f[x₀,x₁; y₀])/(y₁-y₀)

插值公式: P(x,y) = f₀₀ + f₁₀·x + f₂₀·x(x-1) + f₀₁·y + f₀₂·y(y-1) + ...
（使用差商表对角元）
```

**2. Lagrange 插值（张量积网格）**：
```
L(x,y) = Σᵢ Σⱼ f(xᵢ,yⱼ) · lᵢ(x) · lⱼ(y)
其中 lᵢ(x) = Π_{k≠i} (x-x_k)/(x_i-x_k)
```

**3. Aitken 迭代**：
```
第一步（x方向）：对每个y固定层做一维Aitken
  P₀,₁(x) = det([f₀ x₀-x; f₁ x₁-x]) / (x₁-x₀)
第二步（y方向）：对上一步的多项式族做y方向Aitken
  P(x,y) = det([P_{y₀}(x) y₀-y; P_{y₁}(x) y₁-y]) / (y₁-y₀)
```

### 论述题核心要点

**4. 多元插值的存在唯一性**：
- 一元空间：Haar条件成立，总有唯一解
- 多元空间：Haar条件不成立！连 d≥2 时没有对所有点集都适定的空间
- 解决办法：①规定特殊节点几何构型 ②径向基函数(RBF) ③多元样条/有限元

**5. 多元插值与深度学习**：
- DNN本质是高维多元逼近
- ReLU = 多元分段线性插值
- KAN网络（Kolmogorov-Arnold网络）直接用样条插值替代线性权重
- 流形假设帮助克服维数灾难
- RBF网络 / 高斯过程 → 深度贝叶斯学习

**6. RBF 优缺点**：
- 优点：不畏网格、保证存在唯一性（Micchelli定理）、与维数无关
- 缺点：计算O(N³)、存储O(N²)、形状参数敏感、边缘龙格现象

---

## 💡 解题模板

### 模板A：差商表 + Newton公式

```
已知数据：
  y=0    y=1    y=2
x=0  f₀₀   f₀₁   f₀₂
x=1  f₁₀   f₁₁
x=2  f₂₀

步骤：
1. 对每个固定y列，做x方向差商表
2. 对x=0行，做y方向差商表
3. 计算必要的混合差商
4. 取差商表对角元构造插值多项式
```

### 模板B：Lagrange 张量积

```
步骤：
1. 分别构造x方向和y方向的Lagrange基函数
2. 二元基函数 = lᵢ(x) × lⱼ(y)
3. 加权求和: L(x,y) = Σ f(xᵢ,yⱼ)·lᵢ(x)·lⱼ(y)
4. 化简
```

### 模板C：Aitken 迭代

```
步骤：
1. 对每行（固定y），用Aitken法做x方向插值
2. 对第1步的结果，用Aitken法做y方向插值
3. 最终化简得到插值多项式
```

---

接下来：**专项练习 + 7天复习计划**
