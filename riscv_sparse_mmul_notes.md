# RISC-V 向量处理器上的结构化稀疏矩阵乘法优化

> 基于论文 *"Optimizing Structured-Sparse Matrix Multiplication in RISC-V Vector Processors"* (arXiv: 2501.10189v1) 的精读笔记
>
> 日期：2026-06-02

---

## 目录

1. [Gustavson 方法 vs 传统内积法](#1-gustavson-方法-vs-传统内积法)
2. [向量寄存器文件 (VRF)](#2-向量寄存器文件-vrf)
3. [循环展开 (Loop Unrolling) 精读](#3-循环展开-loop-unrolling-精读)
4. [vindexmac 自定义指令](#4-vindexmac-自定义指令)
5. [Fig.4 图解](#5-fig4-图解)

---

## 1. Gustavson 方法 vs 传统内积法

### 1.1 问题

稀疏矩阵乘法 \( C = A \times B \)，两种实现路径：

| | 内积法 (IP) | Gustavson 方法 |
|---|---|---|
| A 格式 | CSR | CSR |
| B 格式 | CSR | **CSC** |
| C 生成 | 按 (i,j) 逐个遍历 | 逐行展开 |
| 核心操作 | 稀疏向量归并 | 稠密累加器归约 |

### 1.2 4×4 例子

```
A:
  [2, 0, 3, 0]
  [0, 1, 0, 0]
  [4, 0, 0, 5]
  [0, 0, 6, 0]

B:
  [1, 0, 0, 2]
  [0, 3, 4, 0]
  [0, 5, 0, 0]
  [6, 0, 7, 0]
```

`nnz(A)=6, nnz(B)=7, 理论 FLOPs=24`

### 1.3 内积法汇总

| (i,j) | 命中? | 比较次数 | 结果 |
|-------|-------|---------|------|
| (0,0) | ✅ | 3 | 2 |
| (0,1) | ✅ | 3 | 15 |
| (0,2) | ❌ | 4 | 0 |
| (0,3) | ✅ | 2 | 4 |
| (1,0) | ❌ | 3 | 0 |
| (1,1) | ✅ | 3 | 3 |
| (1,2) | ✅ | 2 | 4 |
| (1,3) | ❌ | 2 | 0 |
| (2,0) | ✅ | 3 | 4 |
| (2,1) | ❌ | 4 | 0 |
| (2,2) | ✅ | 4 | 35 |
| (2,3) | ✅ | 2 | 8 |
| (3,0) | ✅ | 3 | 36 |
| (3,1) | ❌ | 3 | 0 |
| (3,2) | ✅ | 2 | 42 |
| (3,3) | ❌ | 2 | 0 |

- **合计**：12 次命中乘法 (24 FLOPs)，**45 次索引比较**，6/16 位置空跑 (37.5%)

### 1.4 Gustavson 方法

```
A行0: (k=0,v=2), (k=2,v=3)
  k=0: B第0列 → acc[0]+=2, acc[3]+=12
  k=2: B第2列 → acc[1]+=12, acc[3]+=21 (=33)
  → C第0行: (0,2), (1,12), (3,33)
```

- **零索引比较、零空跑、全部乘法命中**

### 1.5 汇编对比

**内积法（标量困局）**：

```asm
loop:
    cmp    r8, r9          ; 比较 colA vs colB
    jl     .L_inc_pa       ; 分支1
    jg     .L_inc_pb       ; 分支2
    vmulsd xmm0, xmm1, xmm2 ; 标量乘法！
    vaddsd xmm3, xmm3, xmm0 ; 标量加法！
```

- 分支密集 → 预测失败 → 流水线冲刷 15-20 cycle
- 向量宽度利用率 < 15%，IPC ≈ 0.5-1.5

**Gustavson（向量起飞）**：

```asm
    vbroadcastsd zmm0, [a_val]
loop:
    vmovupd   zmm1, [B_val + rsi]   ; 加载 8 个B值
    vgatherdpd zmm2, [acc + rdi*8]   ; 按索引收集8个acc值
    vfmadd213pd zmm2, zmm1, zmm0     ; 8路FMA一条指令！
    vscatterdpd [acc + rdi*8], zmm2  ; 写回8个acc
```

- 每周期 8 个乘加，仅 1 个分支（循环条件），零索引比较
- 有效利用率 > 80%，IPC ≈ 3.0-4.0

### 1.6 性能对比（10⁵ × 10⁵，每行/列 ~10 nz）

| 指标 | 内积法 | Gustavson |
|------|--------|-----------|
| 索引比较 | ~2×10⁸ | **0** |
| 分支指令 | ~2×10⁸ | ~10⁶ |
| 有效 FMA 吞吐 | 1-2 GFLOP/s | 30-40 GFLOP/s |
| 预估时间 | ~200 ms | **~25 ms** |

**加速比：~8×**

### 1.7 核心洞察

> Gustavson 的精髓：用 O(n) 的稠密累加器内存，把充满不规则比较和分支的稀疏计算，转化成了向量处理器最擅长的事：**batch load → gather → FMA×8 → scatter**。

---

## 2. 向量寄存器文件 (VRF)

### 2.1 什么是 VRF

向量寄存器文件 = 32 个**超宽**寄存器组成的阵列，每个寄存器一次存储多个数据元素，配合 SIMD 指令并行操作。

```
标量寄存器 (SRF):              向量寄存器 (VRF):
┌──────┐                         ┌────────────────────────────┐
│ x0   │ ← 1个值                 │ v0 │█│█│█│█│█│█│█│█│ ... │
│ x1   │                         │ v1 │█│█│█│█│█│█│█│█│ ... │
│ ...  │                         │ ...                          │
│ x31  │                         │ v31│█│█│█│█│█│█│█│█│ ... │
└──────┘                         └────────────────────────────┘
 1个值/寄存器                      16个值/寄存器 (512b÷32b)
```

### 2.2 论文中的 VRF 参数

| 参数 | 值 | 含义 |
|------|----|------|
| 寄存器数量 | 32 个 | v0 ~ v31 |
| 每寄存器位宽 | 512-bit | 可配置 (256/512/1024) |
| 向量长度 VL | 16 | 32-bit × 16 lanes |
| 执行通道 | 16 lanes | 16 个 32-bit ALU |
| 读写端口 | 3 读 + 1 写 | 支持三操作数指令 |

### 2.3 VRF vs SRF 数据布局对比

| 算法 | A 放哪里 | B 放哪里 | 特点 |
|------|---------|---------|------|
| Alg 1-S | VRF | VRF | 需要 VRF→SRF 搬运，慢 |
| Alg 2-S | SRF | VRF | 消除搬运，但 SRF 压力大 |
| Alg 3-S | VRF (vrgather) | VRF | 最优：全在 VRF 内操作 |

### 2.4 VRF 容量限制

```
VRF 总容量 = 32 × 512 bit = 2 KB
典型 CNN 权重矩阵 = 4 MB
差距：2000 倍

→ 必须分块 (tiling)
```

### 2.5 硬件结构

```
                  ┌── 写端口 ──┐
                  │            │
  ┌─────────────────────────────────────────────┐
  │              VRF 存储阵列                     │
  │  v0~v31, 每寄存器 16 lane                    │
  │         ↑        ↑        ↑                  │
  │    读端口0   读端口1   读端口2               │
  └─────────────────────────────────────────────┘
         │        │        │
         ↓        ↓        ↓
     ┌──────────────────────────┐
     │   16 条并行计算通道       │
     └──────────────────────────┘
```

---

## 3. 循环展开 (Loop Unrolling) 精读

### 3.1 Algorithm 3-S（原始版，未展开）

```
外循环 (i):  遍历 A 的每一行
  内循环 (j):  遍历当前行的每个非零元
    → 加载 B[col] → vrgather A值 → FMA
```

### 3.2 Algorithm 4（双展开 ×2）

**外循环展开**：一次处理 A 的 2 行 → 同时产出 C 的 2 行
**内循环展开**：一次处理 2 个非零元

```
外循环展开 (lines 2-6):
  for i=0 to numRows-1, i+=2:
      v0 = A[i,:]        ← 行 i
      v1 = A[i+1,:]      ← 行 i+1
      v6 = {0}           ← C[i]
      v7 = {0}           ← C[i+1]

内循环展开 + 交错 (lines 8-29):
  第一组(j):    第二组(j+1):
    s0=col[i,j]    s0=col[i,j+1]
    s1=col[i+1,j]  s1=col[i+1,j+1]
    vload v4,B[s0] vload v4,B[s0]  ← 复用 v4！
    vload v5,B[s1] vload v5,B[s1]  ← 复用 v5！
    vrgather v2    vrgather v2     ← 复用 v2,v3
    v6+=v2*v4      v6+=v2*v4
    v7+=v3*v5      v7+=v3*v5
```

### 3.3 交错执行 (Interleaving) 的作用

```
不交错：
  vload v4, ...    ← 发起 load
  v6 += v2*v4      ← 立即用 v4 → 等！流水线停转 (stall)

交错后：
  vload v4, ...    ← 发起 load
  vload v5, ...
  vrgather v2, ...
  vrgather v3, ...
  v6 += v2*v4      ← v4 已就绪，不空等
```

**寄存器复用**：第一组和第二组用同一套 v4, v5, v2, v3，避免 4 倍寄存器膨胀。

### 3.4 展开度与 VRF 压力

| 展开度 | VRF 用量 | 效果 |
|--------|---------|------|
| ×1 | 4 VR | 基线 |
| ×2 | 8 VR | 吞吐 ↑ |
| ×4 | 16 VR | 吞吐 ↑↑ |
| ×8 | 32 VR | 峰值，VRF 满 |
| ×16 | 不够用 | spill → 性能崩塌 |

论文 Fig.6 显示 ×8 附近到达峰值。

### 3.5 为什么需要外循环展开

1. **隐藏 load 延迟** — 多行交错填充流水线间隙
2. **复用计算** — block_id 计算共享
3. **提高 VRF 利用率** — 32 个寄存器只用 4 个太浪费
4. **指令级并行 (ILP)** — 多行操作的指令不互依赖，可并行发射

---

## 4. vindexmac 自定义指令

### 4.1 痛点

Algorithm 3-S 的瓶颈：**A 有多少个非零元，就要从内存 load 多少次 B 的行**。

### 4.2 核心洞察：结构化稀疏的"围栏"效应

```
非结构化稀疏：列号 ∈ [0, N-1]（无界）→ 无法预装 VRF
2:4 结构化稀疏：块内列号 ∈ [0, M-1]（有界）

A 的 block 0: col ∈ {0,1,2,3}   → B tile 0
A 的 block 1: col ∈ {0,1,2,3}   → B tile 0 (偏移后)
A 的 block 4: col ∈ {0,1,2,3}   → B tile 1
...
```

⇒ **B 的 L 行预装进 VRF，A 的对应 block 的非零元只指向这 L 行，命中率高！**

### 4.3 vindexmac 指令

```
格式: vindexmac.vx vd, vs2, rs
语义: vd[i] += vs2[0] × VRF[ rs[4:0] ][i]

三个操作数:
  vd:  目标/累加器 (向量)
  vs2: 存了A的非零元, 只用 vs2[0] (标量)
  rs:  标量寄存器, rs[4:0] 当 VRF 地址
```

### 4.4 Algorithm 5（基础版）

```
1. 预装 B 的 L 行到 VRF:       v0~v(L-1) = B[0..L-1, :]
2. 外循环遍历 A 的行:
     vload vs2, A[i,:]          ← A 行非零元
     vload vd, C[i,:]           ← C 行旧值
     内循环遍历非零元:
       block_id = floor(j/N)
       s1 = col_idx[i,j] + block_id*M
       vindexmac.vx vd, vs2, s1   ← 核心！
       vs2 = vs2 >> 1             ← 滑到下一个非零元
     vstore vd, C[i,:]
```

**vs Algorithm 3-S 的差异**：

| | Alg 3-S | Alg 5 |
|---|---|---|
| B 行获取 | vload (内存) | VRF 间接读 |
| 指令数/非零元 | 3 条 | 1 条 + 1 slide |

### 4.5 Algorithm 6（多 tile 版本）

当 A 一行 VL 个非零元跨越的列数 > L 时，需要换 tile：

```
外循环 (i):    遍历 A 的行
  中间循环 (m): 遍历 B 的 tile（每轮换 L 行 B）
    内循环 (j): 遍历当前 tile 覆盖的非零元

m = (M/N) × (VL/L)
  例：1:4稀疏, VL=16, L=16 → m = 4 个 tile
```

### 4.6 大矩阵的二维分块 (Fig.5)

```
              B 矩阵
          VL列  VL列  VL列
         ┌─────┬─────┬─────┐
         │seg0 │seg1 │seg2 │  垂直段
         └─────┴─────┴─────┘

每个垂直段执行 Algorithm 6 → 产出一个垂直段 C
多核：每核一个垂直段，C 无写冲突
```

### 4.7 硬件实现

```
改动：在读端口0地址总线上加一个 2→1 MUX

  vs1_addr(5-bit) ──→┐
  rs[4:0]        ──→┼──→ 读端口0
                   MUX

不增加读端口，不增加 ALU，不改变写回路径
面积：4 µm² vs VRF 本身 56,527 µm² (0.007%)
```

---

## 5. Fig.4 图解

Fig.4 展示 **vindexmac.vx vd, vs2, rs** 一条指令的完整数据通路。

### 数据流五阶段

```
  ① SRF                      ② VRF
┌────────┐              ┌──────────────┐
│  rs    │──地址──────→ │  v0~v31      │
│ rs[4:0]│  (如=5)      │  v5 ─→ 读出  │──→ 向量(B行)
└────────┘              └──────────────┘     │
                                              │
                        ③ 另一个寄存器        │
                    ┌──────────────┐          │
                    │  vs2         │          │
                    │ vs2[0] = A₀  │──→ 标量 ─┤
                    └──────────────┘          │
                                              │
                    ④ 乘法器                  │
                    ┌─────────────┐           │
                    │ 向量 × 标量  │←─────────┘
                    │ (广播乘)     │
                    └──────┬──────┘
                           │
                    ⑤ 累加器
                    ┌──────┴──────┐
                    │ vd(旧值) + 乘积│
                    │ → vd(新值)    │
                    └──────────────┘
```

### 与普通 FMA 的区别

```
普通 FMA:  vs1 寄存器号在指令编码里写死
vindexmac: VRF 地址由 rs[4:0] 运行时动态决定

唯一多出的硬件：读端口0地址总线上的 5-bit 2→1 MUX (4 µm²)
```

### 核心总结

> rs 里的值不当数值用，而是当"门牌号"——直接敲开 VRF 里对应编号的寄存器，取出内容 × vs2[0]，累加到 vd。一条指令，三个动作，一个额外 MUX。

---

## 附录：完整术语表

| 术语 | 含义 |
|------|------|
| VRF | Vector Register File，向量寄存器文件 |
| SRF | Scalar Register File，标量寄存器文件 |
| VL | Vector Length，向量长度（论文中 = 16） |
| FMA | Fused Multiply-Add，融合乘加 |
| ILP | Instruction-Level Parallelism，指令级并行 |
| SpGEMM | Sparse GEneral Matrix Multiply，稀疏矩阵乘法 |
| CSR/CSC | Compressed Sparse Row/Column，稀疏存储格式 |
| N:M | 每 M 个元素最多 N 个非零元（如 2:4） |
| MUX | Multiplexer，多路选择器 |
