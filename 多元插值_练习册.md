# 多元插值 — 专项练习

---

## 练习一：差商表 + Newton 插值（15分题型）

### 题目
已知二元函数 f(x,y) 在以下节点上的值：

|       | y=0 | y=1 | y=2 |
|-------|-----|-----|-----|
| x=0   | 0   | 1   | 8   |
| x=1   | 1   | 2   |     |
| x=2   | 8   |     |     |

（这是三角形网格上的数据）

1. 分别对 y=0 列和 x=0 行构造差商表
2. 计算混合差商 f[x₀,x₁; y₀,y₁]
3. 构造二元 Newton 插值公式并化简

---

### 解答思路

**第1步**：x方向差商表（固定 y=0）
```
x    f(x,0)   一阶差商     二阶差商
0    0
                (1-0)/(1-0)=1
1    1                       (3-1)/(2-0)=1
                (8-1)/(2-1)=7
2    8
```
x方向二阶差商 = 1

**第2步**：y方向差商表（固定 x=0）
```
y    f(0,y)   一阶差商     二阶差商
0    0
                (1-0)/(1-0)=1
1    1                       (7-1)/(2-0)=3
                (8-1)/(2-1)=7
2    8
```
y方向二阶差商 = 3

**第3步**：混合差商
f[x₀,x₁; y₀] = (f(1,0)-f(0,0))/(1-0) = 1
f[x₀,x₁; y₁] = (f(1,1)-f(0,1))/(1-0) = 1

f[x₀,x₁; y₀,y₁] = (f[x₀,x₁; y₁] - f[x₀,x₁; y₀])/(1-0) = 0

**第4步**：构造插值公式
```
P(x,y) = f₀₀ 
       + f[x₀,x₁; y₀]·x 
       + f[x₀,x₁,x₂; y₀]·x(x-1)
       + f[x₀; y₀,y₁]·y
       + f[x₀; y₀,y₁,y₂]·y(y-1)
       + f[x₀,x₁; y₀,y₁]·x·y
       + ...

带入选出的对角元：
P(x,y) = 0 + 1·x + 1·x(x-1) + 1·y + 3·y(y-1) + 0·xy + ...
       = x + x² - x + y + 3y² - 3y
       = x² + 3y² - 2y
```

**验证**：
- P(0,0)=0 ✓
- P(1,0)=1 ✓  
- P(0,1)=1 ✓
- P(2,0)=4... 等等 f(2,0)=8, 但 P(2,0)=4 ✗

这说明我算错了。让我仔细重算。

---

### 正确答案

实际上，观察数据：f(x,y) = x³ + y³？
- f(0,0)=0, f(1,0)=1, f(2,0)=8 ✓
- f(0,1)=1, f(0,2)=8 ✓
- f(1,1)=2... 但 1³+1³=2 ✓

所以 f(x,y) = x³ + y³

检查 f(1,2)：1³+8=9，但原数据没有这个点（三角形网格）。

重新用差商法：

x方向（y=0）：
x=0: 0, x=1: 1, x=2: 8
一阶差商：f[0,1;0]=1, f[1,2;0]=7
二阶差商：f[0,1,2;0]=(7-1)/(2-0)=3

y方向（x=0）：
y=0: 0, y=1: 1, y=2: 8  
一阶差商：f[0;0,1]=1, f[0;1,2]=7
二阶差商：f[0;0,1,2]=(7-1)/(2-0)=3

混合差商：
f[0,1;0]=1, f[0,1;1]=1
f[0,1;0,1]=0

f[0,1,2;0]=3, f[0,1,2;1]=(f[1,2;1]-f[0,1;1])/(2-0)
f[1,2;1] = (f(2,1)-f(1,1))/(2-1)
f(2,1)是未知的！这就是三角形网格的特点。

实际上对于三角形网格，x方向的三阶差商和y方向的三阶差商也是需要的。

对于 x³+y³：
P(x,y) = 0 + 1·x + 3·x(x-1) + 1·x(x-1)(x-2)
       + 1·y + 3·y(y-1) + 1·y(y-1)(y-2)

= x + 3x² - 3x + x³ - 3x² + 2x + y + 3y² - 3y + y³ - 3y² + 2y
= x³ + y³ ✓

---

## 练习二：Lagrange 插值（15分题型）

### 题目
已知数据：

|       | y=0 | y=1 | y=2 |
|-------|-----|-----|-----|
| x=0   | 1   | 2   | 5   |
| x=1   | 0   | 1   | 4   |

构造二元 Lagrange 插值多项式。

---

### 解答

**第1步**：x方向基函数（x₀=0, x₁=1）
```
l₀(x) = (x-1)/(0-1) = 1-x
l₁(x) = (x-0)/(1-0) = x
```

**第2步**：y方向基函数（y₀=0, y₁=1, y₂=2）
```
l₀(y) = (y-1)(y-2)/[(0-1)(0-2)] = (y²-3y+2)/2
l₁(y) = (y-0)(y-2)/[(1-0)(1-2)] = -y(y-2) = -y²+2y
l₂(y) = (y-0)(y-1)/[(2-0)(2-1)] = y(y-1)/2 = (y²-y)/2
```

**第3步**：构造二元插值
```
L(x,y) = Σᵢ₌₀¹ Σⱼ₌₀² f(xᵢ,yⱼ)·lᵢ(x)·lⱼ(y)
       = 1·(1-x)·(y²-3y+2)/2 + 2·(1-x)·(-y²+2y) + 5·(1-x)·(y²-y)/2
       + 0·x·(y²-3y+2)/2 + 1·x·(-y²+2y) + 4·x·(y²-y)/2
```

先合并 x=0 部分（即系数 (1-x) 的项）：
```
= (1-x)[(y²-3y+2)/2 - 2y² + 4y + 5(y²-y)/2]
= (1-x)[(y²-3y+2 - 4y² + 8y + 5y² - 5y)/2]
= (1-x)[(2y²)/2 + 0y + 1]
= (1-x)(y² + 1)
```

再合并 x=1 部分（系数 x 的项）：
```
= x[0 + (-y²+2y) + 4(y²-y)/2]
= x[-y² + 2y + 2y² - 2y]
= x·y²
```

最终：
```
L(x,y) = (1-x)(y²+1) + x·y² = y² + 1 - xy² - x + xy² = y² - x + 1
```

**验证**：
- L(0,0)=1 ✓, L(0,1)=2 ✓, L(0,2)=5 ✓
- L(1,0)=0 ✓, L(1,1)=1 ✓, L(1,2)=4 ✓

---

## 练习三：Aitken 迭代（15分题型）

### 题目
在矩形四个角点和中心点上有以下函数值：
- A(1,1): f=1, B(1,-1): f=1, C(-1,-1): f=1, D(-1,1): f=1
- E(1,0): f=2, F(0,-1): f=1, G(-1,0): f=2, H(0,1): f=1
- q(0,0): f=1

用 Aitken 法求插值多项式（可以分步做）。

---

### 解答

**第1步**：对 y=-1 行做 x 方向插值
点：C(-1,-1):1, F(0,-1):1, B(1,-1):1 → 值为常数，P_{y=-1}(x)=1

对 y=1 行：
点：D(-1,1):1, H(0,1):1, A(1,1):1 → P_{y=1}(x)=1

对 y=0 行（需要 Aitken）：
点：G(-1,0):2, q(0,0):1, E(1,0):2

先求 P_{G,q}(x)：
```
P_{G,q}(x) = det([2  -1-x]) / (0-(-1))
           = det([1   0-x])
           = [2(0-x) - 1(-1-x)] / 1
           = -2x + 1 + x
           = 1 - x
```

再求 P_{q,E}(x)：
```
P_{q,E}(x) = det([1  0-x]) / (1-0)
           = det([2  1-x])
           = [1(1-x) - 2(0-x)] / 1
           = 1 - x + 2x
           = 1 + x
```

再求 P_{G,q,E}(x)：
```
P_{y=0}(x) = det([1-x  -1-x]) / (1-(-1))
           = det([1+x   1-x])
           = [(1-x)(1-x) - (1+x)(-1-x)] / 2
           = [1 - 2x + x² + 1 + 2x + x²] / 2
           = (2 + 2x²) / 2
           = 1 + x²
```

**第2步**：在 y 方向用 Aitken 整合
已知：y=-1 → 1, y=0 → 1+x², y=1 → 1

```
P_{y=-1,0} = det([1  -1-y]) / (0-(-1))
           = det([1+x²  0-y])
           = [1(0-y) - (1+x²)(-1-y)] / 1
           = -y + (1+x²)(1+y)
           = -y + 1 + x² + y + x²y
           = 1 + x² + x²y

P_{y=0,1} = det([1+x²  0-y]) / (1-0)
          = det([1     1-y])
          = [(1+x²)(1-y) - 1(0-y)] / 1
          = 1 + x² - y - x²y + y
          = 1 + x² - x²y

P(x,y) = det([1+x²+x²y  -1-y]) / (1-(-1))
       = det([1+x²-x²y   1-y])
       = [(1+x²+x²y)(1-y) - (1+x²-x²y)(-1-y)] / 2
```

展开分子：
```
第一项：(1+x²+x²y)(1-y) = 1+x²+x²y - y - x²y - x²y² = 1 + x² - y - x²y²
第二项：(1+x²-x²y)(-1-y) = -(1+x²-x²y) - y(1+x²-x²y)
         = -1 - x² + x²y - y - x²y + x²y²
         = -1 - x² - y + x²y²

分子 = (1+x²-y-x²y²) - (-1-x²-y+x²y²)
     = 1 + x² - y - x²y² + 1 + x² + y - x²y²
     = 2 + 2x² - 2x²y²

P(x,y) = (2+2x²-2x²y²)/2 = 1 + x² - x²y²
```

---

## 练习四：论述题 — 多元插值存在唯一性

**请用自己的话回答以下问题：**

1. 一元多项式插值为何总存在唯一解？
2. 多元多项式插值为什么不一定存在、不一定唯一？
3. 列举三种解决多元插值适定性问题的方法，并简要说明原理。

### 参考答案要点

1. **一元插值**：n+1 个互异节点 → Vandermonde 行列式 ≠ 0 → 存在唯一的 ≤n 次插值多项式。这就是 Haar 条件。

2. **多元插值**：在 Rᵈ(d≥2) 中，Haar 条件不成立。例如平面上取一直线上的三点做一次插值 → 无限多解，取圆上六点做二次插值 → 可能无解。

3. **解决方法**：
   - **特殊节点几何构型**：矩形张量积网格、三角形单纯形等
   - **径向基函数(RBF)**：Micchelli 定理保证正定性，无需网格
   - **多元样条/有限元**：分片多项式，每片内保证适定

---

## 练习五：论述题 — RBF 优缺点

**请从以下角度分析RBF相对于多元多项式插值的优缺点：**
- 节点分布要求
- 存在唯一性
- 计算复杂度
- 参数敏感性
- 外推能力

### 参考答案要点

| 对比维度 | 多元多项式插值 | RBF插值 |
|----------|---------------|---------|
| 节点分布 | 严格要求网格 | 无限制（散乱数据） |
| 存在唯一性 | 有条件的 | Micchelli定理保证 |
| 计算复杂度 | O(N³)但可递归降阶 | O(N³)全局稠密 |
| 参数敏感性 | 无参数 | 形状参数极敏感 |
| 外推能力 | 多项式外推发散 | 随距离衰减，外推弱 |
| 维数影响 | 节点数指数增长 | 仅依赖距离，与维数无关 |

---

*准备好后可以开始答题练习！*
