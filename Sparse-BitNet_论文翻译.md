# Sparse-BitNet：1.58-bit 大语言模型天然适应半结构化稀疏

> **原文标题**: Sparse-BitNet: 1.58-bit LLMs are Naturally Friendly to Semi-Structured Sparsity  
> **arXiv**: [2603.05168](https://arxiv.org/abs/2603.05168)  
> **代码**: [github.com/AAzdi/Sparse-BitNet](https://github.com/AAzdi/Sparse-BitNet)

**作者**: Di Zhang¹ ², Xun Wu¹, Shaohan Huang¹, Yudong Wang², Hanyong Shao¹ ², Yingbo Hao¹ ³, Zewen Chi¹, Li Dong¹, Ting Song¹, Yan Xia¹, Zhifang Sui², Furu Wei¹

**机构**: ¹ 微软研究院 &emsp; ² 北京大学 &emsp; ³ 华南理工大学

---

## 摘要

半结构化 N:M 稀疏和低比特量化（如 1.58-bit BitNet）是提升大语言模型（LLMs）效率的两种有前景的方法，但它们此前大多被独立研究。本工作探索了二者的相互作用，并证明 1.58-bit BitNet 天然比全精度模型更兼容 N:M 稀疏。为此，我们提出了 **Sparse-BitNet**——一个首次实现 1.58-bit 量化与动态 N:M 稀疏化联合训练的统一框架，并确保训练稳定。在多种模型规模和训练范式（稀疏预训练和稠密→稀疏训练计划）下，1.58-bit BitNet 在相同稀疏度水平下始终表现出比全精度基线更小的性能退化，并能容忍更高的结构化稀疏度。此外，使用我们自定义的稀疏 Tensor Core，Sparse-BitNet 在训练和推理中均实现了显著加速，最高可达 **1.30×**。这些结果表明，将极低比特量化与半结构化 N:M 稀疏相结合是大语言模型效率提升的一个有前景的方向。

---

## 1 引言

大语言模型在各类任务中展现了卓越的性能，但其急剧增长的规模导致了巨大的训练和推理成本，效率成为现代 LLM 研究的核心挑战。在现有方法中，**量化**（AWQ、GPTQ、LLM.int8()）和**稀疏化**（Sparse Transformers、torchao、2:4 稀疏预训练）是两种被广泛研究的有效策略。

**半结构化 N:M 稀疏**，尤其是被广泛支持的 2:4 模式，因能够在 NVIDIA Sparse Tensor Core 上加速矩阵乘法而备受关注——它要求每 4 个连续权重中最多有 2 个非零值。然而，现有工作主要集中在将半结构化稀疏应用于全精度 LLM。在严格的 N:M 约束下，这些全精度模型往往出现快速的精度退化，难以同时达到高稀疏度和高性能。

**极低比特量化**则提供了另一条效率提升路径。特别是 1.58-bit BitNet，它将权重量化到三元集合 {−1, 0, 1}，在大规模下达到了与全精度基线相当的性能。

**核心观察**：如图 1 所示，预训练的 1.58-bit BitNet 权重展现出独特的"量化谷"结构——约 42% 的权重天然为零，在无需显式剪枝的情况下就形成了稀疏表示。尽管这些零值是非结构化的（无法直接利用 N:M 稀疏 kernel 加速），但它们揭示了 BitNet 权重天然更适合基于幅度的 N:M 稀疏选择。

由此引出一个核心研究问题：**在相同的 N:M 稀疏约束下，1.58-bit BitNet 是否比全精度模型更稀疏友好？**

### 本文贡献

1. **首次系统研究发现**极低比特量化天然比全精度模型更兼容半结构化 N:M 稀疏
2. 提出 **Sparse-BitNet 训练框架**，联合集成 1.58-bit 量化和 N:M 稀疏，提升训练稳定性和鲁棒性
3. 大量实验表明 Sparse-BitNet 在精度-效率权衡上优于稀疏全精度基线，并获得显著端到端加速

---

## 2 Sparse-BitNet 方法

### 2.1 背景知识

**1.58-bit BitNet。** BitNet b1.58 将权重约束为三元集合 T = {−1, 0, +1}，信息密度约为 log₂3 ≈ 1.58 比特/参数。基本模块是 BitLinear 层。对于权重矩阵 W ∈ ℝ^(d_out × d_in)，三元权重 W_q 通过以下方式获得：

```
W_q = RoundClip(W / (γ + ε), −1, +1)
```

其中 γ = ||W||₁ / (d_out · d_in) 作为缩放因子。激活值使用 absmax 缩放量化为 8-bit 整数。前向传播近似为 y ≈ γ · matmul(W_q, x̃)，用整数加法和缩放替代了浮点乘法。

**半结构化 N:M 稀疏。** 半结构化稀疏要求每 M 个连续权重中至多 N 个非零。对权重矩阵 W，定义二元掩码 M ∈ {0,1}^(d_out × d_in)，使得每 M 个元素中恰好有 N 个为 1。稀疏计算为 y = (W ⊙ M) x。本文主要使用 **6:8 模式**（25% 稀疏度），同时也对比标准 2:4 模式。

### 2.2 Sparse-BitLinear 架构

Sparse-BitNet 将标准线性投影替换为 **Sparse-BitLinear** 层，该层将三值量化和 N:M 掩码组合为单一算子。训练期间维护高精度主权重 W（如 bf16）用于累积梯度。

**基于幅度的掩码生成。** 直接从主权重 W 计算掩码 M_N:M，采用幅度剪枝：对每个大小为 M 的连续组，选择绝对值最大的 N 个索引。

```
M_N:M = Π_N:M(|W|)
```

其中 Π_N:M(·) 是每组 Top-N 指示函数。关键是掩码基于**量化前的权重**计算，保留细粒度幅度排序，避免了在离散三值上做掩码导致的平局问题。

**"先量化再掩码"计算流程。** 前向传播遵循"先量化再掩码"范式：
1. 激活值 x 量化为 x̃（通常 8-bit）
2. 主权重 W 量化为三值 W_q = Q_t(W) ∈ {−1, 0, +1}
3. 将掩码施加到量化权重：W_eff = W_q ⊙ M_N:M
4. 输出 y ≈ s · (W_eff x̃)

### 2.3 训练策略

**动态掩码重计算。** 不同于后训练剪枝方法，我们**每一步训练都重新计算 M_N:M**，对应一种投影优化方法，防止掩码过时，允许网络拓扑随权重值共同演化。

**Dual STE 梯度估计。** 由于量化函数 Q_t(·) 和掩码选择 Π_N:M(·) 都不可微，我们使用**双重直通估计器（Dual STE）**：

1. 对于三值量化器 Q_t，在截断范围内将梯度恒等传递（标准 BitNet 训练方式）
2. **关键创新**：也对稀疏掩码使用 STE——在反向传播中**不屏蔽梯度**：

```
∂L/∂W = ∂L/∂W_eff · ∂W_eff/∂W ≈ ∂L/∂W_eff
```

这意味着**梯度流向所有主权重，包括被剪枝（掩码掉）的权重**。这不同于用掩码门控梯度的方法（∂L/∂W ≈ M ⊙ ∂L/∂W_eff），后者仅更新活跃权重集。通过允许稠密梯度更新，被剪枝的权重也能接收直接反馈，有可能在下一次迭代中重新进入 Top-N 集合，防止过早的结构坍塌。

**训练算法：**

```
算法 1: 训练 Sparse-BitNet（三值 + 逐步掩码 + Dual STE）

要求: 数据 D，主权重 W，模式 (N, M)

for step t = 1, 2, ... do
    B ← 采样(D)
    M_N:M ← Π_N:M(|W|)                     // 计算掩码
    x̃ ← Q_a(Norm(x))                      // 激活量化
    W_q ← Q_t(W)                          // 权重量化
    W_eff ← W_q ⊙ M_N:M                    // 施加掩码
    y ≈ s · W_eff x̃                        // 前向传播
    反向:
        (1) 对 Q_t(·) 使用 STE
        (2) 对 Π_N:M(·) 使用 STE（不屏蔽梯度）
    通过优化器更新所有主权重 W
end for
```

---

## 3 实验

### 3.1 实验设置

- **骨干模型**: Qwen2.5 系列（0.5B、1.5B、3B），均从头训练
- **稀疏模式**: 6:8 半结构化稀疏（25% 稀疏度），同时对比 2:4
- **训练数据**: RefineWeb，约 50B tokens/模型
- **训练目标**: 标准因果语言建模（下一 token 预测）
- **评估**: 困惑度（PPL）+ 五个下游基准（HellaSwag、ARC-E、PIQA、BoolQ、COPA）
- **效率测试**: NVIDIA A100 和 B200 GPU

### 3.2 主要结果

**表 1: 下游任务性能评估**

| 方法 | HellaSwag | ARC-E | PIQA | BoolQ | COPA | 平均分 | Δ |
|---|---|---|---|---|---|---|---|
| **Qwen2.5-0.5B** | | | | | | | |
| Dense BF16 | 40.45 | 43.31 | 69.04 | 60.12 | 71.00 | 56.78 | – |
| Sparse BF16 (6:8) | 39.21 | 39.84 | 66.43 | 57.32 | 66.00 | 53.76 | **-3.02** |
| Dense BitNet | 35.27 | 40.70 | 65.07 | 59.24 | 69.00 | 53.86 | – |
| Sparse BitNet (6:8) | 34.95 | 37.63 | 63.87 | 59.08 | 68.00 | 52.71 | **-1.15** |
| **Qwen2.5-1.5B** | | | | | | | |
| Dense BF16 | 49.32 | 48.65 | 72.47 | 60.28 | 71.00 | 60.34 | – |
| Sparse BF16 (6:8) | 40.44 | 39.73 | 67.19 | 47.77 | 68.00 | 52.63 | **-7.71** |
| Dense BitNet | 44.64 | 44.61 | 70.29 | 57.43 | 70.00 | 57.39 | – |
| Sparse BitNet (6:8) | 36.95 | 40.57 | 65.23 | 55.26 | 70.00 | 53.60 | **-3.79** |
| **Qwen2.5-3B** | | | | | | | |
| Dense BF16 | 54.88 | 51.77 | 73.67 | 61.56 | 75.00 | 63.38 | – |
| Sparse BF16 (6:8) | 52.87 | 50.52 | 71.58 | 53.91 | 72.00 | 60.18 | **-3.20** |
| Dense BitNet | 50.46 | 48.23 | 71.93 | 53.18 | 70.00 | 58.76 | – |
| Sparse BitNet (6:8) | 51.20 | 47.32 | 71.93 | 51.35 | 68.00 | 57.96 | **-0.80** |

**表 2: 困惑度退化分析**

| 方法 | 0.5B PPL↓ | 1.5B PPL↓ | 3B PPL↓ |
|---|---|---|---|
| Dense BF16 | 21.91 | 18.10 | 16.03 |
| Sparse BF16 (6:8) | 23.11 (+1.20) | 18.70 (+0.60) | 16.48 (+0.45) |
| Dense BitNet | 25.99 | 20.11 | 17.70 |
| Sparse BitNet (6:8) | **26.31 (+0.32)** | **20.35 (+0.24)** | **17.87 (+0.17)** |

**核心发现**：

1. **Sparse-BitNet 跨尺度始终更鲁棒**：6:8 稀疏时 PPL 增加仅为 BF16 的 1/4 到 1/3（BF16: +1.20/+0.60/+0.45 vs BitNet: +0.32/+0.24/+0.17）
2. **下游任务趋势一致**：BitNet 平均准确率下降（Δ）为 1.15/3.79/0.80，而 BF16 为 3.02/7.71/3.20
3. **BitNet 表现出"延迟坍塌"**：在 N:8 稀疏度扫描中，BF16 在 4:8 时突破 10% 退化阈值，BitNet 直到 3:8 时才突破。在 2:4 模式下：BF16 退化 +18.8%，BitNet 仅 +5.7%

### 3.3 消融实验

**动态 6:8 掩码的优化设计选择。** 对 Qwen2.5-0.5B + 6:8 稀疏，比较四种变体：

| 变体 | 描述 | PPL↓ |
|---|---|---|
| **Baseline (Ours)** | 先量化再掩码 + 稠密梯度 | **26.31** |
| Mask without grad | 屏蔽被掩码权重的梯度 | 更高 |
| Mask from quantized weight | 从三值权重选 Top-N | 32.23（严重退化） |
| Sparse before quant | 先掩码再量化 | 次优 |

**关键结论**：
- 阻塞掩码权重的梯度 → 掩码过早冻结，限制探索
- 从三值权重构造掩码 → 大量平局（0 和 ±1），Top-N 选择不稳定
- 先掩码再量化 → 量化噪声与稀疏子集耦合，阻碍有效探索

**稠密→稀疏训练计划。** 两阶段训练实验（先稠密训练，再切换 6:8 稀疏）：

| 稀疏比例 η (%) | 验证 PPL↓ |
|---|---|
| 25 | 27.48 |
| 50 | 27.39 |
| 75 | 26.71 |
| 100 (从头稀疏) | **26.31** |
| 0 (全稠密) | 25.99 |

**结论**：推迟稀疏化会导致持续的模型质量差距，**有效的 6:8 训练需要充足的稀疏适应预算**，从头稀疏训练效果最好。

### 3.4 机制分析

**三值 QAT 诱导极化而非单峰收缩。** 分析稠密训练期间潜在权重的动态：
- BF16 基线：权重持续集中在零附近，形成单峰分布，"重要"和"冗余"权重纠缠在一起
- **BitNet**：表现出明显的**极化趋势**——近零质量随时间显著下降，潜在权重主动从模糊区域向外迁移，形成结构化的三峰直方图

**每块阈值与活跃权重解耦。** N:M 稀疏由每块阈值 τ = |W|_(N+1) 决定。分析发现：
- BF16：阈值分布紧密跟随权重分布，剪枝边界频繁切入权重主体，不可避免地移除重要信息
- **BitNet**（尤其是深层）：展现出"幅度分层"——权重分布发展出高幅度"活跃"模式，而阈值分布集中在低幅度区域并快速下降，**剪枝边界几乎只在噪声/冗余参数空间中操作**，保护了高幅度活跃权重

### 3.5 端到端性能

**表 3: Qwen2.5-3B 推理速度提升**

| 配置 | 稠密吞吐量 (ktok/s) | 稀疏吞吐量 (ktok/s) | 加速比 |
|---|---|---|---|
| **Prefill (A100)** | | | |
| SeqLen 512 | 9.7k | 10.6k | 1.09× |
| SeqLen 4096 | 40.9k | 52.2k | 1.28× |
| SeqLen 65536 | 42.7k | 55.5k | **1.30×** |
| **Decode (B200)** | | | |
| Batch 64 | 11.1k | 12.2k | 1.09× |
| Batch 128 | 17.2k | 20.4k | 1.18× |
| Batch 512 | 30.4k | 34.4k | 1.13× |

---

## 4 相关工作

### 4.1 量化与 BitNet 架构

量化方法通过压缩权重来优化效率。BitNet 系列代表了极低比特量化的前沿：标准 BitNet 使用 1-bit 权重，BitNet b1.58 扩展为三值 {−1, 0, 1} 方案。引入零值显著增强了容量，达到了与全精度模型相当的性能，有效弥合了极端量化与标准架构之间的差距。

### 4.2 模型剪枝

- **非结构化剪枝**：针对低幅度个体权重，可达高稀疏度但缺乏硬件加速
- **N:M 稀疏**：引入细粒度结构约束以支持硬件友好加速
- **结构化剪枝**：移除注意力头或行列等连贯组件，保证推理加速但精度损失大，需昂贵重训练

**本文区别于现有方法**：不是后训练后做剪枝，而是在**训练过程中动态引入稀疏**——不仅用于推理部署，也能显著提升训练效率。

---

## 5 结论

我们提出了 **Sparse-BitNet**，一个统一框架，证明了 1.58-bit 三值大语言模型天然比 BF16 模型更鲁棒于半结构化稀疏。通过将动态 6:8 掩码与三值量化集成，我们的方法显著降低了性能退化，并延迟了不同规模下的模型坍塌。

消融实验强调了两个关键设计：
1. 从稠密主权重构造掩码
2. 保持梯度流经掩码区域

此外，我们的自定义 6:8 kernel 实现了最高 **1.30×** 的推理加速，证实了将极端量化与结构化剪枝相结合为高效 LLM 部署提供了一条可行的帕累托前沿。

---

## 附录

### A. 实验配置

**训练超参数**：AdamW 优化器（β₁=0.9, β₂=0.95），学习率 1e-5，余弦调度，warmup 比 0.5，权重衰减 0.1，微批量 16，梯度累积 4，序列长度 2048，梯度裁剪 1.0，BF16 精度。所有变体使用相同的 tokens、数据混合和优化器设置。

### B. 伪代码实现

```python
class WeightQuantMasked(torch.autograd.Function):
    @staticmethod
    def forward(ctx, w, N, M):
        mask = compute_nm_mask(w, N, M)
        scale = w.abs().mean().clamp(min=1e-5)
        w_q = (w / scale).round().clamp(-1, 1) * scale
        w_masked = w_q * mask
        return w_masked

    @staticmethod
    def backward(ctx, g_out):
        # Dual-STE: 梯度直接流向稠密权重
        return g_out, None, None

class SparseBitLinear(nn.Linear):
    def forward(self, x):
        s_x = 127.0 / x.abs().max(dim=-1, keepdim=True).values.clamp(min=1e-5)
        x_q = (x * s_x).round().clamp(-128, 127) / s_x
        w_q = WeightQuantMasked.apply(self.weight, self.N, self.M)
        return F.linear(x_q, w_q, self.bias)
```

### C. 稀疏度扫描完整 PPL 数据

| 方法 | 8:8 | 7:8 | 6:8 | 5:8 | 2:4 | 3:8 | 2:8 |
|---|---|---|---|---|---|---|---|
| BitNet | 25.99 | 26.12 | 26.31 | 26.71 | 27.48 | 29.80 | 33.12 |
| BF16 | 21.91 | 22.27 | 23.11 | 23.42 | 26.03 | 28.66 | 31.70 |

---

> **翻译说明**: 本文翻译基于 arXiv 2603.05168v1 版本，力求准确传达原文含义。专业术语保留英文注释以便查阅。表格和公式保持原文格式。
